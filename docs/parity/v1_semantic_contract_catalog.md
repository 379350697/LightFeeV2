# V1 Semantic Contract Catalog

> Auto-generated by Coordinator Gate 0. Each entry defines the business contract that V1
> establishes and V2 must satisfy. Coverage is enforced by
> `tests/parity/test_contract_catalog_coverage.py`.

## Categories

| # | Category | Contract Count |
|---|----------|---------------|
| 1 | config | 6 |
| 2 | startup | 1 |
| 3 | runtime-lanes | 1 |
| 4 | opportunity-input | 4 |
| 5 | venue-capabilities | 2 |
| 6 | market-data | 1 |
| 7 | local-l2 | 1 |
| 8 | entry | 3 |
| 9 | close | 1 |
| 10 | passive-close | 1 |
| 11 | risk | 1 |
| 12 | state | 3 |
| 13 | journal | 1 |
| 14 | recovery | 1 |
| 15 | replay | 1 |
| 16 | offline-analysis | 1 |
| 17 | evolution | 1 |
| 18 | ops | 1 |

---

## 1. config

### CONFIG-001: Directed Pairs Restrict Pair Direction

- **area:** config
- **V1 source anchors:**
  - `src/runtime_state/config.rs` — `DirectedPairsConfig`, pair direction filtering
- **V2 source anchors:**
  - `lightfee/config/schema.py` — `RuntimeConfig.directed_pairs`
  - `lightfee/config/loader.py` — config loading
- **observable behavior:** `directed_pairs` must restrict pair direction independently from global symbols. A pair not in `directed_pairs` cannot be traded even if both symbols appear in `daily_universe`.
- **required state fields:** `RuntimeConfig.directed_pairs: list[tuple[Symbol, Symbol]]`
- **required journal/event kinds:** `config_validated`
- **focused test path:** `tests/config/test_v1_config_semantics.py::test_directed_pairs_restrict_direction`
- **deviation id:** —

### CONFIG-002: Daily Universe with Fallback-to-Last-Good

- **area:** config
- **V1 source anchors:**
  - `src/runtime_state/config.rs` — `DailyUniverseConfig` (enabled, generation_time, max_symbols, fallback_to_last_good, path)
- **V2 source anchors:**
  - `lightfee/config/schema.py` — daily universe fields
  - `lightfee/config/universe.py` — (to be created)
- **observable behavior:** Daily universe supports enablement, generation time, max symbols, fallback-to-last-good, and path resolution. When the current day's universe is missing or invalid and fallback is enabled, the last good universe is used.
- **required state fields:** `DailyUniverseConfig.enabled`, `.generation_time`, `.max_symbols`, `.fallback_to_last_good`, `.path`
- **required journal/event kinds:** `daily_universe_loaded`
- **focused test path:** `tests/config/test_v1_config_semantics.py::test_daily_universe_fallback_to_last_good`
- **deviation id:** —

### CONFIG-003: Runtime Opportunity Modes

- **area:** config
- **V1 source anchors:**
  - `src/runtime_state/config.rs` — `OpportunityMode` enum (DirectMarket, CoarseSidecar, SidecarScan, Disabled)
  - `src/opportunity_input/` — mode dispatch
- **V2 source anchors:**
  - `lightfee/config/schema.py` — opportunity mode config
- **observable behavior:** Runtime distinguishes direct market, coarse sidecar, sidecar scan, disabled, and explicit non-parity fallback modes. The mode determines which opportunity provider is constructed and whether the runtime enters a degraded state.
- **required state fields:** `RuntimeConfig.opportunity_mode: enum`
- **required journal/event kinds:** `opportunity_mode_selected`
- **focused test path:** `tests/config/test_v1_config_semantics.py::test_opportunity_modes`
- **deviation id:** —

### CONFIG-004: Directed Pairs Validation at Config Load

- **area:** config
- **V1 source anchors:**
  - `src/runtime_state/config.rs` — config validation
- **V2 source anchors:**
  - `lightfee/config/loader.py` — validation logic
- **observable behavior:** Missing or contradictory config fails before runtime starts. Invalid generation time, zero max_symbols, and missing paths are all hard errors.
- **required state fields:** n/a (validation, not state)
- **required journal/event kinds:** `config_invalid` (error log)
- **focused test path:** `tests/config/test_v1_config_semantics.py::test_config_validation_errors`
- **deviation id:** —

### CONFIG-005: Max Symbols Enforcement

- **area:** config
- **V1 source anchors:**
  - `src/runtime_state/config.rs` — `max_symbols` validation
- **V2 source anchors:**
  - `lightfee/config/schema.py` — `max_symbols`
  - `lightfee/config/universe.py` — symbol selection
- **observable behavior:** Runtime cannot silently trade symbols outside `directed_pairs` or exceeding `max_symbols`. Symbol count is validated at config load and at daily universe refresh.
- **required state fields:** `DailyUniverseConfig.max_symbols`
- **required journal/event kinds:** `symbol_limit_enforced`
- **focused test path:** `tests/config/test_v1_config_semantics.py::test_max_symbols_enforcement`
- **deviation id:** —

### CONFIG-006: Path Resolution for Universe Files

- **area:** config
- **V1 source anchors:**
  - `src/runtime_state/config.rs` — path resolution in `DailyUniverseConfig`
- **V2 source anchors:**
  - `lightfee/config/loader.py` — path resolution
- **observable behavior:** Universe file paths are resolved relative to the config directory or an absolute root. Relative paths without a resolvable root are a hard error.
- **required state fields:** `DailyUniverseConfig.path`
- **required journal/event kinds:** `config_validated`
- **focused test path:** `tests/config/test_v1_config_semantics.py::test_path_resolution`
- **deviation id:** —

---

## 2. startup

### STARTUP-001: Ordered Startup Phases

- **area:** startup
- **V1 source anchors:**
  - `src/main.rs` — `build_opportunity_input_provider`, startup ordering
  - `src/app_runtime/bootstrap.rs` — bootstrap phases
- **V2 source anchors:**
  - `lightfee/engine/bootstrap.py` — bootstrap sequence
  - `lightfee/apps/live.py` — live entry point
- **observable behavior:** Startup phases execute in order: config validation, symbol preparation, adapter construction, adapter prewarm, opportunity provider construction, local-L2 activation, recovery, and a `started` journal event. Each phase must complete before the next begins. A phase failure blocks startup.
- **required state fields:** `EngineLifecycle` transitions through BOOTING → RECONCILING → RUNNING
- **required journal/event kinds:** `startup_phase_begin`, `startup_phase_complete`, `started`
- **focused test path:** `tests/engine/test_startup_activation_semantics.py::test_ordered_startup_phases`
- **deviation id:** —

---

## 3. runtime-lanes

### LANE-001: Independent Lane Scheduling with Failure Isolation

- **area:** runtime-lanes
- **V1 source anchors:**
  - `src/execution_core/engine/engine.rs` — tick scheduling (tokio::select)
  - `src/app_runtime/loop_control.rs` — metrics export, state snapshots
- **V2 source anchors:**
  - `lightfee/engine/runtime.py` — asyncio runtime
  - `lightfee/engine/loop_control.py` — lane control
- **observable behavior:** Full tick, active tick, maker-event tick, rate-limit reload, SIGHUP reload, and shutdown are independently scheduled. A slow or failing full tick does not prevent active or maker-event lane eligibility. Per-lane failure backoff does not block unrelated lanes.
- **required state fields:** per-lane `last_run`, `last_error`, `backoff_until`
- **required journal/event kinds:** `lane_tick_begin`, `lane_tick_end`, `lane_error`
- **focused test path:** `tests/engine/test_runtime_lane_scheduling.py::test_lane_failure_isolation`
- **deviation id:** —

---

## 4. opportunity-input

### OPP-001: Sidecar Snapshot Freshness Semantics

- **area:** opportunity-input
- **V1 source anchors:**
  - `src/opportunity_input/snapshot.rs` — snapshot freshness
- **V2 source anchors:**
  - `lightfee/sidecar/snapshot.py` — snapshot management
- **observable behavior:** Sidecar snapshots have five freshness states: fresh, last-good fallback, stale, missing, and degraded. Each state has a distinct runtime behavior: fresh snapshots are used directly; last-good fallback is used when the current snapshot is stale but a recent valid one exists; stale triggers a warning; missing blocks trading; degraded domains (market, transfer, hint, perp-liquidity) are individually tracked.
- **required state fields:** `snapshot.freshness`, `snapshot.generated_at`, `snapshot.degraded_domains`
- **required journal/event kinds:** `sidecar_snapshot_loaded`, `sidecar_snapshot_stale`, `sidecar_snapshot_fallback`
- **focused test path:** `tests/sidecar/test_opportunity_input_semantics.py::test_snapshot_freshness_states`
- **deviation id:** —

### OPP-002: Health Domain Diagnostics

- **area:** opportunity-input
- **V1 source anchors:**
  - `src/opportunity_input/` — health domain tracking
- **V2 source anchors:**
  - `lightfee/sidecar/` — health domain models
- **observable behavior:** Market, transfer, hint, and perp-liquidity health domains survive into runtime diagnostics. Each domain can independently be healthy, degraded, or missing. Degraded domains are visible in current-state export and metrics.
- **required state fields:** `health_domains: dict[str, HealthStatus]` per domain
- **required journal/event kinds:** `health_domain_changed`
- **focused test path:** `tests/sidecar/test_opportunity_input_semantics.py::test_health_domain_diagnostics`
- **deviation id:** —

### OPP-003: Sidecar Scan Mode Discovery

- **area:** opportunity-input
- **V1 source anchors:**
  - `src/opportunity_input/` — live scan, sidecar-backed scan
- **V2 source anchors:**
  - `lightfee/sidecar/pairing.py` — pair discovery
- **observable behavior:** In sidecar-scan mode, pairs are discovered from the sidecar snapshot rather than direct market queries. Pairs outside `directed_pairs` are filtered. Disabled mode produces an explicit no-op (not silent omission).
- **required state fields:** `discovered_pairs`, `filtered_pairs`
- **required journal/event kinds:** `pair_discovery_complete`
- **focused test path:** `tests/sidecar/test_opportunity_input_semantics.py::test_sidecar_scan_discovery`
- **deviation id:** —

### OPP-004: Chillybot Removal as Approved Deviation

- **area:** opportunity-input
- **V1 source anchors:**
  - `src/chillybot/` — Chillybot feed integration
- **V2 source anchors:**
  - n/a (intentionally removed)
- **observable behavior:** V1 Chillybot-era inputs are intentionally removed in V2. This is a deliberate deviation, not a gap.
- **required state fields:** n/a
- **required journal/event kinds:** n/a
- **focused test path:** n/a
- **deviation id:** DEV-001

---

## 5. venue-capabilities

### VENUE-001: Capability Matrix Parity

- **area:** venue-capabilities
- **V1 source anchors:**
  - `src/market_gateway/ports.rs` — venue capability declarations
  - `src/market_gateway/` — per-venue capability registration
- **V2 source anchors:**
  - `lightfee/venues/specs.py` — `VenueSpec`
  - `lightfee/venues/base.py` — capability enums
  - `lightfee/venues/capabilities.py` — (to be created)
- **observable behavior:** Every venue declares supported capabilities. The capability matrix must match V1 unless an approved deviation exists. Key drift: V1 marks Bitget and Gate `risk_health` as unsupported; V2 currently marks them supported. Must fix or create explicit approved deviation.
- **required state fields:** `VenueCapabilities` per venue
- **required journal/event kinds:** `venue_capability_validated`
- **focused test path:** `tests/venues/test_v1_capability_matrix.py::test_capability_matrix_matches_v1`
- **deviation id:** DEV-002 (Bitget/Gate risk_health drift)

### VENUE-002: Venue Adapter Contract Completeness

- **area:** venue-capabilities
- **V1 source anchors:**
  - `src/market_gateway/ports.rs` — full adapter trait
- **V2 source anchors:**
  - `lightfee/core/contracts.py` — `VenueAdapter` ABC
- **observable behavior:** The venue adapter contract must cover: private health, private passive progress, private wakeups, passive metadata, order sizing spec, entry open-notional headroom, transfer status, supported symbols, market-data activity control, live startup activation, local-L2 reconcile targets, worker status, cached private health, prewarm, and shutdown.
- **required state fields:** `adapter.health`, `adapter.passive_progress`, `adapter.capabilities`
- **required journal/event kinds:** `adapter_status_changed`
- **focused test path:** `tests/venues/test_v1_capability_matrix.py::test_adapter_contract_completeness`
- **deviation id:** —

---

## 6. market-data

### MD-001: Market Snapshot Diagnostics

- **area:** market-data
- **V1 source anchors:**
  - `src/execution_core/engine/market_data.rs` — snapshot diagnostics
- **V2 source anchors:**
  - `lightfee/marketdata/l2.py` — L2 snapshot
  - `lightfee/marketdata/local_l2_data_plane.py` — data plane
- **observable behavior:** Market snapshot diagnostics preserve missing, stale, partial, degraded, and returned-symbol semantics. A missing symbol in a snapshot is a distinct condition from a stale snapshot. Partial snapshots are usable for symbols present but block trading for missing symbols.
- **required state fields:** `snapshot.symbols_present`, `snapshot.generated_at`, `snapshot.degraded_symbols`
- **required journal/event kinds:** `market_snapshot_received`, `market_snapshot_degraded`
- **focused test path:** `tests/marketdata/test_local_l2_runtime_targets.py::test_snapshot_diagnostics`
- **deviation id:** —

---

## 7. local-l2

### L2-001: Local L2 Bootstrap and Resilience

- **area:** local-l2
- **V1 source anchors:**
  - `src/market_gateway/book.rs` — local order book
  - `src/execution_core/engine/market_data.rs` — L2 management
- **V2 source anchors:**
  - `lightfee/marketdata/local_l2_ws.py` — WebSocket L2 feed
  - `lightfee/marketdata/local_l2_runtime.py` — runtime integration
  - `lightfee/marketdata/local_l2_data_plane.py` — data plane
- **observable behavior:** Local L2 preserves bootstrap (initial snapshot load), sequence gap detection, checksum failure handling, stale book detection, retained snapshot for recovery, resume metadata, runtime targets (which books are active), and active-book budgets (max concurrent books).
- **required state fields:** `local_l2_books: dict[Symbol, LocalBook]`, `active_book_budget`, `sequence_gaps: list`
- **required journal/event kinds:** `local_l2_bootstrap`, `local_l2_sequence_gap`, `local_l2_checksum_failure`, `local_l2_stale_book`
- **focused test path:** `tests/marketdata/test_local_l2_runtime_targets.py::test_bootstrap_and_resilience`
- **deviation id:** CL-163 diagnostic extension: preserve `missing_book`, true `stale`, `sequence_gap`, and true `clock_skew` through the final entry gate; time is read after awaits so scheduler delay is not a false clock skew. Aster keeps documented `pu/u` rebuild semantics and adds only ACK/snapshot/buffer/rebuild/backoff evidence.

---

## 8. entry

### ENTRY-001: Entry Planning Semantics

- **area:** entry
- **V1 source anchors:**
  - `src/execution_core/entry_execution_planner.rs` — entry planning
  - `src/execution_core/engine/entry.rs` — entry execution
- **V2 source anchors:**
  - `lightfee/engine/execution_planner.py` — execution planning
  - `lightfee/engine/entry.py` — entry state machine
- **observable behavior:** Entry planning preserves incremental entry (funding legs build up over ticks), min-notional gating, remainder tracking, matched ratio computation, and reason strings for every decision. Entries must not execute below min-notional.
- **required state fields:** `EntryPlan.legs`, `.min_notional`, `.remainder`, `.matched_ratio`, `.reason`
- **required journal/event kinds:** `entry_planned`, `entry_leg_allocated`
- **focused test path:** `tests/engine/test_entry_semantic_parity.py::test_entry_planning_semantics`
- **deviation id:** —

### ENTRY-002: Entry Execution and Idempotency

- **area:** entry
- **V1 source anchors:**
  - `src/execution_core/engine/entry.rs` — entry state machine, client-order idempotency
  - `src/execution_core/entry_sync.rs` — entry sync
- **V2 source anchors:**
  - `lightfee/engine/entry.py` — entry execution
  - `lightfee/engine/entry_sync.py` — sync
- **observable behavior:** Entry execution preserves maker/hedge ordering, client-order idempotency (same client_order_id does not produce duplicate orders), uncertain outcome classification, reject classification (rate-limit vs balance vs other), residual task creation for partial fills, pending entry registration in state, and complete journal payloads for every outcome.
- **required state fields:** `PendingEntry.client_order_id`, `.legs`, `.state`, `.outcome`
- **required journal/event kinds:** `entry_maker_submitted`, `entry_maker_filled`, `entry_hedge_submitted`, `entry_hedge_filled`, `entry_failed`, `entry_uncertain`, `entry_residual_created`
- **focused test path:** `tests/engine/test_entry_semantic_parity.py::test_entry_execution_idempotency`
- **deviation id:** —

### ENTRY-003: Pending Entry Passive Opening Lifecycle

- **area:** entry
- **V1 source anchors:**
  - `src/execution_core/entry_sync.rs` — pending-entry passive opening lifecycle, terminal fallback, remainder repost
  - `crates/lightfee-engine/src/lib.rs` — pending-entry state fields
- **V2 source anchors:**
  - `lightfee/engine/pending_entry_lifecycle.py` — source-named lifecycle state transitions
  - `lightfee/engine/runtime.py` — Python adapter IO boundary
  - `lightfee/engine/state.py` — pending-entry passive lifecycle state
- **observable behavior:** Pending-entry passive opening preserves zero-fill cycle recording, high-to-low maker phase switch, low-phase dual-taker fallback, frozen-candidate terminal recheck, candidate-derived ForceStandard materialization/defer behavior, post-only submit retry, maker fill checkpoint progress, maker remainder FIFO tracking, and terminal partial remainder repost before finalization.
- **required state fields:** `PendingEntry.phase_state`, `.passive_order`, `.maker_remainder_slices`, `.frozen_candidate`, `.repost_attempt_count`, `.passive_attempt_count`
- **required journal/event kinds:** `passive_maintenance.zero_fill_cycle`, `execution.passive_phase_switched`, `execution.dual_taker_armed`, `execution.entry_fallback_to_taker`, `execution.entry_fallback_to_taker_deferred`, `execution.passive_entry_reposted`
- **focused test path:** `tests/engine/test_v1_pending_entry_lifecycle_parity.py`
- **deviation id:** DEV-003

---

## 9. close

### CLOSE-001: Close Execution Semantics

- **area:** close
- **V1 source anchors:**
  - `src/execution_core/engine/exit.rs` — close execution
- **V2 source anchors:**
  - `lightfee/engine/close_executor.py` — close executor
- **observable behavior:** Close execution preserves reduce-only order flag, chunking (large positions split into multiple orders), min-notional dust handling (remainders below min-notional are either aggregated or logged as dust), residual split for partial fills, partial close state tracking, final close (all legs zero), and PnL attribution (fee-adjusted, per-leg).
- **required state fields:** `CloseExecution.legs`, `.chunks`, `.dust`, `.pnl_attribution`
- **required journal/event kinds:** `close_submitted`, `close_filled`, `close_partial`, `close_dust`, `close_final`, `close_pnl_attributed`
- **focused test path:** `tests/engine/test_close_semantic_parity.py::test_close_execution_semantics`
- **deviation id:** CL-163 normalizes restored and new terminal accounting-only reconciliation records in the shared business contract. Clean pair-scoped position-flat plus no-open-order truth releases trading risk while retaining accounting backfill; unclean truth blocks. Terminal accounting retries avoid broad recovery collection and retain archival evidence.

---

## 10. passive-close

### PCLOSE-001: Passive Close Phase Semantics

- **area:** passive-close
- **V1 source anchors:**
  - `src/execution_core/engine/exit.rs` — passive close phases
- **V2 source anchors:**
  - `lightfee/engine/passive_close.py` — passive close executor
- **observable behavior:** Passive close preserves: high-slippage phase (aggressive initial close), low-slippage phase (tight spread, patient), zero-fill counter (consecutive ticks with no fills trigger escalation), delta hedge (offsetting position while closing), cumulative hedge catch-up (tracking hedge deficit over time), fallback to dual taker (when passive fails to fill), and terminal cleanup (cancel all orders, final state update).
- **required state fields:** `PassiveCloseState.phase`, `.zero_fill_count`, `.hedge_deficit`, `.fallback_triggered`
- **required journal/event kinds:** `passive_close_phase_change`, `passive_close_zero_fill`, `passive_close_fallback`, `passive_close_terminal`
- **focused test path:** `tests/engine/test_passive_close_semantic_parity.py::test_passive_close_phases`
- **deviation id:** —

---

## 11. risk

### RISK-001: Risk Evaluation and Venue Health

- **area:** risk
- **V1 source anchors:**
  - `src/execution_core/engine/risk.rs` — risk evaluation
  - `src/risk.rs` — top-level risk checks
  - `src/health.rs` — health aggregation
- **V2 source anchors:**
  - `lightfee/engine/risk_actions.py` — risk action planner
  - `lightfee/risk/` — risk models
- **observable behavior:** Risk evaluation preserves: unsupported snapshot policy (behavior when market data is missing), warning/delever/death lines (PnL thresholds), cooldowns (minimum time between risk actions), max steps (maximum delever steps per tick), single-side protection (don't delever both sides simultaneously without explicit approval), fail-closed (when risk is uncomputable, block trading), and venue-health aggregation (per-venue health contributes to global risk mode).
- **required state fields:** `RiskState.warning_line`, `.delever_line`, `.death_line`, `.cooldown_until`, `.max_steps`, `.fail_closed`
- **required journal/event kinds:** `risk_evaluated`, `risk_action_taken`, `risk_fail_closed`, `venue_health_changed`
- **focused test path:** `tests/engine/test_risk_semantic_parity.py::test_risk_evaluation_semantics`
- **deviation id:** —

---

## 12. state

### STATE-001: OpenPosition Field Completeness

- **area:** state
- **V1 source anchors:**
  - `src/execution_core/engine/state.rs` — `OpenPosition`
  - `crates/lightfee-engine/src/lib.rs` — engine types
- **V2 source anchors:**
  - `lightfee/engine/state.py` — `OpenPosition`
- **observable behavior:** `OpenPosition` preserves V1 semantic fields: review id, origin tags, opportunity source, funding legs, edge breakdowns, transfer state, liquidity source, VWAP, capacity constraints, advisories, blocked reasons, quality markouts, risk/protection PnL, and settlement fields.
- **required state fields:** `OpenPosition.review_id`, `.origin_tags`, `.opportunity_source`, `.funding_legs`, `.edge_breakdowns`, `.transfer_state`, `.liquidity_source`, `.vwap`, `.capacity_constraints`, `.advisories`, `.blocked_reasons`, `.quality_markouts`, `.risk_pnl`, `.protection_pnl`, `.settlement_fields`
- **required journal/event kinds:** `position_opened`, `position_updated`
- **focused test path:** `tests/persistence/test_v1_state_snapshot_semantics.py::test_open_position_field_completeness`
- **deviation id:** —

### STATE-002: PendingEntry Field Completeness

- **area:** state
- **V1 source anchors:**
  - `src/execution_core/engine/state.rs` — pending entry state
- **V2 source anchors:**
  - `lightfee/engine/state.py` — `PendingEntry`
- **observable behavior:** `PendingEntry` preserves metadata, client ids, leg fills, deadlines, retry state, route, outcome, and recovery dedup fields.
- **required state fields:** `PendingEntry.metadata`, `.client_order_ids`, `.leg_fills`, `.deadline`, `.retry_count`, `.route`, `.outcome`, `.recovery_dedup_key`
- **required journal/event kinds:** `pending_entry_created`, `pending_entry_resolved`
- **focused test path:** `tests/persistence/test_v1_state_snapshot_semantics.py::test_pending_entry_field_completeness`
- **deviation id:** —

### STATE-003: EngineState Lifecycle and Control Fields

- **area:** state
- **V1 source anchors:**
  - `src/execution_core/engine/state.rs` — engine-level state
  - `crates/lightfee-engine/src/lib.rs` — lifecycle enums
- **V2 source anchors:**
  - `lightfee/engine/state.py` — `EngineState`
- **observable behavior:** `EngineState` preserves lifecycle, risk mode, operator control, venue health, pending close reconciliation, recovery blocked state, residual repairs, live recovery reduce-only pairs, venue cooldowns, market-data degradations, transfer truth, retained local-L2 books, and entry liquidity qualification records.
- **required state fields:** `EngineState.lifecycle`, `.risk_mode`, `.operator_control`, `.venue_health`, `.pending_close_reconciliation`, `.recovery_blocked`, `.residual_repairs`, `.reduce_only_pairs`, `.venue_cooldowns`, `.market_data_degradations`, `.transfer_truth`, `.retained_l2_books`, `.liquidity_qualifications`
- **required journal/event kinds:** `lifecycle_transition`, `risk_mode_changed`
- **focused test path:** `tests/persistence/test_v1_state_snapshot_semantics.py::test_engine_state_lifecycle_fields`
- **deviation id:** —

---

## 13. journal

### JRNL-001: Journal Envelope and Durability

- **area:** journal
- **V1 source anchors:**
  - `src/observability_ops/journal_bridge.rs` — journal bridge
  - `src/runtime_state/sqlite_store.rs` — SQLite journal storage
- **V2 source anchors:**
  - `lightfee/persistence/journal.py` — journal
  - `lightfee/persistence/journal_index.py` — index
  - `lightfee/persistence/sqlite_store.py` — SQLite storage
- **observable behavior:** Journal envelope preserves seq (monotonic), run_id (unique per process start), ts (microsecond or better), kind (string event type), payload (JSON), critical durability (fsync for critical events), malformed-line tolerance (skip and log, don't crash), streaming (appends are visible to readers without restart), and indexed seek behavior (seek by seq, kind, or ts range).
- **required state fields:** `JournalRecord.seq`, `.run_id`, `.ts`, `.kind`, `.payload`
- **required journal/event kinds:** all event kinds listed in this catalog
- **focused test path:** `tests/persistence/test_journal_event_semantics.py::test_journal_envelope_and_durability`
- **deviation id:** CL-163 adds allowlisted final BBO/OI timing and result fields (queue, HTTP, validation, total, generation, age, outcome, rejection) and a diagnose incomplete-event-coverage conclusion. URLs, query/body data, signatures, credentials, and full snapshots are excluded from this evidence shape.

---

## 14. recovery

### REC-001: Restart Recovery Block/Resume Semantics

- **area:** recovery
- **V1 source anchors:**
  - `src/engine/recovery.rs` — recovery logic
- **V2 source anchors:**
  - `lightfee/engine/recovery.py` — recovery engine
  - `lightfee/engine/reconciliation.py` — reconciliation
- **observable behavior:** Recovery blocks live trading when V1 would block (unresolved positions, uncertain entries, pending closes) and resumes only when V1 would resume (all positions reconciled, no uncertain states). Recovery state is journaled. Reduce-only mode is enforced during recovery.
- **required state fields:** `RecoveryState.blocked`, `.blocked_reasons`, `.reconciled_positions`, `.uncertain_entries`
- **required journal/event kinds:** `recovery_started`, `recovery_position_reconciled`, `recovery_blocked`, `recovery_resumed`
- **focused test path:** `tests/recovery/test_restart_recovery_semantics.py::test_recovery_block_resume`
- **deviation id:** —

---

## 15. replay

### REPLAY-001: Replay Semantic Equivalence

- **area:** replay
- **V1 source anchors:**
  - `src/observability_ops/replay_bridge.rs` — replay bridge
  - `src/offline_replay/` — replay engine
- **V2 source anchors:**
  - `lightfee/offline/replay/engine.py` — replay engine
  - `lightfee/persistence/projection_writer.py` — projection writer
- **observable behavior:** Replay reconstructs open positions, pending entries, pending closes, lifecycle, risk mode, scan stats, recovery events, risk events, local-L2 events, and timeline. Replaying the same journal twice is idempotent. Replay must produce the same semantic summary as the original run.
- **required state fields:** reconstructed `EngineState`, `OpenPosition[]`, `PendingEntry[]`
- **required journal/event kinds:** `replay_started`, `replay_event_applied`, `replay_complete`
- **focused test path:** `tests/offline/replay/test_replay_semantic_equivalence.py::test_replay_idempotent`
- **deviation id:** —

---

## 16. offline-analysis

### ANAL-001: Journal Analysis and Reporting

- **area:** offline-analysis
- **V1 source anchors:**
  - `src/analysis.rs` — analysis entry point
  - `src/bin/journal_report.rs` — journal report CLI
  - `src/bin/incident_report.rs` — incident report CLI
- **V2 source anchors:**
  - `lightfee/offline/analysis/journal.py` — journal analysis
  - `lightfee/offline/analysis/incident.py` — incident analysis
  - `lightfee/offline/reports/daily.py` — daily reports
  - `lightfee/offline/reports/render.py` — report rendering
- **observable behavior:** Journal analysis counts: order lifecycle events, entry/exit PnL, recovery events, risk events, scan diagnostics, execution liquidity blocks, local-L2 sequence gaps, local-L2 sync failures, fail-closed reasons, and classification breakdowns. Daily and incident reports preserve V1 operator-facing sections and numeric meanings.
- **required state fields:** n/a (analysis output, not engine state)
- **required journal/event kinds:** all journal kinds consumed by analysis
- **focused test path:** `tests/offline/test_journal_analysis_semantics.py::test_analysis_counts_match_v1`
- **deviation id:** —

---

## 17. evolution

### EVOL-001: Evolution Governance and Ledger

- **area:** evolution
- **V1 source anchors:**
  - `src/evolution/` — evolution cycle, catalog, ledger, approval
  - `src/llm_evolution/` — LLM-driven evolution
- **V2 source anchors:**
  - `lightfee/offline/evolution/cycle.py` — evolution cycle
  - `lightfee/offline/evolution/ledger.py` — evolution ledger
  - `lightfee/offline/evolution/approval.py` — approval queue
  - `lightfee/offline/evolution/report.py` — rendered report
  - `lightfee/offline/llm_evolution/report.py` — LLM evolution report
  - `lightfee/offline/llm_evolution/evidence_pack.py` — (to be created)
  - `lightfee/offline/llm_evolution/proposal.py` — (to be created)
- **observable behavior:** Evolution ledger preserves proposal catalog, approval queue, experiment ledger, diagnostics, rendered report, and deterministic cycle results. LLM evolution preserves evidence pack, prompt contract, proposal schema, review, validation, root-cause summary, and disabled/enabled behavior. Disabled mode produces auditable no-op output.
- **required state fields:** `EvolutionLedger.proposals`, `.approval_queue`, `.experiments`, `.diagnostics`
- **required journal/event kinds:** `evolution_proposal_created`, `evolution_proposal_approved`, `evolution_experiment_started`, `evolution_experiment_completed`
- **focused test path:** `tests/offline/test_evolution_governance_semantics.py::test_evolution_governance`
- **deviation id:** —

---

## 18. ops

### OPS-001: Current State and Metrics Export

- **area:** ops
- **V1 source anchors:**
  - `src/app_runtime/loop_control.rs` — metrics export, state snapshots
  - `src/observability_ops/` — observability bridge
- **V2 source anchors:**
  - `lightfee/ops/` — operations commands
  - `lightfee/ops/metrics.py` — (to be created)
  - `lightfee/engine/loop_control.py` — loop control
- **observable behavior:** Post-tick housekeeping exports snapshot, current state, metrics, and journal diagnostics with V1-visible meanings. Shutdown persists final state and exports final current-state snapshot. Operator commands preserve V1 risk-mode and pending-reconcile semantics.
- **required state fields:** `current_state_snapshot`, `metrics_export`
- **required journal/event kinds:** `snapshot_exported`, `metrics_exported`, `shutdown_complete`
- **focused test path:** `tests/ops/test_current_state_and_metrics_export.py::test_export_matches_v1_semantics`
- **deviation id:** —

---

## Deviation Index

| Deviation ID | Area | Summary |
|-------------|------|---------|
| DEV-001 | opportunity-input | Chillybot-era inputs intentionally removed |
| DEV-002 | venue-capabilities | Bitget/Gate risk_health capability drift |
| DEV-003 | pending-entry-lifecycle | Python runtime IO boundaries for pending-entry passive opening |
