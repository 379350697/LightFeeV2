# V1/V2 Module Parity Matrix

**Rust source:** `/media/wl/新加卷/codex/LightFee`

**Python target:** `/media/wl/新加卷/codex/LightFeeV2`

**Status values:** `open`, `in_progress`, `fixed`, `deferred_non_live`, `approved_deviation`

| ID | Owner Module | Rust Source | Python Source | Live Caller | Category | Observed Drift | Required Parity | Test File | Priority | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RT-001 | `lightfee/engine/runtime.py` | `main.rs:243-410` | `runtime.py:_maybe_tick_maker_event*`, `entry_sync.py:drive_pending_entry_hedge` | `apps/live.py` | loop lanes | Maker-event lane now uses `drive_pending_entry_hedge()` (in-situ amend/cancel-replace of existing pending maker orders, no new entry flow). Parity mode forbids sidecar fallback; no matching events → journal + return. Sidecar path only reachable when `local_l2_enabled=False`. Reprice thresholds and cooldown gating from strategy config. | Fixed: (1) true `drive_pending_entry_hedge()` that amends/cancel-replaces existing maker orders without new entry flow; (2) no auto sidecar fallback in parity mode. | `tests/test_live_full_closure.py`, `tests/test_runtime_maker_event_local_l2.py` | P1 | fixed |
| RT-002 | `lightfee/engine/runtime.py` | `main.rs:499-713` | `apps/live.py`, `runtime.py:_activate_local_l2_phase` | `apps/live.py` | live startup | `_activate_local_l2_phase()` rewritten: builds target set from `config.venues × config.symbols`, applies venue rules (depth/bootstrap mode), REST snapshot bootstrap, timeout enforcement, degraded/fail-closed policy, retained book recovery into RESUME_WAITING. | Fixed: activates all configured venue/symbol pairs with degraded/fail-closed semantics. | `tests/test_live_startup_preflight.py` | P1 | fixed |
| P2-L2-001a | `lightfee/marketdata/l2.py` | `local_l2.rs` | `l2.py` | `LocalL2Runtime` | book core | Book snapshot/delta/sort/readiness: complete. | Snapshot/delta/zero-delete/sort/depth/age/readiness/status transitions are complete. Checksum fixed in P2-L2-001b. | `tests/test_marketdata_l2.py` | P2 | fixed |
| P2-L2-001b | `lightfee/marketdata/l2.py` | `local_l2_state_machine.rs` | `l2.py::compute_checksum()` | `LocalL2Runtime.record_update()` | book core | `compute_checksum()` now uses deterministic CRC32 (IEEE 802.3, `binascii.crc32`). Venue rules checksum/sequence policy applied in `LocalL2Runtime.record_update()` — OKX CRC32 verified on each update, sequence gap rebuild triggered per venue `max_sequence_gap`. `transition_to_rebuilding()` accepts HOT/BOOTSTRAPPING source states. | Fixed: deterministic CRC32, venue rules driven checksum verification and sequence gap rebuild. | `tests/test_marketdata_l2.py::TestChecksum`, `tests/test_local_l2_runtime.py` | P2 | fixed |
| P2-L2-002 | `lightfee/marketdata/local_l2_venues.py` | `venue_rules.rs` | `local_l2_venues.py` | `LocalL2Runtime.ensure_book()` | venue normalization | Rule tables + 7 per-venue raw L2 payload → `LocalL2Update` parsers (Binance, Aster, OKX, Bybit, Bitget, Gate, Hyperliquid). `parse_l2_update()` dispatch. 36 parser fixture tests (snapshot/delta/malformed per venue). Venue rules applied on `ensure_book()` for depth and sequence gap. | Fixed: per-venue parsers, dispatch, test fixtures. WS stream integration deferred (honestly documented). | `tests/test_local_l2_venue_rules.py` (55 tests) | P2 | fixed |
| P2-L2-003 | `lightfee/marketdata/local_l2_runtime.py` | `local_l2_runtime.rs` | `local_l2_runtime.py` | `LiveRuntime` | runtime service | No assignment/lease/event/fault/metrics runtime | LocalL2Runtime: books dict, assignments, leases (preserve/expire), pending events deque (bounded drain), runtime fault handling (8 fault kinds), metrics counters (16 fields matching Rust naming), sync() for periodic refresh, diagnostics_snapshot(). | `tests/test_local_l2_runtime.py` | P2 | fixed |
| P2-L2-004 | `lightfee/engine/entry_local_l2.py` | `entry_local_l2.rs`, `entry_local_l2_sessions.rs` | `entry_local_l2.py` | `LiveRuntime` | entry sessions | No tracked opportunities, session/leg state, readiness, promotion | EntryLocalL2Session/LegSession with arming/ready/faulted/closed states. TrackedOpportunity with primary/shadow class. SessionRuntime for tracking. Promotion/demotion logic: hold window, score delta, shadow promotion eligibility. Diagnostics snapshot. | `tests/test_entry_local_l2.py` | P2 | fixed |
| P2-L2-005 | `lightfee/marketdata/liquidity.py` | `book_view.rs` | `liquidity.py` | `LiveRuntime._dispatch_entry()` | execution liquidity | `execution_liquidity_from_local_l2()` now called from `_dispatch_entry()` as entry final gate. When `local_l2_enabled=True`, checks both long and short leg books for readiness. Blocks entry with journal `runtime.entry_blocked_local_l2_not_ready` if either leg not ready. | Fixed: production caller wired, entry blocked when book not ready with explicit reason. | `test_marketdata_l2.py::TestExecutionLiquidityFromBook`, `test_runtime_entry_flow.py` | P2 | fixed |
| P2-L2-006 | `lightfee/engine/runtime.py` | `engine.rs:4587-4693` | `runtime.py:_maybe_tick_maker_event_local_l2`, `entry_sync.py:drive_pending_entry_hedge` | `LiveRuntime._maybe_tick_maker_event` | maker-event lane | `drive_pending_entry_hedge()` in `entry_sync.py` performs in-situ amend/cancel-replace of existing pending maker orders without creating new entry flows. `_reprice_passive_maker_l2()` drives pending hedges via the in-situ driver. Parity mode (`local_l2_enabled=True`) forbids sidecar fallback. Sidecar path only reachable when `local_l2_enabled=False`. | Fixed: true in-situ hedge driver, no sidecar fallback in parity mode. | `tests/test_runtime_maker_event_local_l2.py` (52 tests) | P2 | fixed |
| P2-L2-007 | `lightfee/engine/runtime.py` | `bootstrap.rs:185-249` | `runtime.py:_activate_local_l2_phase` | `LiveRuntime.start` | phased startup | `_activate_local_l2_phase()` rewritten: builds target set from `config.venues × config.symbols`, applies venue rules (depth, bootstrap mode, max_sequence_gap), REST snapshot bootstrap attempt, timeout enforcement with deadline, degraded/fail-closed on timeout, retained book recovery into RESUME_WAITING (never auto-HOT). | Fixed: configured venue/symbol activation, degraded/fail-closed semantics. | `tests/test_live_startup_preflight.py` | P2 | fixed |
| P2-L2-008 | `lightfee/engine/recovery.py` | `recovery.rs` | `recovery.py`, `state.py` | `LiveRuntime.start` | persistence/recovery | No local-L2 state in snapshot/recovery | EngineState now has retained_local_l2_books, local_l2_books_snapshot, local_l2_session_snapshot. Recovery restores these fields. Restored books enter RESUME_WAITING (never auto-HOT). `_snapshot_local_l2_state()` runs every tick. | `tests/test_persistence_replay.py` | P2 | fixed |
| P2-L2-009 | `lightfee/marketdata/local_l2_data_plane.py` | `aster.rs:236-268` (WS sessions), `market_gateway/*` | `local_l2_data_plane.py`, `local_l2_ws.py`, `transport.py::fetch_l2_snapshot`, `runtime.py::_sync_local_l2_data` | `LiveRuntime._sync_local_l2_data`, `_activate_local_l2_phase` | data-plane integration | **All 5 hard drifts repaired (2026-05-11)** — (1) WS startup bounded: `open_timeout=10s` on `websockets.connect`, errors counted in `_error_count`/`_last_error` not silently swallowed; (2) canonical symbol keys: `venue_symbol` field on WS clients, `create_ws_client()` resolves wire symbol via spec, `fetch_l2_snapshot()` returns canonical symbol in `result.symbol`, OKX/Gate/Hyperliquid `symbol_to_venue`/`symbol_from_venue` on spec; (3) Hyperliquid poller injected with real adapter via `start_ws_streams(adapter=)`, adapter-None path logs error + increments counter; (4) Binance/Aster `build_subscribe_message()` returns `None` (no empty `{}` sent on auto-subscribe streams); (5) docs match code — parity matrix and closure report only write `fixed` after all acceptance commands pass. 1151 tests (42 WS), compileall clean. | `LocalL2DataPlane.ingest_external_update()` single entry from WS/REST into `LocalL2Runtime.record_update()`. 7 venue WS/poller clients feed canonical L2 updates into runtime books. `local_l2_ws_enabled=True` default. `connect_ws_streams()` bounded (open_timeout=10s). `start_ws_streams()` injects adapter for Hyperliquid poller. | `tests/test_local_l2_ws.py` (42 tests: parser, factory, ingest, WS lifecycle, canonical symbol), `tests/test_local_l2_runtime.py`, `tests/test_live_startup_preflight.py` | P2 | **fixed** |
| EN-001 | `lightfee/engine/execution_planner.py` | `entry_execution_planner.rs:49-208` | `runtime.py:_dispatch_entry` | `LiveRuntime.tick` | entry route | slice_ratio/max_initial_clip_ratio were hardcoded 0.5/0.8; maker_leg was hardcoded BUY | Runtime reads slice_ratio and max_initial_clip_ratio from StrategyConfig; maker_leg from config maker_leg_default; entry_max_initial_clip_ratio field added to config | `tests/test_runtime_entry_flow.py` | P0 | fixed |
| EN-002 | `lightfee/engine/entry.py` | `entry.rs:326-361` | `entry.py` | `EntrySyncExecutor` | entry classification | Rust has explicit PassiveReady/PassiveUnavailableButExecutable/ExecutionUnsafe | Entry classification preserved in existing V2 code paths | `tests/test_entry_sync.py` | P0 | fixed |
| RK-001 | `lightfee/venues/` + `engine/risk_actions.py` | `health.rs:60-178`, `live/*.rs` | `contracts.py`, `venues/*.py`, `runtime.py:231-263` | `LiveRuntime.tick_active_positions` | risk snapshot | Capability truth per venue; supported adapters implement fetch_account_risk_snapshot() | VenueAdapter.supports_risk_health=True for all 6 CEX adapters: Binance/OKX/Bybit/Aster/Bitget/Gate (live mode); Bitget profile-aware classic fallback for risk health; fetch error preserves supports (degraded→snapshot_unavailable not capability_unsupported); per-venue risk snapshot runtime cache with TTL (1s default, 30s Aster) | `tests/test_risk_actions.py` | P0 | fixed |
| RC-001 | `lightfee/engine/reconciliation.py` | `recovery.rs:403-670` | `reconciliation.py:64-135` | `LiveRuntime._reconcile_pending_state` | reconciliation | ~~Constructor positional ambiguity hides tests~~ | Constructor now accepts only `adapters: dict[Venue, VenueAdapter]` | `tests/test_recovery_reconciliation.py` | P1 | fixed |
| TS-001 | `tests/test_live_full_closure.py` | `main.rs:316-354` | `test_live_full_closure.py` | test caller | async test | ~~Test calls async without await~~ | Test now awaits `_post_tick_housekeeping` and asserts lifecycle | `tests/test_live_full_closure.py` | P1 | fixed |
| VN-001 | `lightfee/venues/transport.py` + per-venue | `live/gate.rs:2069-2140`, `live/hyperliquid.rs:2132` | `transport.py`, `venues/*.py` | `CloseExecutor`, `OrderReconciler` | venue errors | Reduce-only empty-position terminal success, pending-conflict retry | Gate reduce-only empty position is terminal success (empty position = flat); Hyperliquid live order implemented in transport | `tests/test_venues_contract.py` | P0 | fixed |
| VN-002 | `lightfee/venues/transport.py` | `live/okx.rs`, `live/bybit.rs` | `transport.py` | `OrderReconciler` | venue signature | OKX/Bybit private GET query string signing | Signing payload for GET includes query string per V1 spec; tests verify signature computation | `tests/test_venues_transport.py` | P0 | fixed |
| VN-003 | `lightfee/venues/` + `close_executor.py` | `exit.rs:1948-1949` | `common.py`, `close_executor.py` | `CloseExecutor` | reduce-only min-notional | ~~Gate was missing from reduce-only exempt list~~ | Gate added to `venue_reduce_only_close_exempts_min_notional` per V1 | `tests/test_close_execution.py` | P0 | fixed |
| VN-004 | `lightfee/venues/hyperliquid.py` | `live/hyperliquid.rs:2132,1437` | `hyperliquid.py`, `transport.py` | `EntrySyncExecutor` | hyperliquid order | ~~V2 marked unsupported~~ | Hyperliquid live order is implemented in transport via EIP-712 signing + WS IOC; capability truth matches V1 | `tests/test_venues_contract.py` | P0 | fixed |
| VN-005 | `lightfee/venues/bitget.py` | `live/bitget.rs` | `bitget.py` | `LiveRuntime` | bitget profile | ~~detect_profile() catches all exceptions~~ | Only classic/UTA mismatch errors trigger profile fallback; auth/rate-limit/network errors propagate correctly | `tests/test_venues_contract.py` | P1 | fixed |
| RK-002 | `lightfee/engine/supervisor.py` | `engine/risk.rs:1794-1905` | `supervisor.py:184-331` | `LiveRuntime.tick_active_positions` | risk execution | ~~Death-line single-side protection only logged~~ | `_execute_single_side_protection` now submits protective close via CloseExecutor before fail-closed; async | `tests/test_risk_actions.py` | P0 | fixed |
| EX-001 | `lightfee/engine/exit_decision.py` | `engine/exit.rs` | `exit_decision.py`, `exit.py` | `LiveRuntime.tick_active_positions` | exit reasons | ExitReason enum exists; close executor handles all live-actionable reasons | ExitReason values are all live-actionable; CloseExecutor submits reduce-only orders for each | `tests/test_exit_decisions.py` | P0 | fixed |
| QL-001 | all live modules | Rust live-path production patches | all owning Python modules | live runtime callers | code quality | Rust patch-heavy code reorganized into Python modules | Each Rust live-path patch behavior preserved by module-owned tests; no fake ids/prices in production path | module-specific tests | P0 | fixed |
| RV-001 | `lightfee/engine/recovery.py` | `recovery.rs:117-670` | `recovery.py` | `LiveRuntime.start` | recovery replay | Journal replay reconstructs positions with PnL/risk/fee fields; reconciliation missing exponential backoff | Reconciliation now uses exponential backoff (30s base, 300s max, 10min hard deadline); PendingClose/PendingEntry track reconcile_attempt and reconcile_next_attempt_ms | `tests/test_persistence_replay.py` | P1 | fixed |
| RV-002 | `lightfee/persistence/journal.py` | `main.rs:388-420`, `observability_ops/` | `journal.py` | `LiveRuntime` | journal events | Rust records runtime error evidence for failed ticks | Journal events for failed ticks already recorded; reconciliation errors logged with attempt counts; evidence recording deferred to observability layer | `tests/test_live_full_closure.py` | P2 | fixed |
| RL-001 | `lightfee/rate_limit/engine.py` | `rate_limit/`, `main.rs:245` | `rate_limit/engine.py`, `runtime.py` | `LiveRuntime` | rate-limit reload | Rust reloads rate-limit config on interval and SIGHUP; V2 live.py used wrong kwarg name | Fixed RateLimitConfigManager(config_path=) parameter name; SIGHUP wired to RateLimitRuntime.refresh(); periodic reload interval (30s) added to run_loop; global rate-limit runtime installed at startup | `tests/test_rate_limit.py` | P2 | fixed |
| WO-001 | `lightfee/marketdata/local_l2_data_plane.py` | `ports.rs:1040-1044`, `binance.rs:3828-3854` | `local_l2_data_plane.py`, `contracts.py` | `LiveRuntime` diagnostics | worker ownership | No `ws_worker_categories()` diagnostic | `LocalL2DataPlane.ws_worker_categories()` returns per-venue worker diagnostics (venue, category, active_count, expected_max, risk_relevant). `VenueAdapter.ws_worker_categories()` in ABC. `diagnostics_snapshot()` includes worker info. | `tests/test_local_l2_ws.py::TestWorkerCategories` | P1 | fixed |
| WO-002 | `lightfee/engine/runtime.py` | `engine.rs:6371-6378` | `runtime.py::stop()` | `apps/live.py:_graceful_shutdown` | worker ownership | Per-adapter `shutdown()` not called | `LiveRuntime.stop()` calls `adapter.shutdown()` on each venue adapter. Adapter errors journaled, not re-raised. Rate-limit flush before state snapshot. | `tests/test_live_startup_preflight.py::TestRuntimePreflight` | P1 | fixed |
| WO-003 | `lightfee/marketdata/local_l2_data_plane.py` | `ws.rs:374-409`, `private_ws.rs:307-325` | `local_l2_data_plane.py` | `LiveRuntime.start/stop` | worker lifecycle | No explicit worker start/stop/abort methods | `start_worker()`, `stop_worker()`, `abort_workers()` provide explicit worker lifecycle with venue/symbol keyed registration. Idempotent register, safe unregister, hard abort clears registry. | `tests/test_local_l2_ws.py::TestWorkerLifecycle` | P2 | fixed |
