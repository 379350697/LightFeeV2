# Bug Ledger Daily - 2026-08-21

## Cluster CL-116-background-accounting-debt-deploy-gate

### Status

**Closed for the deployment-gate defect.** Fix commit: `1d13de3`, deployed in
`cb31ff20`. This change does not clear, import, or alter a pending
close-accounting owner.

### Symptom and Production Evidence

The 2026-08-20 read-only production verifier reported one warning and exited
non-zero after the COTI position was physically flat with no open orders. The
only remaining owner was a final `evidence_debt` reconciliation missing the
Binance short close identity. The recovery decision was
`RUNNING_WITH_EVIDENCE_GAP/background_close_reconciliation` and allowed new
entries, but the deployment verifier treated every pending close owner as a
failed health check.

The COTI owner is new work from the same execution run, not a historical record
reappearing. Its Binance order ID, fill price, and fee remain unavailable; the
debt must remain durable until exact exchange evidence is supplied.

### Authoritative Contract

V1 recovery distinguishes live execution work from durable accounting work.
When exchange truth is high-confidence flat with no open orders, and the only
remaining close owner is a background reconciliation, restarting the process
is deploy-safe. The accounting owner must remain visible in state and recovery
diagnostics. Any live position, open order, pending close/passive close,
residual repair, missing/low-confidence exchange truth, or unresolved order
truth remains a failing health condition.

### Root Cause and Repair

`analyze_current_state` counted all three close-owner classes in one `clean`
predicate and emitted `pending_close_owner_present` for all of them. This
overrode the V1 recovery decision that had already classified a reconciliation
as non-blocking background work. The production verifier converts that warning
to a non-zero exit, so a durable billing debt indefinitely prevented deploys.

The health boundary now recognizes only the V1
`background_close_reconciliation` decision when there are no execution owners
or other local risk owners. It exposes
`background_close_reconciliation_pending` in report details and permits a
green deploy gate only after the existing high-confidence exchange-flat checks
also pass. It does not weaken exchange-truth, open-order, passive-close,
residual, or weak-order-truth checks.

### Counterexample Matrix

| Case | Required result |
|---|---|
| flat, high-confidence exchange truth, only accounting reconciliation | deploy health passes; debt remains visible |
| same reconciliation plus exchange open order | critical health failure |
| pending passive close or legacy pending close | health remains non-green |
| exchange truth missing, unavailable, or below high confidence | health remains non-green |
| unresolved order-truth event | health remains non-green |

### Verification

| Environment | Command / evidence | Result |
|---|---|---|
| local RED | `.venv/bin/python -m pytest -q tests/ops/test_production_health.py -k 'background_reconciliation or pending_close_owner'` before repair | 2 failed: background reconciliation was warning/non-green and hid the open-order check behind the non-clean predicate |
| local GREEN | same command after repair | 3 passed |
| local production path | `.venv/bin/python -m pytest -q tests/ops/test_production_health.py` | 40 passed; includes `verify_production_services.py` CLI regression |
| full regression suite | `.venv/bin/python -m pytest -q` | 4356 passed, 9 skipped; one known collection warning |
| production | `verify_deploy_manifest.py --check`, `check_process_singleton.py --strict`, and `verify_production_services.py --json` on `cb31ff20` | manifest and singleton passed; health `ok: true`, zero warnings/critical findings, one visible background reconciliation |

### Production Closure / Remaining Evidence

The 2026-08-21 post-deploy verifier passed on `cb31ff20`: `ok: true`, zero
warnings and critical findings, high-confidence flat exchange truth, no open
orders, and exactly one visible owner with
`background_close_reconciliation_pending: true`. The singleton check found one
live runtime and one sidecar. This closes the deployment-gate defect.

The COTI debt is not closed by this repair: Binance must provide exact close
execution evidence before an audited import and reconciliation can occur. That
is a separate accounting-evidence task, not a reason to block a safe deploy.
