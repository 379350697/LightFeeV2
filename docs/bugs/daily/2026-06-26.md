# 2026-06-26 Bug Ledger

## Cluster CL-121 - Aster Advisory Lifecycle Mapping and Admission Card Drift

Status: local RED/GREEN complete; rollout pending.

### GitNexus Keys

- Fingerprint: `aster_headroom_advisory_unmapped + admission_card_aster_scope_drift`
- Components: `v1-lifecycle-closure`, `diagnose-live`, `bug-ledger`
- Symbols: `map_lifecycle_event_kind`, `_EVENT_KIND_PHASES`, `_build_entry_admission_cooldown_summary`
- Files: `lightfee/engine/v1_lifecycle_closure.py`, `scripts/diagnose_live.py`, `tests/engine/test_v1_lifecycle_closure_table.py`, `tests/test_diagnose_live.py`
- Watch: `runtime.entry_admission_headroom_advisory`, `aster_headroom_advisory_zero`, `unmapped_event_kinds`, `aster:*`, `aster:SYMBOL`
- Card: [pending-entry-hedge-admission](../cards/pending-entry-hedge-admission.md)

### Production Symptom

After CL-120, current production trading truth was clean: account truth could be
funded, endpoint-zero was no longer treated as wallet capacity, real Aster
`-5018` rejects were symbol-scoped, and LAB sub-step rejects reported the
correct `1 LAB` minimum/step. The remaining issue was in since-deploy
diagnostics: `runtime.entry_admission_headroom_advisory` could still appear in
`production_acceptance_gate.v1_lifecycle_closure.unmapped_event_kinds`.

### Root Cause

Runtime and diagnose already used the new CL-120 business meaning:
`remainingOpenableNotionalValue=0` can be advisory when account truth, position
truth, and open-order truth prove submit is allowed. The V1 lifecycle closure
mapping table did not include the new runtime event, so the production gate saw
a historical advisory artifact as unmapped residue. The admission bug card also
still carried the older CL-115 rule that Aster max-notional evidence should arm
venue-scope cooldown, which drifted from the CL-120 scope contract.

### Fix

- Map `runtime.entry_admission_headroom_advisory` to `DIAGNOSTIC_ONLY` in the V1
  lifecycle closure table.
- Add lifecycle regression coverage so the event is absent from
  `unmapped_event_kinds`.
- Add a diagnose since-deploy regression for Aster advisory-zero account truth:
  `reason_counts.aster_headroom_advisory_zero == 1`, `scope_counts.none == 1`,
  and lifecycle `unmapped_event_kinds == []`.
- Update the admission card: Aster endpoint-zero is advisory with sufficient
  account truth; real `-5018` is `aster:SYMBOL`; `aster:*` is allowed only for
  account-level balance insufficiency, account-level permission/auth failure, or
  venue-wide private-truth unavailability.

### Verification

| Scope | Evidence | Result |
|---|---|---|
| local RED | `tests/engine/test_v1_lifecycle_closure_table.py::test_owned_pending_passive_close_projects_as_passive_close_blocker`, `tests/engine/test_v1_lifecycle_closure_table.py::test_recent_cloud_event_kinds_are_mapped_or_diagnostic_only`, `tests/test_diagnose_live.py::test_diagnose_since_deploy_maps_aster_advisory_zero_lifecycle_event` | old mapping reports `runtime.entry_admission_headroom_advisory` as unmapped |
| local GREEN | focused lifecycle and diagnose tests after mapping | advisory event maps to `DIAGNOSTIC_ONLY`; diagnose since-deploy fixture has no unmapped lifecycle residue |

### Acceptance

This cluster is diagnostic and ledger semantics only. It does not change entry
selection, sizing, leverage targets, order submit routing, or any close path.
It closes the wording/diagnosis drift left after CL-120 so final production
truth can stay aligned with the business contract: current exchange truth is
separate from historical diagnostic artifacts, and Aster account-level blocks
must be proven by account-level or venue-wide evidence.
