# 2026-06-01 Bug Ledger

## Cluster CL-026-ws-bbo-quote-lease-coverage-prewarm-lifecycle

### GitNexus Keys

- Fingerprint: `ws-bbo-quote-lease.missing-quote-after-provider-switch + ranked-subscription-budget-drift + prewarm-after-freshness-filter + subscription-error-evidence-gap`
- Components: `entry-readiness-provider`, `ws-bbo-data-plane`, `live-runtime`,
  `production-observability`, `exchange-docs`
- Symbols: `LiveRuntime._ensure_entry_bbo_active_for_candidates`,
  `LiveRuntime.tick`, `BboWsClient._handle_message`
- Files: `lightfee/engine/runtime.py`, `lightfee/marketdata/ws_bbo.py`,
  `tests/test_entry_local_l2.py`, `tests/test_ws_bbo.py`,
  `tests/test_runtime_entry_flow.py`, this ledger, and `docs/bugs/BUG_INDEX.md`
- Watch: `runtime.entry_blocked_local_l2_selection` with
  `readiness_evidence.provider=ws_bbo_quote_lease`,
  `entry_ws_bbo_quote_lease_missing_quote`,
  `runtime.entry_blocked_quote_lease.reason=stale_quote_lease`,
  `runtime.ws_bbo_dynamic_ws_started`, `long_stream_state.connected`,
  `short_stream_state.connected`, `message_count`, `last_error`

### Production Evidence

After switching production to `entry_readiness_provider=ws_bbo_quote_lease`,
the provider entered the real entry path:

| Event | Count |
|---|---:|
| `execution.entry_selected` | 8 |
| `runtime.entry_dispatched` | 8 |
| `runtime.position_opened` | 2 |
| `order.submit_attempt` | 16 |
| `order.submitted` | 8 |
| `order.filled` | 2 |
| `runtime.entry_blocked_local_l2_selection` with provider `ws_bbo_quote_lease` | 179 |
| `runtime.entry_blocked_quote_lease` | 9 |

The two actual opens were `ALLOUSDT` and `BASUSDT`. The high-frequency
`runtime.entry_blocked_local_l2_selection` event name is legacy; payload
evidence showed `provider=ws_bbo_quote_lease` and
`reason=entry_ws_bbo_quote_lease_missing_quote`, so this was the new provider
failing closed on missing BBO evidence, not old Local-L2 selecting books.

### Exchange Documentation Check

Official docs confirm the selected BBO/ticker channels are suitable for
top-of-book quote leases, but they also imply venue-specific lifecycle handling:

| Venue | Official contract | Implementation decision |
|---|---|---|
| Binance USD-M | Individual symbol `bookTicker` pushes best bid/ask updates in real time; current docs route public market streams through `/public/ws/...`. | Keep `bookTicker`, update URL to the routed public endpoint to match current docs. |
| Aster | Binance-compatible futures `bookTicker` stream is real-time and symbols are lowercase in stream names. | Keep existing Aster raw stream and parser. |
| OKX | Public `tickers` channel carries `bidPx`, `askPx`, sizes, and millisecond `ts`; OKX also requires ping/reconnect if no data is received for under 30s. | Keep `tickers` for BBO lease, keep fail-closed freshness budget, and surface subscribe/control errors. |
| Bybit | Linear public ticker uses `tickers.{symbol}`, has snapshot/delta semantics, and missing fields in deltas mean unchanged values. | Keep last-value merge for deltas; keep linear public URL. |
| Bitget | Futures ticker channel returns `bidPr`/`askPr` with roughly 300-400ms updates on change. | Keep ticker channel; keep TTL/freshness gate. |
| Gate | `futures.book_ticker` subscribes by contract such as `BTC_USDT`; empty `a` or `b` means empty side. | Keep venue symbol mapping and invalid empty-side rejection. |
| Hyperliquid | `bbo` subscription sends `WsBbo` only when BBO changes on a block. | Keep BBO channel and fail closed when quote age exceeds the lease budget. |

Docs checked:

- Binance: https://developers.binance.com/docs/derivatives/usds-margined-futures/websocket-market-streams/Individual-Symbol-Book-Ticker-Streams
- Binance connect/routing: https://developers.binance.com/docs/derivatives/usds-margined-futures/websocket-market-streams/Connect
- OKX: https://tr.okx.com/docs-v5/en/#order-book-trading-market-data-ws-tickers-channel
- Bybit ticker: https://bybit-exchange.github.io/docs/v5/websocket/public/ticker
- Bybit connect: https://bybit-exchange.github.io/docs/v5/ws/connect
- Bitget ticker: https://www.bitget.com/api-doc/contract/websocket/public/Tickers-Channel
- Gate futures book ticker: https://www.gate.com/docs/developers/futures/ws/en/#best-askbid-subscription
- Hyperliquid subscriptions: https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
- Aster: https://docs.asterdex.com/product/aster-perpetuals/api/api-documentation

### Root Cause

This is not a Local-L2 sequence-continuity failure. It is a new provider
lifecycle bug:

1. WS BBO streams were activated from the post-snapshot-freshness `tradeable`
   list. If a candidate was filtered before final selection, the provider did
   not prewarm BBO for it, so later entry windows could hit missing quote on
   first sight.
2. Per-venue stream budget used `set` plus alphabetical `sorted(symbols)`.
   That could spend a small venue budget on a lower-ranked symbol and leave the
   highest-ranked candidate without BBO coverage.
3. Subscription/control errors were swallowed as non-data messages, so a venue
   reject looked the same as "connected but no quote yet."

The safe behavior stayed fail-closed: missing or stale quote blocked entry.
The instability was opportunity coverage, not wrong-price entry.

### Fix

- `LiveRuntime.tick()` now prewarms WS BBO from the catalog-supported discovery
  list before final snapshot-quote freshness can remove candidates from the
  final tradeable list.
- `LiveRuntime._ensure_entry_bbo_active_for_candidates()` now preserves
  candidate ranking order when filling per-venue BBO budgets; it no longer
  alphabetically truncates symbols.
- Binance BBO URL now uses the current official routed public endpoint:
  `wss://fstream.binance.com/public/ws/<symbol>@bookTicker`.
- `BboWsClient._handle_message()` now records WS control/subscription errors
  in `last_error` and includes `subscribed` / `last_control_message` in stream
  state evidence.

No Local-L2, entry execution, order placement, close, residual repair, sizing,
or recovery semantics changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| local RED | `tests/test_entry_local_l2.py::TestEntryReadinessProviderFactory::test_ws_bbo_provider_budget_preserves_candidate_rank_order`; `tests/test_entry_local_l2.py::TestEntryReadinessProviderFactory::test_ws_bbo_provider_tick_prewarms_before_snapshot_quote_filter`; `tests/test_ws_bbo.py::test_bbo_ws_client_records_subscription_error_control_message` | failed before implementation for alphabetical budget drift, no prewarm before snapshot filter, and missing control-error evidence |
| local GREEN focused | same focused tests plus adjacent WS BBO activation/budget tests | `4 passed`; WS control test included in `tests/test_ws_bbo.py` |
| local adjacent | `.venv/bin/pytest tests/test_ws_bbo.py tests/test_ws_bbo_probe.py -q`; `.venv/bin/pytest tests/test_entry_local_l2.py::TestEntryReadinessProviderFactory -q`; `.venv/bin/pytest tests/test_runtime_entry_flow.py::TestPlannerDispatchIntegration -q`; focused config provider tests | `10 passed`; `13 passed`; `21 passed`; `3 passed` |
| public read-only WS probes | local with `NO_PROXY=*`: Binance, Bybit, Hyperliquid; production-cloud network: OKX, Bitget, Gate, Aster | all returned `classification=quote_received` with positive bid/ask and `stream_state.connected=true`; local OKX/Bitget/Gate/Aster direct attempts reset, so cloud network was used as the closer production-path evidence |
| GitNexus pre-edit impact | `_ensure_entry_bbo_active_for_candidates` upstream | HIGH because it is called from `tick -> run_loop -> live main`; direct caller is `LiveRuntime.tick` |
| GitNexus pre-edit impact | `VenueBboDataPlane.start_ws_streams`, `BboWsClient._handle_message`, `WsBboQuoteLeaseEntryReadinessProvider._validate_quotes` | LOW |
| local full gate | `.venv/bin/pytest -q`; `.venv/bin/python -m compileall -q lightfee tests`; `git diff --check` | `3337 passed, 9 skipped, 1 warning`; compileall passed; diff check passed |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded to `7745e86`; remote focused provider/dispatch tests; service restart | remote focused tests `43 passed`; `lightfee-sidecar.service` and `lightfee-live.service` active; deployed manifest written to `.deploy_version` |

### Post-Deploy Production Check

The code fix is deployed, but the live runtime is not a clean CL-026 effect
window yet. After restart, production entered `risk_only` / `fail_closed`
because of a pre-existing `ARIAUSDT` pending-entry recovery block whose
`recovery_blocked_at_ms` was before this deployment. Credentialed read-only
truth for the pending symbol reported nonzero live exposure on both legs and no
open orders:

| Venue | Symbol | Live side | Quantity | Open orders |
|---|---|---|---:|---:|
| Bybit | `ARIAUSDT` | buy | 1238 | 0 |
| Binance | `ARIAUSDT` | sell | 619 | 0 |

That state-consistency failure is not evidence that CL-026 regressed. It does
mean post-deploy entry-path counters cannot yet prove the new provider in live
selection because entry remains blocked by the older pending-entry/live-truth
condition. Track that separately under the pending-entry live-truth family
before using production order counts as CL-026 acceptance evidence.

### Acceptance

Accepted locally and deployed in `7745e86`. This cluster changes only the WS BBO
quote provider's activation order, per-venue budget ordering, current Binance
public endpoint alignment, and stream error evidence. The expected production
effect is fewer `entry_ws_bbo_quote_lease_missing_quote` blocks after provider
warmup and better evidence when a venue rejects or stalls a subscription.
`stale_quote_lease` remains a correct fail-closed guard when a venue does not
send a fresh BBO update within the configured TTL/age budget. Full production
behavior still needs a clean post-ARIA recovery window because current
`risk_only` / `fail_closed` state prevents new entry-path exercise.
