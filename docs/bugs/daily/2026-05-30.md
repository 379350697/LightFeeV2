# 2026-05-30 Production Regression Notes

## GitNexus Index

GitNexus MCP tools were unavailable in this session, so the CLI index was used
instead. `npx gitnexus analyze` refreshed the index, impact analysis reported
HIGH risk for the runtime admission/residual symbols and LOW for the transport
classifier, and `npx gitnexus detect-changes --scope all --repo LightFeeV2`
reported medium risk after the scoped fix.

| Cluster ID | Status | Severity | Fingerprint | Components | Symbols | Files | Commits | Outcome |
|---|---|---:|---|---|---|---|---|---|
| CL-019 | fixed, deployed, harness/probe verified | high | `v2.post-94e6257.ack-only-fill-gap + unsupported-probe-catalog-evidence-gap + okx-dust-rule-source-gap` | `venue-transport`, `exchange-error-evidence`, `live-runtime`, `passive-close`, `probe-diagnostics`, `v1-parity` | `GENIUSUSDT`, `SPACEUSDT`, `POWERUSDT`, `HOMEUSDT`, `RAVEUSDT`, `FORMUSDT`, `LABUSDT`, `BSBUSDT`, `BASUSDT`, `SNDKUSDT`, `HMSTRUSDT`, `ORCAUSDT`, `NOMUSDT` | `lightfee/venues/transport.py`, `lightfee/core/exchange_errors.py`, `lightfee/engine/runtime.py`, `lightfee/engine/passive_close.py`, `tests/test_venues_transport.py`, `tests/test_exchange_errors.py`, `tests/test_live_startup_preflight.py`, `tests/test_passive_close.py` | `e6b7645` after production `94e6257` | Post-`94e6257` production state and targeted read-only probes were flat/no-open-orders. No new trading semantic change was justified. Root fix is scoped to missing evidence: accepted-but-unfilled order acks now carry raw ack and fill-confirmation gap evidence, unsupported position probes carry catalog/cooldown diagnostics, and OKX passive-close dust terminal events carry cached official instrument rule source. Cloud fast-forward, manifest, compileall, focused harness, service restart, health, singleton, and targeted credentialed probes passed. |
| CL-018 | fixed, deployed, harness/probe verified | high | `v2.post-bbcd7b9.hmstr-residual-open-orders-pause + binance-2027-leverage-admission-gap + nomusdt-drift-failed-then-cleared + orca-live-recovered-mismatch + rave-reduceonly-flat + l2/probe-noise` | `live-runtime`, `residual-repair`, `passive-close`, `pending-entry`, `entry-admission`, `local-l2`, `diagnose-live`, `exchange-docs` | `HMSTRUSDT`, `NOMUSDT`, `ORCAUSDT`, `RAVEUSDT`, `LITEUSDT`, `AVGOUSDT`, `FHEUSDT`, `HOMEUSDT`, `POWERUSDT`, `HEIUSDT`, `GENIUSUSDT` | `lightfee/engine/runtime.py`, `lightfee/venues/transport.py`, `scripts/diagnose_live.py`, `tests/live_harness/test_residual_repair_incident_replay.py`, `tests/live_harness/test_exchange_admission_incidents.py`, `tests/test_venues_transport.py`, `tests/live_harness/test_20260529_jct_parti_regressions.py`, `tests/live_harness/test_local_l2_incident_replay.py` | fixed/deployed `0fd9a74` after production `bbcd7b9` | HMSTR open-order pause diagnosability is root-fixed without changing trading semantics. Binance `-2027` is now classified from official USD-M docs as an admission block on initial entry and pending hedge. Cloud fast-forward, manifest, compileall, service restart, production health, and targeted credentialed probes passed. |

## Cluster CL-019-post-94e6257-evidence-gap-closure

### GitNexus Keys

- Fingerprint: `v2.post-94e6257.ack-only-fill-gap + unsupported-probe-catalog-evidence-gap + okx-dust-rule-source-gap`
- Components: `venue-transport`, `exchange-error-evidence`, `live-runtime`, `passive-close`, `probe-diagnostics`, `v1-parity`
- Symbols: `VenueTransport._parse_order_fill`, `build_evidence_from_order_submit_error`, `LiveRuntime._filter_symbols_supported_by_venue`, `LiveRuntime._position_probe_exception_payload`, `PassiveCloseExecutor._resolve_hedge_min_notional_quote`, `PassiveCloseExecutor._submit_hedge_for_delta`, `PassiveCloseExecutor._flatten_live_one_sided_position`, `PassiveCloseExecutor._handle_live_imbalanced_positions`
- Files: `lightfee/venues/transport.py`, `lightfee/core/exchange_errors.py`, `lightfee/engine/runtime.py`, `lightfee/engine/passive_close.py`, `tests/test_venues_transport.py`, `tests/test_exchange_errors.py`, `tests/test_live_startup_preflight.py`, `tests/test_passive_close.py`
- Watch: `order accepted but fill not confirmed`, `accepted_order_id`, `fill_confirmation`, `recovery.live_position_probe_error`, `runtime.live_position_probe_unsupported_symbols`, `exit.passive_close_hedge_dust_aborted`
- Cards: [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md), [pending-entry-hedge-admission](../cards/pending-entry-hedge-admission.md), [residual-repair-live-truth](../cards/residual-repair-live-truth.md)

### Summary

Post-`94e6257` production health and targeted read-only probes showed no current false-green or stuck open state:

- `open_position_count=0`, `pending_entry_count=0`, `pending_close_count=0`, `pending_passive_close_count=0`, and `pending_residual_repair_count=0`.
- Real opens after deploy (`POWERUSDT`, `HOMEUSDT`, `GENIUSUSDT`, `SPACEUSDT`) later returned flat.
- Targeted credentialed probes were high-confidence flat/no-open-orders for the recent open symbols and the old watch symbols: `POWER`, `HOME`, `GENIUS`, `SPACE`, `RAVE`, `FORM`, `LAB`, `BSB`, `BAS`, `SNDK`, `HMSTR`, `ORCA`, `NOM`.

The remaining recurring events were evidence quality gaps, not proof of a new V1 semantic drift:

- GENIUS Bybit hedge emitted an accepted order ack without fill fields. V2 correctly failed uncertain, but the evidence did not preserve the accepted exchange/client IDs, raw ack body, or explicit fill-confirmation gap.
- Unsupported/timeout position probes did not carry enough catalog source, supported-symbol samples, symbol mapping, or cooldown-scope evidence to separate unsupported catalog noise from private-position probe instability after the fact.
- SPACE OKX passive-close dust terminal event logged a zero venue min-notional without preserving the official instrument rule source and quantity-contract metadata used to reach that dust decision.

### V1 / Exchange Decision

| Item | Decision | Fix Type | Closure |
|---|---|---|---|
| Passive close / residual repair trading semantics | No current open/pending/residual loop and no exchange nonzero truth after targeted probes, so no new trading-path V1 copy was justified. Existing V1 flatness/residual rules remain authoritative. | No behavior change | Existing passive/residual harnesses stayed green. |
| Accepted order ack without fill confirmation | This is a shared transport/evidence problem. A successful ack is not a fill, and V2 must remain uncertain until fill evidence or live truth exists. | Evidence root fix | Preserve raw ack, accepted IDs, missing fill fields, and mark evidence partial/medium instead of complete. |
| Unsupported/timeout position probes | This is a shared probe diagnosability problem. Missing symbol catalog/backoff evidence prevents root-cause classification. | Evidence root fix | Add catalog source/count/samples, venue-symbol mappings, probe category, symbol cooldown scope, and cooldown duration. |
| OKX dust terminal metadata | This is exchange-rule evidence, not a threshold change. OKX instrument rules can have zero min-notional while quantity/contract rules still define dust. | Exchange-rule evidence root fix | Preserve cached official instrument source, venue symbol, lot size, min quantity, contract value, and max market quantity in dust events. |

### Verification

| Environment | Evidence | Result |
|---|---|---|
| local RED | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_venues_transport.py::TestOrderAckNotFill::test_ack_only_response_raises_uncertain tests/test_venues_transport.py::TestOrderAckNotFill::test_bitget_ack_only_raises_uncertain tests/test_exchange_errors.py::test_build_evidence_from_ack_only_ose_preserves_fill_gap_context tests/test_live_startup_preflight.py::TestRuntimePreflight::test_live_position_fallback_probe_aggregates_unsupported_symbols tests/test_live_startup_preflight.py::TestRuntimePreflight::test_live_position_probe_timeout_has_explicit_classification tests/test_passive_close.py::TestTerminalMakerFillHedgeFail::test_terminal_maker_filled_okx_dust_logs_official_rule_source` before the fix | failed on missing ack-only, catalog/backoff, and OKX rule-source evidence |
| local GREEN | same focused test command after the fix | `6 passed` |
| local focused | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_passive_close.py tests/test_exchange_errors.py tests/test_diagnose_live.py tests/test_live_startup_preflight.py tests/live_harness/test_recovery_probe_evidence_incidents.py tests/live_harness/test_okx_recovery_probe_rate_limit_incident.py tests/live_harness/test_20260529_jct_parti_regressions.py tests/live_harness/test_historical_passive_close_incidents.py` | `244 passed` |
| local transport-adjacent | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_venues_transport.py::TestOrderAckNotFill tests/test_venues_transport.py::TestOrderSubmitDiagnosticsAndQuantization tests/test_venues_transport.py::TestAckOnlyResponses tests/test_venues_transport.py::TestExchangeErrorEnvelopes tests/test_venues_transport.py::TestJournalEvidenceFields tests/test_venues_transport.py::TestPassivePreflight` | `49 passed` |
| local wider transport | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_venues_transport.py` | unrelated Hyperliquid optional dependency failures (`Crypto`, `eth_account`) after the relevant transport groups passed |
| GitNexus CLI | impact before edits and `npx gitnexus detect-changes --scope all --repo LightFeeV2` before commit | high risk from the already-warned passive-close/runtime flows; changed scope stayed in the expected evidence payload and tests |
| cloud deploy | `/opt/lightfee-v2` `git pull --ff-only origin main`, `.deploy_version=e6b7645`, remote manifest generation/check, remote `compileall` | fast-forwarded `94e6257..e6b7645`; all 14 critical deploy files verified |
| cloud harness | `.venv/bin/python3 -m pytest -q` with the same six focused evidence tests under `/opt/lightfee-v2` | `6 passed` |
| cloud restart | `systemctl restart lightfee-sidecar.service lightfee-live.service` | both services `active/running`, `NRestarts=0`, `Result=success`, start time `Sat 2026-05-30 16:31:37 CST` |
| cloud health | `.venv/bin/python3 scripts/verify_production_services.py --json` after startup settled | `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/residual counts zero, and no exchange-truth mismatches |
| cloud singleton | `.venv/bin/python3 scripts/check_process_singleton.py --strict` | one sidecar, one live runtime, zero snapshot writers |
| cloud probes | credentialed `diagnose_live.py --since-deploy` with explicit venues for `POWER`, `HOME`, `GENIUS`, `SPACE`, `RAVE`, `FORM`, `LAB`, `BSB`, `BAS`, `SNDK`, `HMSTR`, `ORCA`, `NOM` | all reported `health=True`, no nonzero exchange positions, no open orders, and `state_verdict=consistent` when venue truth was not temporarily rate-limited. `POWER` later hit Aster `429` during repeated manual probe load, but Bybit stayed flat and production health remained green. |
| cloud current run count | current run `lightfee-1780129898734-1697977` event scan | `entry.opened=0`, `runtime.position_opened=0`, `runtime.pending_entry_registered=0`, `exit.passive_close_hedge_dust_aborted=0`, `execution.residual_repair_paused=0`; old-family noise remained: `runtime.snapshot_degraded=2`, `runtime.snapshot_fallback_last_good=3`, `runtime.local_l2_snapshot_error=19`, `recovery.live_position_probe_error=424` mostly Hyperliquid probe errors |

### Acceptance

Accepted for the scoped evidence gap. This work intentionally does not alter order routing, quantity sizing, passive-close retry decisions, residual repair clearing, or Local-L2 continuity semantics. Code is deployed at `e6b7645`, local and cloud focused harnesses pass, production health is green, current run has no opens/pending/residual/passive dust/residual pause, and targeted credentialed probes showed flat/no-open-order exchange truth. Remaining post-deploy events are old-family Local-L2/snapshot/probe noise and are not caused by this evidence-only patch.

## Cluster CL-018-post-bbcd7b9-production-watch-residual-live-truth-and-exchange-admission

### GitNexus Keys

- Fingerprint: `v2.post-bbcd7b9.hmstr-residual-open-orders-pause + binance-2027-leverage-admission-gap + nomusdt-drift-failed-then-cleared + orca-live-recovered-mismatch + rave-reduceonly-flat + l2/probe-noise`
- Components: `live-runtime`, `residual-repair`, `passive-close`, `pending-entry`, `entry-admission`, `local-l2`, `diagnose-live`, `exchange-docs`
- Symbols: `Runtime._recover_residual_repairs`, `Runtime._maybe_check_active_position_drift`, `Runtime._flatten_live_position_leg_quantity`, `scripts.diagnose_live`
- Files: `lightfee/engine/runtime.py`, `lightfee/venues/transport.py`, `scripts/diagnose_live.py`, `tests/live_harness/test_residual_repair_incident_replay.py`, `tests/live_harness/test_exchange_admission_incidents.py`, `tests/test_venues_transport.py`, `tests/live_harness/test_20260529_jct_parti_regressions.py`, `tests/live_harness/test_local_l2_incident_replay.py`
- Commits: production was `bbcd7b9`; fixed and deployed as `0fd9a74`
- Watch: `execution.residual_repair_paused`, `residual_repair_live_open_orders_present`, `runtime.position_drift_correction_failed`, `recovery.live_detected`, `recovery.live_mismatch_flattened`, `exit.passive_close_hedge_error`, `-2022`, `-2027`, `110126`, `-2013`, `51000`, `runtime.local_l2_snapshot_error`, `recovery.live_position_probe_error`
- Cards: [residual-repair-live-truth](../cards/residual-repair-live-truth.md), [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md), [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md), [pending-entry-hedge-admission](../cards/pending-entry-hedge-admission.md), [local-l2-sequence-continuity](../cards/local-l2-sequence-continuity.md)

### Summary

Post-`bbcd7b9` production stayed healthy at the current check:

- `lightfee-sidecar.service` and `lightfee-live.service` are `active/running`, `NRestarts=0`.
- Current `runtime/live-state-current.json`: `lifecycle=running`, `risk_mode=running`, `global_risk_mode=running`, `open_position_count=0`, `pending_entry_count=0`, `pending_close_count=0`, `pending_passive_close_count=0`, `pending_residual_repair_count=0`, `last_error=null`, `recovery_blocked_reason=null`.
- Fresh credentialed read-only probes on 2026-05-30 11:15 +0800:
  - `HMSTRUSDT` `bybit,okx`: high-confidence flat, open orders empty, gate passed.
  - `ORCAUSDT` `bybit,aster`: high-confidence flat, open orders empty, gate passed.
  - `NOMUSDT` `bybit,aster`: high-confidence flat, open orders empty, gate passed.
  - `RAVEUSDT` `bybit,okx`: high-confidence flat, open orders empty. The diagnose acceptance gate still flags historical residual/entry events, so it is not a fixture-closed incident yet even though current exchange truth is clean.

Since deploy, production did emit recurring old-family signals:

- `entry.opened=9` and all current state later returned flat.
- `HMSTRUSDT` residual repair paused repeatedly with `last_error=residual_repair_live_open_orders_present` and later completed as `already_flat`.
- `NOMUSDT` emitted one `runtime.position_drift_correction_failed`, then `runtime.stale_recovery_block_cleared`.
- `ORCAUSDT` emitted `recovery.live_detected`, resolved a passive close, then later emitted `recovery.live_mismatch_flattened`; current probe is flat.
- `RAVEUSDT` emitted residual repair completions and one complete Aster `-2022 ReduceOnly Order is rejected` passive-close hedge error; current probe is flat/no-open-orders.
- Admission/transport noise included Bybit trading-terms rejects for `LITEUSDT`/`AVGOUSDT`, Binance `-2027` leverage-cap rejects for `HMSTRUSDT`, Binance `-2013 Order does not exist` reconcile noise for `FHEUSDT`, OKX `51000 Parameter ordId error` for `RAVEUSDT`, and a Binance `-5022` post-only maker reject.
- Probe and data-plane noise included Binance/Bitget/Gate/Hyperliquid/OKX timeout or rate-limit probe errors plus Local-L2 rebuild/snapshot errors, mostly official continuity mismatch shapes.

Post-fix cloud verification on 2026-05-30:

- Cloud `/opt/lightfee-v2` fast-forwarded from `bbcd7b9` to `0fd9a74`, wrote `.deploy_version=0fd9a74`, generated `.deploy_manifest.json`, passed remote `compileall`, and verified all 14 critical deploy files.
- `systemctl restart lightfee-sidecar.service lightfee-live.service` completed without timeout/KILL. Both services restarted at `Sat 2026-05-30 11:53:46 CST`, `active/running`, `NRestarts=0`, `Result=success`.
- Final `verify_production_services.py --json`: `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, `open_position_count=0`, `pending_entry_count=0`, `pending_close_count=0`, `pending_residual_repair_count=0`, and no exchange-truth mismatches.
- During the post-fix watch, `HOMEUSDT` and `POWERUSDT` opened as real hedged positions and then auto-closed. Final credentialed probes are high-confidence flat/no-open-orders for both.
- `HEIUSDT` reproduced the Binance `-2027` family after deployment. The new path emitted `runtime.entry_admission_blocked`, `pending_entry.hedge_admission_blocked`, and `entry.aborted(reason=hedge_admission_blocked:leverage_admission_blocked)`; targeted Binance/Bybit probe is high-confidence flat/no-open-orders.
- `GENIUSUSDT` hit Binance `-5022` post-only maker rejection; targeted Binance probe is high-confidence flat/no-open-orders.
- POWER close produced post-terminal exchange noise (`OrderLinkedID is duplicate`, `ReduceOnly Order is rejected`) after live-matched close, then emitted `exit.passive_close_recovery_probe_flat`, `exit.passive_close_fallback_terminal_flat`, and `recovery.flat`. Current state and exchange truth are flat, so this is logged as watch noise, not an unresolved open loop.

### V1 / Exchange Decision

The current evidence does not justify a new trading-path semantic change.

- Residual repair: V1 treats residual repair as normal housekeeping, but it must not clear when trusted open-order truth says live orders still exist. If live excess is zero and trusted open-order truth is clear, the task must complete and release the pair gate. The HMSTR run ended in that state, so the production behavior is consistent unless a sanitized fixture proves the `open_orders_present` evidence was stale, local-only, or unsupported.
- Passive close / drift: V1 lets live exchange truth outrank local recovered state. NOM and ORCA resolved through live truth; RAVE `-2022` also required live flat/no-open-order confirmation before being called safe. These are covered by the passive-close/live-truth family, but RAVE still needs a fixture because diagnose sees historical residual events without a fixture conclusion.
- Admission: Bybit trading-terms and Binance/Aster leverage/max-notional rejects are deterministic exchange admission blocks. They should be classified, block repeated entry attempts, and drive cleanup if exposure exists. This run proved a real gap: Binance `-2027` is an official USD-M futures `MAX_LEVERAGE_RATIO` code, but V2 only handled the same code for Aster. The fix is exchange-doc classification, not V1 semantic copy.
- Local-L2: Binance/Aster official order-book rules still require strict continuity. High rebuild volume is not fixed by accepting broken deltas; only missing evidence or entry-blocking without diagnostics is root-fixable.
- Probe rate limits/timeouts: V1-style scoped cooldown and explicit venues are the right direction. This run shows noise, not a current false-green state. A root fix is warranted only if rate limits hide live nonzero positions or prevent closure evidence.

### Exchange Docs

- Binance USD-M error codes document `-1003 TOO_MANY_REQUESTS`, `-1007 TIMEOUT`, `-2013 NO_SUCH_ORDER`, `-2022 REDUCE_ONLY_REJECT`, `-2027 MAX_LEVERAGE_RATIO`, and `-5022 GTX_ORDER_REJECT`: https://developers.binance.com/docs/derivatives/usds-margined-futures/error-code
- Binance USD-M local order-book docs require buffering stream events, bridging to a REST snapshot, then requiring each new event's `pu` to equal the previous event's `u`; otherwise the local book must be reinitialized: https://developers.binance.com/docs/derivatives/usds-margined-futures/websocket-market-streams/How-to-manage-a-local-order-book-correctly
- Bybit V5 error docs currently list `110123`/`110125` trading-terms errors. Production saw `110126` with the same "must sign required agreement" family, so V2 should classify by both code and message text instead of relying on one exact code: https://bybit-exchange.github.io/docs/v5/error
- OKX docs define `50011` as rate-limit reached and document per-endpoint rate-limit rules; the same docs show order-detail `ordId`/`clOrdId` requirements and `51000` parameter errors around invalid required order parameters: https://www.okx.com/docs-v5/en/

### Root-Fix Triage

| Item | Decision | Root-fixable? | Harness / Probe Path |
|---|---|---:|---|
| `HMSTRUSDT` residual pause with `residual_repair_live_open_orders_present` | Current production probe is high-confidence flat/no-open-orders and the task completed. The repeated pause behavior matches fail-closed V1 parity when open orders are present, but the pause event lacked enough evidence to prove the open-order truth after the fact. | Evidence gap root-fixed locally. No trading-path semantic change selected. | Added RED/GREEN HMSTR-style harness asserting paused events carry `open_order_count`, `open_order_counts_by_venue`, `live_truth_venues`, and live excess evidence. |
| `NOMUSDT` `position_drift_correction_failed` then stale block cleared | Current exchange truth is high-confidence flat. Existing drift/passive harness covers failure -> fail-closed -> flat truth cleanup. | Low unless repeated failures stay latched or leave nonzero exchange truth. | Add fixture only if another NOM sample shows stale local/pending after live flat truth. Probe path is explicit `diagnose_live.py --symbol NOMUSDT --venues bybit,aster`. |
| `ORCAUSDT` recovered live pair then later live mismatch flattened | Current exchange truth is high-confidence flat. This matches exchange-truth priority, not a current false-green. | Low unless repeated mismatch reveals duplicate hydration or stale local quantity. | Keep under pending-entry live-truth card. Fixture should assert recovered live pair closes and later mismatch flattening leaves no open/pending state. |
| `RAVEUSDT` residual/passive events and Aster `-2022` | Current exchange truth is high-confidence flat, but diagnose gate remains false on historical residual/entry events without a fixture conclusion. | Yes as harness closure, not necessarily production code. | Add RAVE fixture or reuse passive terminal-flat harness: Aster `-2022` must re-probe live truth and clear only when both legs are flat/no-open-orders. |
| Bybit `110126` trading terms and Binance `-2027` leverage cap | Bybit terms were already covered. Binance `-2027` was a real exchange-doc classification gap: initial entry and pending hedge could treat the official leverage cap as generic reject. | Root-fixed locally for Binance `-2027`; no strategy change. | RED/GREEN coverage added to runtime admission, pending hedge abort/cleanup, and transport diagnostics. |
| Local-L2 rebuild/snapshot errors | Official continuity mismatch or snapshot rebuild behavior; not safe to relax. | Only if evidence classification becomes insufficient or blocks entries without diagnostics. | Existing Local-L2 incident replay plus public `probe_local_l2_rebuilds.py` for affected venue/symbol. |
| Recovery probe timeout/rate-limit noise | Operational/API pressure unless it masks live truth. | Maybe for venue-scoped backoff/reporting, not trading semantics. | Existing OKX rate-limit harness. Add Binance timeout/rate-limit fixture only if live truth is blocked during cleanup. |

### Harness / Probe Closure Plan

1. Preserve the production read-only probe command family:
   - `diagnose_live.py --json --symbol HMSTRUSDT --venues bybit,okx`
   - `diagnose_live.py --json --symbol ORCAUSDT --venues bybit,aster`
   - `diagnose_live.py --json --symbol NOMUSDT --venues bybit,aster`
   - `diagnose_live.py --json --symbol RAVEUSDT --venues bybit,okx`
2. Local HMSTR evidence harness:
   - RED before fix: `tests/live_harness/test_residual_repair_incident_replay.py::test_hmstr_open_orders_present_pause_records_truth_evidence` failed with `KeyError: 'open_order_count'`.
   - Fix: `Runtime._recover_residual_repairs()` now records per-venue open-order counts and passes them into `execution.residual_repair_paused`; pause/resume/failure events also preserve the aggregate count evidence. This does not change clear/pause/submit decisions.
   - GREEN after fix: the same focused test passed.
3. Local Binance `-2027` admission harness:
   - RED before fix: Binance `-2027` initial entry was not blocked, pending hedge stayed pending, and transport diagnostics reported generic `rejected`.
   - Fix: runtime and transport now classify Binance `-2027` / `MAX_LEVERAGE_RATIO` / "maximum allowable position at current leverage" as `leverage_admission_blocked` with Binance USD-M official error-code URL. Pending hedge routes through existing admission abort/cleanup.
   - GREEN after fix: runtime admission, pending hedge, and transport tests all passed.
4. Add sanitized JSON fixtures only if a future recurrence needs event replay beyond the synthetic live-harness path:
   - `tests/fixtures/live_incidents/2026-05-30/hmstrusdt_residual_open_orders_pause.json`
   - `tests/fixtures/live_incidents/2026-05-30/raveusdt_reduceonly_flat_terminal.json`
   - Optional only if recurrence continues: `nomusdt_drift_failed_then_cleared.json`, `orcausdt_live_recovered_mismatch_flattened.json`
5. GREEN requirements:
   - Focused residual/passive/admission/Local-L2 harness passes.
   - Credentialed read-only probes remain high-confidence flat/no-open-orders for affected symbols.
   - Production current state remains zero open/pending/residual and no recovery block.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| local RED | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/live_harness/test_residual_repair_incident_replay.py::test_hmstr_open_orders_present_pause_records_truth_evidence` before the runtime change | failed on missing `open_order_count` in `execution.residual_repair_paused` |
| local GREEN | same focused HMSTR harness after the runtime change | `1 passed` |
| local RED | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/live_harness/test_exchange_admission_incidents.py::test_exchange_rule_rejects_create_admission_blocks_with_evidence_payload tests/live_harness/test_exchange_admission_incidents.py::test_pending_hedge_binance_leverage_reject_aborts_without_retry tests/test_venues_transport.py::TestV1PassiveBusinessFlowParity::test_binance_2027_leverage_reject_is_classified_as_admission` before the Binance classification fix | failed: initial entry retried, pending hedge stayed pending, and transport emitted `response_classification=rejected` |
| local GREEN | same Binance `-2027` admission/transport harness after the classification fix | `9 passed` |
| local focused | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_live_entry_hedge_root_fix.py::TestResidualRepairExecutionV1Parity tests/live_harness/test_residual_repair_incident_replay.py tests/live_harness/test_20260529_jct_parti_regressions.py tests/live_harness/test_exchange_admission_incidents.py tests/live_harness/test_local_l2_incident_replay.py` | `39 passed` |
| local admission/transport | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_live_entry_hedge_root_fix.py::TestResidualRepairExecutionV1Parity tests/live_harness/test_residual_repair_incident_replay.py tests/live_harness/test_20260529_jct_parti_regressions.py tests/live_harness/test_exchange_admission_incidents.py tests/test_venues_transport.py -k '2027 or 2019 or 5022 or bybit_110126 or trading_terms or max_notional' tests/test_live_startup_preflight.py::TestRuntimePreflight::test_bybit_trading_terms_reject_blocks_symbol_admission tests/test_live_startup_preflight.py::TestRuntimePreflight::test_aster_max_leverage_reject_blocks_symbol_admission tests/live_harness/test_local_l2_incident_replay.py` | `16 passed, 401 deselected` |
| local profile | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev python scripts/validate_change.py --profile live-harness --keep-going` | compileall passed, `git diff --check` passed, `tests/live_harness` `66 passed` |
| GitNexus CLI | `npx gitnexus impact ...` on edited runtime/transport symbols, then `npx gitnexus detect-changes --scope all --repo LightFeeV2` | runtime impacts HIGH and transport classifier LOW; final detect-changes reported 10 files, 33 symbols, 3 affected residual-repair flows, medium risk |
| cloud deploy | `/opt/lightfee-v2` `git pull --ff-only origin main`, `.deploy_version=0fd9a74`, remote manifest generation/check, remote `compileall` | fast-forwarded `bbcd7b9..0fd9a74`; all 14 critical deploy files verified |
| cloud restart | `systemctl restart lightfee-sidecar.service lightfee-live.service` | both services `active/running`, `NRestarts=0`, `Result=success`, no stop timeout/KILL |
| cloud health | `.venv/bin/python3 scripts/verify_production_services.py --json` | `ok=true`, no critical/warning reports, final local open/pending/residual counts zero |
| cloud probe | `diagnose_live.py` with systemd env for `HMSTRUSDT`, `ORCAUSDT`, `NOMUSDT`, `RAVEUSDT`, `LITEUSDT`, `AVGOUSDT`, `FHEUSDT`, `HOMEUSDT`, `POWERUSDT`, `HEIUSDT`, `GENIUSUSDT` and explicit real venues | all final targeted probes high-confidence flat/no-open-orders and `state_verdict=consistent`; unsupported Aster/OKX symbol noise for HEI/GENIUS was excluded by real-venue targeted probes |

### Acceptance

Accepted for this cluster. The code fix is deployed at `0fd9a74`, local RED/GREEN harnesses pass, cloud health is clean, and targeted read-only probes show no current open positions, pending work, residual repairs, or open orders on the affected symbols. HMSTR's diagnosability gap is closed without changing trading semantics. Binance `-2027` is closed by official exchange-doc classification and existing pending-entry cleanup. RAVE remains watch-only only for future recurrence classification; current truth is flat.
