# 2026-06-03 Bug Ledger

## Cluster CL-035-post-e087513-long-window-follow-up

### GitNexus Keys

- Fingerprint: `post-e087513.binance-recovery-placeholder-orderid + ws-bbo-close-missing-quote-evidence + ws-bbo-budget-exhausted-not-subscription-regression`
- Components: `venue-transport`, `pending-entry-reconciliation`,
  `live-runtime`, `passive-close`, `entry-readiness-provider`,
  `production-observability`, `exchange-docs`
- Symbols: `VenueTransport._fetch_order_status_binance`,
  `LiveRuntime._resolve_ws_bbo_close_mid`,
  `WsBboQuoteLeaseEntryReadinessProvider._validate_quotes`
- Files: `lightfee/venues/transport.py`, `lightfee/engine/runtime.py`,
  `tests/test_venues_transport.py`, `tests/test_runtime_snapshot_freshness.py`
- Commits: `e087513`, working tree follow-up
- Watch: `pending_entry.finalize_fill_reconciliation_error`,
  Binance `-1102`, `runtime.close_price_evidence_missing`,
  `runtime.close_price_evidence_stale`,
  `exit.passive_close_missing_l2_or_tick`,
  `entry_ws_bbo_quote_lease_budget_exhausted`,
  `entry_ws_bbo_quote_lease_stale_quote`,
  `runtime.local_l2_hot_stale_rebuild`
- Cards: [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md),
  [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md)

### Production Evidence

Read-only production review after cloud deploy `e087513d4b7e799227c14181341b9313f88cd74a`
showed current service health green and no active local trading work, but the
longer deploy window produced three follow-up evidence tracks.

| Track | Evidence | Current conclusion |
|---|---|---|
| Current service state | `verify_production_services.py --json`: `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/residual counts `0`, `exchange_truth_mismatches=[]` | deploy is healthy now |
| Opens after deploy | `entry.opened=13`, `runtime.position_opened=13`; current open/pending/residual `0` | real entries opened and later flattened |
| Old WS BBO subscription bug | `entry_ws_bbo_quote_lease_waiting_for_subscription=0` in the inspected long window | old subscription-waiting shape did not recur |
| WS BBO budget/stale split | `entry_ws_bbo_quote_lease_budget_exhausted=282`, `entry_ws_bbo_quote_lease_stale_quote=86` | budget-out is the new correct classification; stale quote is fail-closed with REST refresh evidence |
| Local-L2 hot rebuild | concrete `runtime.local_l2_hot_stale_rebuild=0`; `runtime.local_l2_sequence_gap=0` | CL032 original event did not recur |
| L2 diagnostic aggregate | `diagnose_live.py` showed `l2_evidence.stale_rebuild_count=5` | not equal to CL032; diagnose also counts `runtime.snapshot_stale`, `runtime.snapshot_degraded`, and `runtime.local_l2_sync_failed` |
| Binance finalize | `pending_entry.finalize_fill_reconciliation_error=3`, Binance `-1102`, symbols `CLOUSDT`, `TRIAUSDT`, `PEOPLEUSDT`, order ids shaped like `entry-...-recovery-short` | real root-fix candidate |
| Passive close price | `exit.passive_close_missing_l2_or_tick=2`, symbols `STEEMUSDT`, `TRIAUSDT`, positive tick size but `price_hint=0.0` | current state recovered; evidence was insufficient when WS BBO fallback had no quote |

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| Root-fixed locally | Binance recovery placeholder order id sent as Binance `orderId` | Binance USD-M query-order semantics distinguish exchange `orderId` from `origClientOrderId`; V2 recovery synthetic ids such as `entry-...-recovery-short` are not numeric exchange order ids and must not be sent as `orderId`. |
| Evidence gap fixed locally | close-price WS BBO fallback returned `0.0` when no cache/quote/budget existed | The fail-closed behavior was correct, but missing quote/cache/budget branches were silent, leaving only the later passive-close `price_hint=0.0` event. |
| Not old bug recurrence | WS BBO still appears as `budget_exhausted` | This is the intended classification from CL033. Do not widen budget or TTL from this evidence alone. |
| Needs exchange-doc/probe if it blocks trading | WS BBO `stale_quote` with REST refresh no-quote/invalid/cache-updated outcomes | Use the existing `rest_refresh` evidence to split venue top-book idle behavior from actual transport errors. |
| Evidence ambiguity | `diagnose_live.py` `stale_rebuild_count=5` | Follow up by splitting the aggregate by exact event kind; do not call it Local-L2 hot stale rebuild without `runtime.local_l2_hot_stale_rebuild`. |

### Fix

- `VenueTransport._fetch_order_status_binance()` now sends Binance `orderId`
  only when the id is numeric. When recovery supplies a synthetic placeholder
  and a client id is available, it queries `/fapi/v1/order` with
  `origClientOrderId`. If only a nonnumeric id exists, it is treated as
  `origClientOrderId` instead of malformed `orderId`.
- `LiveRuntime._resolve_ws_bbo_close_mid()` now emits
  `runtime.close_price_evidence_missing` for active `ws_bbo_quote_lease`
  fallback branches where the quote-lease budget, cache, quote, or fallback
  path is unavailable. It keeps the same fail-closed return value `0.0`.
- No WS BBO budget increase, TTL widening, Local-L2 lifecycle change, order
  submit behavior, close executor behavior, residual repair, or recovery policy
  changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus analyze --index-only --force --max-file-size 2048` | index refreshed with `runtime.py` included |
| GitNexus pre-edit impact | `VenueTransport._fetch_order_status_binance` UID upstream | LOW: 1 direct caller, 1 affected `fetch_order_status` process |
| GitNexus pre-edit impact | `LiveRuntime._resolve_ws_bbo_close_mid` UID upstream | LOW: 3 impacted symbols, affected `run_loop` / live `main` through close hint resolution |
| GitNexus pre-edit impact | `PassiveCloseExecutor._submit_maker_order` UID upstream | LOW; reviewed but not changed |
| local RED | Binance recovery placeholder order id with valid client id | failed before fix: result was `None` because query used malformed `orderId` |
| local RED | active WS BBO close fallback with no quote | failed before fix: no `runtime.close_price_evidence_missing` event |
| local GREEN focused | new Binance and close-price evidence tests | `2 passed` |
| local adjacent | venue reconciliation, close hint, client-order-id reconciliation | `4 passed`, `4 passed`, `6 passed` |
| local impacted files | `tests/test_venues_transport.py`; `tests/test_runtime_snapshot_freshness.py` | `381 passed`; `20 passed` |
| local full gate | `.venv/bin/python -m pytest -q` | `3367 passed`, `9 skipped`, `1 warning` |
| local hygiene | `.venv/bin/python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | MEDIUM: `7 files`, `21 symbols`, `2` affected flows (`_resolve_ws_bbo_close_mid` runtime/main path and `_fetch_order_status_binance` fetch-order-status path), matching the intended scope |

### Acceptance

Local full gate green. First cloud deploy to `a272b6b` fast-forwarded and the
focused remote regression suite passed, but production health did not turn
green because CL-036 startup live-position probe fanout triggered Binance
private REST rate limiting and stale live ticks. CL035 still needs a clean
post-CL036 deploy-window scan proving Binance `-1102` finalize errors do not
recur and any future `exit.passive_close_missing_l2_or_tick` has nearby
`runtime.close_price_evidence_missing` or stale/fallback evidence.

## Cluster CL-036-startup-live-position-probe-static-universe-fanout

### GitNexus Keys

- Fingerprint: `startup-live-position-probe.empty-requested-symbols + static-config-universe-fanout + binance-positionrisk-429 + live_tick_stale`
- Components: `live-runtime`, `startup-recovery`, `venue-binance`,
  `production-health`, `production-observability`
- Symbols: `LiveRuntime._fetch_startup_live_position_snapshots`,
  `LiveRuntime._startup_position_probe_symbols`, `LiveRuntime.start`
- Files: `lightfee/engine/runtime.py`, `tests/test_live_startup_preflight.py`
- Commits: `a272b6b`, working tree follow-up
- Watch: `live_tick_stale`, Binance `HTTP/1.1 429 Too Many Requests`,
  `GET /fapi/v2/positionRisk`,
  `recovery.live_position_static_config_probe_skipped`

### Production Evidence

The first `a272b6b` deploy updated `.deploy_version` and restarted both
services. `lightfee-live.service` and `lightfee-sidecar.service` were active
with `NRestarts=0`, but `verify_production_services.py --json` stayed red:
`critical_count=1`, fingerprint `live_tick_stale`, tick age growing from
`17082ms` to `136029ms`. Current local trading state was otherwise flat:
open/pending/passive/residual counts were `0` and
`exchange_truth_mismatches=[]`.

Recent live logs showed startup recovery repeatedly calling Binance
`GET /fapi/v2/positionRisk` and finally receiving
`HTTP/1.1 429 Too Many Requests`. The exported state file existed, but its
payload had no fresh `ts_ms`, so production health correctly rejected the
deploy as stale.

### Root Cause

`LiveRuntime._startup_position_probe_symbols()` correctly returned an empty
list for clean startup state, matching the existing rule that startup recovery
must not expand clean state to the configured trading universe. But
`LiveRuntime._fetch_startup_live_position_snapshots([])` had a later fallback:
if a venue did not return bulk positions and no per-venue requested symbols
remained, it rebuilt the probe set from `config.symbols`.

That preserved small local false-clean tests, but in production the configured
universe is large. For Binance, where the fallback endpoint is
`/fapi/v2/positionRisk`, this created an unbounded per-symbol private REST
fanout during startup, hit Binance rate limits, and prevented the runtime from
reaching fresh ticking health.

### Fix

- Keep existing false-clean protection for a single explicitly configured
  symbol by allowing static fallback only when `len(config.symbols) <= 1`.
- When the static universe is larger, skip per-symbol fallback and emit
  `recovery.live_position_static_config_probe_skipped` with venue, static
  symbol count, limit, requested symbol count, and decision evidence.
- Requested/recovery symbols, local open/pending/passive/residual work,
  catalog filtering, bulk position APIs, mismatch flattening, balanced
  hydration, and all trading execution paths are unchanged.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus pre-edit impact | `LiveRuntime._fetch_startup_live_position_snapshots` UID upstream | HIGH: startup `main` path and runtime clean-position probe affected; edit kept to empty/static fallback branch |
| local RED | clean startup, empty requested symbols, two static config symbols, no bulk positions | failed before fix: per-symbol `fetch_position` called for `BTCUSDT` and `ETHUSDT` |
| local GREEN focused | new static-universe fanout regression | `1 passed` |
| local adjacent | `tests/test_live_startup_preflight.py::TestRuntimePreflight` | `47 passed` |
| local adjacent | OKX/rate-limit recovery probe harnesses | `11 passed` |
| local full gate | `.venv/bin/python -m pytest -q` | `3368 passed`, `9 skipped`, `1 warning` |
| local hygiene | `.venv/bin/python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |

### Acceptance

Local full gate green. Cloud deploy `cb1abbed4516ff016b78be6cc1e0f588d40853c7`
fast-forwarded from `a272b6b`, remote focused regression tests passed
(`60 passed`), compileall passed, manifest/deploy marker were updated, and
both services restarted active with `NRestarts=0`.

Post-deploy `diagnose_live.py --since-deploy --json` no longer showed the
Binance startup `429` / `live_tick_stale` fanout. `l2_evidence` stayed clean
(`missing_l2_or_tick_count=0`, `stale_rebuild_count=0`,
`sequence_gap_count=0`), so CL036 is effective. Production health was still
red for a separate CL037 recovery-block lifecycle issue:
`lifecycle=risk_only`, `recovery_blocked_reason=startup_recovery_pending_work_without_open_positions`,
and one balanced live-recovered `HIVEUSDT` position.

## Cluster CL-037-runtime-live-recovery-stale-startup-block

### GitNexus Keys

- Fingerprint: `runtime-live-position-probe.recovered-balanced-position + stale startup_recovery_pending_work_without_open_positions + lifecycle_risk_only`
- Components: `live-runtime`, `startup-recovery`, `pending-entry-recovery`,
  `production-health`, `production-observability`
- Symbols: `LiveRuntime._maybe_recover_clean_live_positions`,
  `LiveRuntime._finalize_startup_recovery`,
  `clear_stale_recovery_block_if_recovery_clean`
- Files: `lightfee/engine/runtime.py`, `tests/test_live_startup_preflight.py`
- Commits: `cb1abbe`, working tree follow-up
- Watch: `recovery.blocked`, `runtime.started lifecycle=risk_only`,
  `pending_entry.missing_hedge_detected`,
  `pending_entry.hedge_submit_result`, `recovery.live_detected`,
  `runtime.running reason=startup_recovery_completed_with_positions`

### Production Evidence

Post-`cb1abbe` cloud verification showed services active with fresh ticks, but
`verify_production_services.py --json` stayed red:
`critical_count=1`, fingerprint `live_lifecycle_not_running`,
`risk_mode=running`, `lifecycle=risk_only`, `open_position_count=1`,
`pending_entry_count=0`, and no local close/residual work.

Credentialed read-only `diagnose_live.py --since-deploy --json` showed high
confidence exchange truth for the same recovered position: Binance
`HIVEUSDT` long `394`, Bybit `HIVEUSDT` short `394`, and no open orders on
either venue. Local state held
`live-recovered:HIVEUSDT:binance->bybit` quantity `394`.

The deploy-window event chain was:

| Seq | Event | Evidence |
|---|---|---|
| 23 | `order.reconcile_result` | Binance maker `HIVEUSDT` filled `394` |
| 28 | `recovery.blocked` | `reason=startup_recovery_pending_work_without_open_positions` |
| 31 | `runtime.started` | `lifecycle=risk_only`, `risk_mode=running` |
| 43 | `pending_entry.missing_hedge_detected` | retained HIVE pending needed hedge recovery |
| 45-48 | hedge submit/result | Bybit hedge submitted with exchange order id |
| 51 | `recovery.live_detected` | recovered balanced `HIVEUSDT` Binance->Bybit quantity `394` |

Old tracks in the same window did not recur: concrete
`runtime.local_l2_hot_stale_rebuild=0`, sequence gaps `0`, old WS BBO
`waiting_for_subscription=0`, and no Binance `-1102` finalize errors in the
54 parsed deploy-window events.

### Root Cause

This is a V2/V1 lifecycle-release semantic drift. `_finalize_startup_recovery()`
already encodes the V1 rule that open positions after recovery are normal
managed positions and should transition to `RUNNING` when they do not exceed
the configured maximum. But the stale block was created before the runtime
live-position probe recovered the balanced HIVE position.

After runtime housekeeping submitted the missing hedge and
`_maybe_recover_clean_live_positions()` hydrated the balanced live position,
the stale startup block was not re-finalized. The generic
`clear_stale_recovery_block_if_recovery_clean()` could not clear it because it
treats any `open_positions` as reconciliation work, which is correct for
startup classification but wrong after a balanced live position has just been
recovered as the managed state.

### Fix

- `LiveRuntime._maybe_recover_clean_live_positions()` now records the open
  position count before the runtime live probe.
- If that probe creates a new open position while the only block is
  `startup_recovery_pending_work_without_open_positions`, no pending
  entry/close/passive-close work remains, and the operator has not requested
  fail-closed, it reuses `_finalize_startup_recovery()`.
- The fix is intentionally narrow: no order submit path, Local-L2, WS BBO,
  residual repair, close executor, mismatch flattening, or generic recovery
  block clearing behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus pre-edit impact | `LiveRuntime._maybe_recover_clean_live_positions` UID upstream | LOW: 1 direct caller, affected `run_loop` / live `async_main` |
| local RED | stale `startup_recovery_pending_work_without_open_positions` plus balanced live positions recovered by runtime probe | failed before fix: lifecycle stayed `risk_only` |
| local GREEN focused | `test_runtime_live_recovery_clears_stale_pending_without_open_block` | `1 passed` |
| local adjacent | `tests/test_live_startup_preflight.py::TestRuntimePreflight` | `48 passed` |
| local adjacent | `tests/test_live_full_closure.py::TestLiveFullClosure` | `11 passed` |
| local adjacent | `tests/test_v1_parity_pending_entry_recovery_red.py::TestRecoveryLifecycleTransition` | `4 passed` |
| local adjacent | recovery block/classification/lifecycle tests | `15 passed` |
| local full gate | `.venv/bin/python -m pytest -q` | `3369 passed`, `9 skipped`, `1 warning` |
| local hygiene | `.venv/bin/python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | LOW: `5 files`, `14 symbols`, `0` affected processes |
| cloud focused | CL037 runtime/recovery plus CL035 Binance/close-price tests | `80 passed` |
| cloud hygiene | compileall; manifest generation/check | passed; all 14 critical deploy files verified |

### Acceptance

Local and cloud focused gates are green. Cloud deploy
`5625361a99d8f5efd0a589446d809353674c7b5e` fast-forwarded from `cb1abbe`,
updated `.deploy_version`, regenerated and verified the deploy manifest, and
restarted sidecar/live active with `NRestarts=0`.

Immediate verify first showed a short post-restart `live_tick_stale` while the
runtime was already `lifecycle=running`. A settled follow-up returned
`ok=true`, `critical_count=0`, `warning_count=0`, `tick_age_ms=2195`,
`risk_mode=running`, `lifecycle=running`, and local open/pending/close/residual
counts `0/0/0/0`. A credentialed HIVE-only probe over Binance/Bybit confirmed
exchange truth flat and no open orders:
`exchange_truth.available=true`, `confidence=high`, `has_nonzero_position=false`,
`has_open_order=false`, and `state_verdict=consistent`.

The old tracks did not recur in the settled post-deploy window:
`runtime.local_l2_hot_stale_rebuild=0`, Local-L2 sequence gaps `0`, old WS BBO
`waiting_for_subscription=0`, and no Binance `-1102` finalize lookup errors.
CL037 is cloud-verified.

The same HIVE close window still produced close-path evidence noise:
`runtime.close_price_evidence_missing=9`,
`exit.passive_close_missing_l2_or_tick=3`,
`exit.passive_close_missing_l2_tick_escalated=1`, plus one Bybit reduce-only
ack-only/duplicate-client-id retry and one Binance reduce-only-rejected sample
after the position was already converging flat. The terminal events were
`exit.closed`, `recovery.flat`, `runtime.position_drift_corrected`, and
`execution.residual_repair_completed`; current verify and credentialed exchange
truth are green. Treat this as a separate close-path evidence watch item, not
an active Local-L2 or old WS BBO subscription recurrence, unless it later
leaves nonzero local/exchange exposure or blocks close completion.

## Cluster CL-038-single-leg-force-close-and-ledger-attribution

### GitNexus Keys

- Fingerprint: `single-leg.normal-flatten-fails-no-force-close + ledger-row-outside-position-window + terminal-position-evidence-missing + okx-copy-trade-not-main-loss`
- Components: `passive-close`, `close-executor`, `position-lifecycle`,
  `production-observability`, `ledger-attribution`, `exchange-truth`
- Symbols: `PassiveCloseExecutor._flatten_live_one_sided_position`,
  `PassiveCloseExecutor._clear_live_flat_state`,
  `CloseExecutor._compensate_close_leg_exposure`
- Files: `lightfee/engine/passive_close.py`,
  `lightfee/engine/close_executor.py`, `tests/test_passive_close.py`
- Commits: `5625361`, `338caec`, working tree follow-up
- Watch: `exit.passive_close_live_one_sided_error`,
  `exit.passive_close_live_one_sided_force_close_problem`,
  `runtime.position_lifecycle_terminal`,
  `exit.compensated`, `execution.residual_repair_paused`,
  exchange ledger rows outside position windows
- Cards: [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md),
  [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md)

### Production Evidence

Read-only exchange-truth PnL classification over the recent 72-hour window
split realized exchange ledger rows into normal dual-leg funding, quick
single-leg flatten, long/unclosed single-leg, unclassified, and unallocated
ledger rows. The severe part was not current exposure; the settled service
state was green. The severe part was that realized ledger rows could not always
be tied back to a durable position lifecycle window or terminal order id.

| Track | Evidence | Current conclusion |
|---|---|---|
| Current production health | `verify_production_services.py --json`: `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/close/residual counts `0/0/0/0`, `exchange_truth_mismatches=[]` | no current stuck exposure |
| 72-hour category split | normal dual-leg funding `-0.1288613084`; single-leg quick flatten `-4.8860429605`; single-leg long/unclosed `-0.5583005328`; unclassified `-6.814394858`; unallocated ledger rows `-14.213442883` | ledger attribution and single-leg lifecycle need separate closure evidence |
| Long/unclosed single-leg samples | `DASHUSDT` 32.35m `-0.2633324149`; `ARIAUSDT` 210.125m `-0.2092261773`; `POWERUSDT` live-recovered 46.025m `-0.0476442076`; `HIVEUSDT` live-recovered 21.201m `-0.038097733` | not all single-leg exposure closed quickly enough |
| Unallocated ledger probe | 72-hour unallocated net by venue: Aster `-0.9360035`, Binance `-9.75266309`, Bybit `-3.18859255`, OKX `-0.299592603`; top symbols: Binance `HIVEUSDT=-8.23939478`, Bybit `LABUSDT=-3.50867762`, Bybit `HUSDT=-1.07859964` | OKX is not the main unallocated-loss source |
| Unallocated reason split | `same_symbol_outside_window=335`, `no_same_symbol_lightfee_window=43`, `has_lightfee_id=20` | time-window-only attribution is too weak; terminal order/client ids must be carried into lifecycle events |
| OKX follow/copy check | OKX current open positions `[]`; OKX 72-hour all-bill categories: `lightfee_tagged_or_runtime_id=120` net `-1.241017206`, `external_or_copy_clOrdId=8` net `+0.07306395`, `no_order_id_bill=20` net `+0.0733661833` | OKX follow/copy activity exists, but is small and currently flat; it does not explain the large unallocated loss |

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2/V1 semantic drift, fixed locally | normal live one-sided flatten can fail, then the path only probes/retries instead of forcing the existing V1 compensation hard-stop and marking the terminal as problematic | use existing `CloseExecutor.compensate_failed_full_close()` as the forced close fallback, then clear only after live truth proves flat |
| Evidence gap, fixed locally | flat cleanup writes drift/terminal-flat events but no durable `runtime.position_lifecycle_terminal` with client/order ids and problem reason | emit a lifecycle terminal event before clearing local state so ledger attribution can use ids and terminal reason instead of broad windows |
| Evidence gap, partially improved | compensation hard-stop order did not explicitly carry IOC on the reduce-only request in this low-level path | set `time_in_force=IOC` on compensation close-leg exposure |
| Not main issue | OKX follow/copy trading | cloud evidence shows only 8 OKX external/copy-style bill rows in 72 hours, net positive `+0.07306395`, and no current OKX position |
| Still needs follow-up evidence | historical unallocated ledger rows on Binance/Bybit/Aster | new lifecycle terminal evidence helps future rows; historical rows still require exact order/client-id replay or exchange order-history joins |

### Fix

- If `_flatten_live_one_sided_position()` receives a normal one-sided
  reduce-only IOC error and a close executor is available, it invokes
  `compensate_failed_full_close()` for a forced close/hard-stop path.
- The forced close branch appends
  `exit.passive_close_live_one_sided_force_close_problem` with
  `problem=true`, the failed stage/error, and force-close client/order ids.
- State is still cleared only through `_clear_if_live_flat()`, which probes
  both exchange legs twice and refuses to clear on untrusted live truth.
- `_clear_live_flat_state()` now writes
  `runtime.position_lifecycle_terminal` with `terminal_state=flat`,
  `terminal_reason`, `problem`, `problem_reason`, `client_order_ids`, and
  `order_ids` before removing local pending/open state.
- `CloseExecutor._compensate_close_leg_exposure()` now submits compensation
  reduce-only requests with `time_in_force=IOC`.
- No entry selection, Local-L2, WS BBO, sizing, residual-repair scheduling, or
  startup recovery behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus pre-edit impact | `PassiveCloseExecutor._flatten_live_one_sided_position` upstream | LOW: direct callers `_fallback_to_aggressive_close`, `_resolve_flat_maker_leg_from_live_truth`; affected `drive_pending_passive_close` process |
| GitNexus pre-edit impact | `PassiveCloseExecutor._clear_live_flat_state` upstream | HIGH: direct callers `_clear_if_live_flat`, `_fallback_to_aggressive_close`; affected `start_pending_passive_close`, `drive_pending_passive_close`, `_handle_live_imbalanced_positions` processes |
| GitNexus pre-edit impact | `CloseExecutor._compensate_close_leg_exposure` upstream | LOW: affected `execute_close` process through compensation flows |
| local RED/GREEN focused | normal one-sided IOC failure must force-close, mark problem, clear only after flat, and emit lifecycle terminal ids | `1 passed` |
| local adjacent | `tests/test_passive_close.py::TestFallbackResidualReal` | `14 passed` |
| local adjacent | `tests/test_close_execution.py::TestCompensateFailedFullClose` | `6 passed` |
| local hygiene | `python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| local full gate | `pytest -q` | `3370 passed`, `9 skipped`, `1 warning` |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | HIGH: `5 files`, `17 symbols`, `8` affected close/compensation flows; expected because `_clear_live_flat_state` is a high-impact cleanup symbol |
| cloud read-only OKX attribution | `/tmp/lightfee_okx_external_probe.py` on `/opt/lightfee-v2` | OKX current positions empty; external/copy-style bill rows are small and not the large unallocated-loss source |
| cloud read-only health | `scripts/verify_production_services.py --json` | `ok=true`, no critical/warning, running/running, local open/pending/close/residual counts `0/0/0/0`, exchange truth mismatches `[]` |

### Acceptance

Cloud deployed `338caec` and settled verification is green:
`verify_production_services.py --json` returned `ok=true`, `critical_count=0`,
`warning_count=0`, `risk_mode=running`, `lifecycle=running`, local
open/pending/close/residual counts `0/0/0/0`, and
`exchange_truth_mismatches=[]`. Services are active with `NRestarts=0`,
warning-level systemd logs since deploy are empty, and deploy-window diagnose
has `.deploy_version=338caec`, `version_mismatch=false`, `health.ok=true`, and
`runtime.position_lifecycle_terminal=14`.

The explicit force-close problem branch has not yet been hit in production
after deploy, so keep watching
`exit.passive_close_live_one_sided_force_close_problem`. Historical unallocated
Binance/Bybit/Aster rows remain evidence to join against exchange order
history; do not reclassify them as OKX copy trading.

## Cluster CL-039-pending-entry-orphan-maker-open-orders

### GitNexus Keys

- Fingerprint: `pending-entry.local-flat-exchange-maker-open-order + client-id-only-cancel-skipped + abort-removes-pending-before-open-order-truth`
- Components: `live-runtime`, `pending-entry`, `passive-maker-maintenance`,
  `startup-recovery`, `exchange-truth`, `production-safety`
- Symbols: `LiveRuntime._recover_cancel_maker_order`,
  `LiveRuntime._abort_pending_entry`,
  `LiveRuntime._maintain_pending_entry_passive_orders`,
  `LiveRuntime._recover_poll_order_status`,
  `LiveRuntime._pending_entry_has_maker_order_reference`,
  `EngineState.to_dict`, `PendingPassiveOrder`,
  `_restore_state_from_snapshot_dict`, `build_persistent_state_view`
- Files: `lightfee/engine/runtime.py`, `lightfee/engine/state.py`,
  `lightfee/engine/recovery.py`,
  `tests/test_live_entry_hedge_root_fix.py`
- Commits: `338caec`, `4b6a6ee`
- Watch: `entry.abort_retained_maker_open_order`,
  `entry.abort_maker_order_truth_unavailable`,
  `entry.abort_maker_cancel_requested`,
  `recovery.maker_cancel_requested`,
  `reconciliation.entry_abandoned_flat`,
  exchange open orders while local `pending_entry_count=0`
- Cards: [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md)

### Production Evidence

After `338caec` was healthy, an explicit all-venue exchange-truth probe found
local state flat but 9 exchange maker orders still resting:
`nonzero_position_count=0`, `open_order_count=9`, with Binance `1` and Bybit
`8`. The orders were non-reduce-only PostOnly/GTX entry maker orders with
LightFee client ids; event replay showed matching `order.submitted`,
`order.passive_submitted`, and `runtime.pending_entry_registered` chains.
OKX current positions and open orders were both empty, so this was not the OKX
copy/follow account path.

Safety action: after confirming all current positions were zero, the 9 exact
order id/client id targets were canceled through `VenueTransport`
`cancel_passive_order()`. Post-cancel truth returned
`nonzero_position_count=0`, `open_order_count=0`, and per-venue open-order
counts `0` for Binance, Aster, Bybit, OKX, Bitget, Gate, and Hyperliquid.
`verify_production_services.py --json` then returned `ok=true`,
`critical_count=0`, `warning_count=0`, `tick_age_ms=3253`,
`risk_mode=running`, `lifecycle=running`, local open/pending/close/residual
counts `0/0/0/0`, and `exchange_truth_mismatches=[]`.

Deploy-window event scan showed no `runtime.local_l2_hot_stale_rebuild`.
Local-L2 related counts were only `runtime.local_l2_phase_start=1`,
`runtime.local_l2_phase_complete=1`, and
`runtime.entry_blocked_local_l2_selection=149`. The diagnose aggregate
`l2_evidence.stale_rebuild_count=2` is therefore not CL032 hot stale rebuild
evidence; it needs exact-kind split before being treated as a Local-L2
recurrence.

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2/V1 semantic drift, fixed locally | pending-entry hard-ceiling/abort cleanup can remove local pending after live positions are flat without first proving the maker resting order is gone | abort must not clear pending until maker open-order truth is absent or terminal |
| V2 lifecycle drift, fixed locally | recovery cancel required `maker_order_id`, but production pending entries can still be addressable by `maker_client_order_id` only | cancel/progress paths must use exchange order id or client order id |
| V2 maintenance drift, fixed locally | passive maker maintenance skipped entries whose `PendingPassiveOrder.order_id` was empty even when `client_order_id` existed | client-id-only passive orders remain maintainable and cancelable |
| V2 state-fidelity drift, fixed locally | `PendingEntry.passive_order` and `next_progress_poll_ms` were not persisted/restored, unlike V1 `PendingEntryHedge.passive_order` | snapshot/recovery now preserves the passive-order lifecycle so restart recovery still has maker identity, cancel state, progress state, and poll timing |
| V2 cancel-state drift, fixed locally | recovery cancel set only transient `_cancel_requested` | successful maker cancel now writes `passive_order.cancel_requested_at_ms`, matching V1 durable cancel-confirmation semantics |
| Current production safety closed | 9 orphan maker orders | exact canceled; follow-up all-venue truth shows no positions and no open orders |
| Not current active exposure | OKX copy/follow | OKX positions/open orders are empty; prior OKX external/copy-style ledger rows are small and not this bug |
| Still evidence watch | WS BBO quote lease budget/stale blocks | current events show fail-closed entry blocking, not old `waiting_for_subscription`; do not widen subscription budgets without further evidence |

### Fix

- V1 reference checked: `PendingEntryHedge.passive_order` is mandatory in V1,
  and `force_terminalize_pending_entry_if_budget_exhausted()` cancels via
  `cancel_pending_entry_passive_order()` without requiring a separate
  `maker_order_id` gate.
- `_recover_cancel_maker_order()` now issues maker cancel when either
  `maker_order_id`, `maker_client_order_id`, or the persisted
  `PendingPassiveOrder` identity is present, records both ids, and writes
  `passive_order.cancel_requested_at_ms` after a successful cancel.
- `_recover_poll_order_status()` now polls passive-order progress by exchange
  order id or client order id, so startup recovery resolves client-id-only
  maker orders instead of skipping them.
- `_abort_pending_entry()` now first cancels the maker order if needed, then
  checks exact open-order truth. If the maker order is still visible, or open
  order/progress truth is unavailable, it enters fail-closed and retains the
  pending entry instead of deleting local state.
- `_maintain_pending_entry_passive_orders()` no longer skips client-id-only
  passive maker orders.
- `EngineState.to_dict()`, `build_persistent_state_view()`, and
  `_restore_state_from_snapshot_dict()` now roundtrip
  `PendingEntry.passive_order` and `next_progress_poll_ms`.
- New evidence events:
  `entry.abort_maker_cancel_requested`,
  `entry.abort_retained_maker_open_order`, and
  `entry.abort_maker_order_truth_unavailable`.
- No entry selection, sizing, WS BBO budget/TTL, Local-L2 lifecycle, close
  executor, or residual-repair policy changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus analyze` | refreshed successfully; `19,760` nodes, `41,615` edges, `300` flows |
| GitNexus pre-edit impact | runtime private symbols | GitNexus could not resolve `_abort_pending_entry`, `_recover_cancel_maker_order`, `_recover_poll_order_status`, `_maintain_pending_entry_passive_orders`, or `LiveRuntime`; treated manually as HIGH because `_abort_pending_entry` is the pending-entry removal choke point |
| GitNexus pre-edit impact | state/recovery symbols | `PendingPassiveOrder` and `EngineState` MEDIUM; `_restore_state_from_snapshot_dict` LOW; `build_persistent_state_view` LOW; `EngineState.to_dict` not individually resolvable |
| local RED | client-id-only maker cancel | failed before fix: `_recover_cancel_maker_order()` returned `False` and sent no cancel |
| local RED | abort while maker open order still visible | failed before fix: `_abort_pending_entry()` returned `True` and removed pending |
| local RED | passive-order snapshot roundtrip | failed before fix: `passive_order` key missing from `EngineState.to_dict()`/persistent view and restore returned `None` |
| local RED | durable cancel-request state | failed before fix: maker cancel sent but `passive_order.cancel_requested_at_ms` stayed `0` |
| local RED | startup passive progress by client id | failed before fix: `_recover_poll_order_status()` did not query client-id-only maker progress |
| local GREEN focused | CL039 maker-order tests | `4 passed` |
| local GREEN focused | passive-order snapshot tests | `3 passed` |
| local adjacent | `tests/test_live_entry_hedge_root_fix.py::TestPendingEntryPersistenceRoundtrip` | `7 passed` |
| local adjacent | `tests/test_venues_transport.py::TestPendingEntryRecoveryRoundtrip` | `4 passed` |
| local adjacent | `tests/test_recovery_reconciliation.py::TestSnapshotExtendedState` | `2 passed` |
| local adjacent | passive maker zero-fill harness | `4 passed` |
| local adjacent | `tests/test_live_entry_hedge_root_fix.py::TestRealPathAbortCleanupDeadline` | `33 passed` |
| local adjacent | `tests/test_pending_entry_v1_semantic_drift.py` | `12 passed` |
| local adjacent | passive maker zero-fill + pending-entry recovery | `22 passed` |
| local adjacent | full live-entry root-fix file | `134 passed` |
| local adjacent | `tests/test_recovery_reconciliation.py` | `43 passed` |
| local adjacent | runtime/entry/domain/passive-close suites | `31 + 66 + 107 passed` |
| local full gate | `uv run --extra dev pytest -q` | `3377 passed, 9 skipped, 1 warning` |
| local static gate | `compileall` + `git diff --check` | passed |
| GitNexus detect-changes | local diff | MEDIUM; affected flow `Startup_recovery_ready -> Is_terminal`, matching pending passive-order lifecycle; unrelated AGENTS/CLAUDE working-tree edits are not part of this fix |
| cloud safety | exact cancel + all-venue truth | 9/9 target orders canceled; all venues nonzero positions `0`, open orders `0` |
| cloud deploy | commit `4b6a6ee` | fast-forwarded on `/opt/lightfee-v2`; remote focused maker/passive lifecycle tests `117 passed`; compileall and deploy-manifest check passed; sidecar/live restarted active with `NRestarts=0` |
| cloud diagnose | `diagnose_live.py --since-deploy --json` | `git_head=4b6a6ee`, `.deploy_version=4b6a6ee`, `version_mismatch=false`, `health.ok=true`, local open/pending/close `0/0/0`, Local-L2 missing/stale/sequence-gap counts `0/0/0`, no entry/open-position events |
| cloud all-venue truth | credentialed read-only probe using production env | Binance, Aster, Bybit, OKX, Bitget, Gate, and Hyperliquid all `status=ok`; `nonzero_position_count=0`; `open_order_count=0`; per-venue open-order counts `0` |
| cloud logs/health | `journalctl -p warning` since restart; `verify_production_services.py --json` | no warning/error entries; `ok=true`, `critical_count=0`, `warning_count=0`, running/running, local open/pending/close/residual `0/0/0/0`, `exchange_truth_mismatches=[]` |

### Acceptance

Production safety and the code fix are cloud-verified. The 9 known orphan maker
orders were removed first, then the V1 passive-order lifecycle replication was
committed in `4b6a6ee` and deployed to `/opt/lightfee-v2`. Remote focused
tests, compileall, manifest verification, service restart, deploy-window
diagnose, warning-level journal scan, production health, and credentialed
all-venue exchange truth are all green.

The deployed window has no new local-flat/exchange-open maker orphan, no open
positions, no pending entries, no pending closes, no residual repairs, and no
Local-L2 hot stale rebuild recurrence. This closes the active CL039 incident.
Keep the new evidence events as watch keys for the next real maker-order
terminality sample; do not widen WS BBO budgets, Local-L2 lifecycle, sizing,
close, or residual-repair policy from this incident.

## Cluster CL-040-v1-ledger-bridge-lifecycle-attribution

### GitNexus Keys

- Fingerprint: `entry-to-exit.lifecycle-ledger-missing + compensation-terminal-unclassified + recovery-flat-ledger-blind-spot`
- Components: `persistence`, `projection-writer`, `lifecycle-ledger`,
  `ledger-attribution`, `offline-analysis`
- Symbols: `SqliteStore`, `ProjectionWriter`,
  `ProjectionWriter.project_records`, `is_projected_kind`, `classify_kind`
- Files: `lightfee/persistence/sqlite_store.py`,
  `lightfee/persistence/projection_writer.py`,
  `lightfee/persistence/projection_contracts.py`,
  `tests/test_offline_analysis.py`, `tests/test_persistence.py`
- Commits: `09292c1`
- Watch: `trade_ledger_events`, `position_ledger`, `order_ledger`,
  `fill_ledger`, `position_pnl_facts`, `exit.compensated`,
  `entry.compensated`, `execution.compensation_failed`,
  `runtime.position_lifecycle_terminal`, `recovery.live_detected`,
  `recovery.flat`
- Cards: [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md),
  [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md)

### Production Evidence

After CL038 and CL039, current production safety was green and all-venue
exchange truth was flat/no-open-orders, but the user-level requirement was
larger than runtime safety: every open-to-close lifecycle, including single-leg
cleanup, compensation, and recovered terminal paths, must be attributable
without broad time-window guesses.

| Track | Evidence | Current conclusion |
|---|---|---|
| CL038 safety | `338caec` deployed; production health green; `runtime.position_lifecycle_terminal=14` in deploy diagnose | runtime terminal evidence exists, but force-close problem branch still needs real production trigger |
| CL039 safety | `4b6a6ee` deployed; all venues nonzero positions `0`, open orders `0`; no maker orphan recurrence | maker lifecycle safety is closed |
| Attribution gap | V2 `projection_writer.py` projected only simple order/entry/exit facts and left `exit.compensated`, `entry.compensated`, `execution.compensation_failed`, and `runtime.position_lifecycle_terminal` unclassified | compensation/terminal events were visible in journal but not queryable as lifecycle ledger rows |
| V1 reference | V1 `runtime_state/ledger_bridge.rs` records `trade_ledger_events`, `position_ledger`, `order_ledger`, `fill_ledger`, and `position_pnl_facts` with `truth_level` | V1 answer is a structured, rebuildable ledger bridge, not another ad hoc event |

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2/V1 semantic drift, fixed locally | V2 had no V1-style lifecycle ledger bridge, so journal evidence could not be joined reliably by position/order/fill ids | add V1-compatible ledger tables and projection bridge |
| Evidence gap, fixed locally | compensation and terminal-problem events were unclassified and absent from structured store | classify/project `entry.compensated`, `exit.compensated`, `execution.compensation_failed`, and `runtime.position_lifecycle_terminal` into ledger bridge |
| Contract mapping drift, fixed locally | `execution.compensation_failed` implementation belonged to the ledger bridge but projection contracts mapped it to `diagnostic_facts` | move it to `PROJECTED_LEDGER_BRIDGE_KINDS` so `fact_table_for_kind()` returns `trade_ledger_events` |
| Recovery attribution gap, fixed locally | `recovery.live_detected` / `recovery.flat` stayed journal-first but had no structured lifecycle row | keep recovery journal-first for replay, but add rebuildable ledger rows for attribution |
| Historical evidence gap, still needs replay | old unallocated Binance/Bybit/Aster rows predate the new ledger bridge | future rows are attributable; historical rows still need exchange order-history joins |

### Fix

- Added V1-compatible, journal-derived SQLite tables:
  `trade_ledger_events`, `position_ledger`, `position_pnl_facts`,
  `order_ledger`, and `fill_ledger`.
- `ProjectionWriter` now writes a lifecycle ledger view while preserving the
  existing projection facts and journal-first recovery semantics.
- `entry.opened` and `recovery.live_detected` open/update `position_ledger`.
- `exit.closed`, `runtime.position_lifecycle_terminal`, and `recovery.flat`
  close/update `position_ledger`; terminal problem fields are preserved and
  are not overwritten by later `recovery.flat`.
- `order.submitted`, `order.filled`, `order.rejected`, and `order.uncertain`
  update `order_ledger`; `order.filled` also writes `fill_ledger`.
- `entry.compensated`, `exit.compensated`, and
  `execution.compensation_failed` now enter `trade_ledger_events` with
  `truth_level=runtime_estimated`.
- `exit.closed` with venue fill evidence and `order.filled` use
  `truth_level=venue_fill_confirmed`; recovery/compensation/terminal rows use
  `runtime_estimated`.
- No live order submit, close executor, recovery policy, Local-L2, WS BBO,
  sizing, or residual-repair behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus analyze` | refreshed to current `706c80a`; `19,780` nodes, `41,661` edges, `300` flows |
| GitNexus context | `ProjectionWriter`, `SqliteStore` | direct imports are offline daily report/projection/tests; no live trading execution process depends on the structured store |
| GitNexus impact | `npx/npm exec gitnexus impact ...` | CLI impact command hung after index refresh; manual blast radius from GitNexus context treated as MEDIUM and kept to persistence/projection |
| local RED | V1 full position/order/fill/exit chain ledger bridge | failed before fix: `position_ledger` missing |
| local RED | recovery + compensation + terminal problem ledger bridge | failed before fix: `trade_ledger_events` missing |
| local RED | ledger backfill over pre-existing legacy fact row | failed before fix: new ledger row wrote but result was counted as `skipped` |
| local RED | `execution.compensation_failed` fact-table contract | failed before fix: kind was not in `PROJECTED_LEDGER_BRIDGE_KINDS` |
| local GREEN focused | new V1 ledger bridge tests plus schema/classification | `6 passed` |
| local adjacent | persistence/offline projection suites | `114 passed` |
| local adjacent | close compensation/replay journal classification suites | `30 passed` |
| local adjacent | offline/persistence/replay plus close compensation subset | `179 passed` |
| local full gate | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q` | `3381 passed`, `9 skipped`, `1 warning` |
| local hygiene | `UV_CACHE_DIR=/private/tmp/uv-cache uv run python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | LOW: `9 files`, `79 symbols`, `0` affected processes. Current working tree still includes pre-existing `AGENTS.md`/`CLAUDE.md` edits that are not part of CL040 and must not be staged with this fix. |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded `4b6a6ee..09292c1`; `.deploy_version=09292c1`; manifest generated and checked | all 14 critical files verified; services already active and not restarted because `ProjectionWriter`/`SqliteStore` are only imported by offline report/analysis code |
| cloud focused | `.venv/bin/python -m pytest -q tests/test_offline_analysis.py tests/test_persistence.py` | `114 passed` |
| cloud hygiene | `.venv/bin/python -m compileall -q lightfee tests scripts` | passed |
| cloud health | `verify_production_services.py --json` | `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/close/residual `0/0/0/0`, `exchange_truth_mismatches=[]` |
| cloud diagnose | `diagnose_live.py --since-deploy --json` | `git_head=09292c1`, `.deploy_version=09292c1`, `version_mismatch=false`, `health.ok=true`, Local-L2 missing/stale/sequence-gap `0/0/0`, entry/open-position events `0/0`. Exchange truth was unavailable in this invocation because credentials were not loaded, so consistency confidence stayed low. |

### Acceptance

Local and cloud V1 ledger bridge replication is green on focused, adjacent,
full local pytest, remote focused, compileall, deploy-manifest, and production
health gates. The fix preserves existing runtime behavior boundaries and closes
the structural attribution blind spot for new journal rows. Historical
unallocated ledger rows remain a separate replay task because they require
exchange order-history joins against old venue ledger rows.

## Cluster CL-041-entry-final-gate-local-l2-skew-parity

### GitNexus Keys

- Fingerprint: `v2.entry-final-gate.local-l2-observed-at-skew-not-blocked + v1-execution-skew-missing`
- Components: `live-runtime`, `entry-final-gate`, `local-l2`,
  `v1-parity`, `production-observability`
- Symbols: `LiveRuntime._dispatch_entry`,
  `LiveRuntime._entry_final_gate_skew_blocker`
- Files: `lightfee/engine/runtime.py`, `tests/test_runtime_entry_flow.py`
- Commits: working tree
- Watch: `runtime.entry_blocked_final_gate`,
  `review.candidate_rejected.rejected_stage=entry_final_gate`,
  `entry_final_gate_max_skew_ms`, `execution_skew`

### Production Evidence

This CL comes from the V1/V2 trading-preflight parity audit, not from a new
current production error. V1 has `entry_final_gate_skew_result`: when both HOT
Local-L2 books are available but their `observed_at_ms` values diverge beyond
`entry_final_gate_max_skew_ms`, V1 blocks entry with `ExecutionSkew`. V2 had the
config value but no dispatch-time equivalent.

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2/V1 semantic drift, fixed locally | V2 checked each Local-L2 book's readiness independently but did not compare the two leg timestamps before dispatch | replicate V1 final gate skew semantics in the live local-L2 dispatch gate |
| Not WS BBO recurrence | This branch is Local-L2 execution book skew, not stale/missing WS BBO quote lease | do not widen WS BBO budget/TTL or subscription capacity for this CL |
| Not Local-L2 data-plane recurrence | The fix does not change snapshot bootstrap, WS lifecycle, sequence continuity, or book ownership | keep Local-L2 stale rebuild as a separate evidence track |

### Fix

- Added `LiveRuntime._entry_final_gate_skew_blocker`.
- The blocker applies only when runtime mode is live, local-L2 is enabled, and
  the active entry readiness provider uses Local-L2.
- After both execution-L2 books pass the existing readiness gate, V2 now compares
  `long_book.observed_at_ms` and `short_book.observed_at_ms`.
- If `abs(long_observed_at_ms - short_observed_at_ms)` exceeds
  `entry_final_gate_max_skew_ms`, dispatch is blocked.
- The block emits `runtime.entry_blocked_final_gate` with pair/symbol/venue,
  observed timestamps, `skew_ms`, and `max_skew_ms`, plus
  `review.candidate_rejected` with `rejected_stage=entry_final_gate`.
- No Local-L2 data-plane, WS BBO quote lease, sizing, order submit, close,
  recovery, or residual-repair behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus status`; `npx gitnexus analyze` | index was stale (`706c80a` vs current `c63ec7d`), then refreshed successfully |
| GitNexus impact | `npx gitnexus impact -r LightFeeV2 _dispatch_entry ...`; `npx gitnexus impact -r LightFeeV2 _entry_quote_lease_execution_check ...`; `npx gitnexus impact -r LightFeeV2 _entry_local_l2_selection_blocker ...` | method-level lookup still returned `UNKNOWN`/not found for runtime.py private methods, so the edit was treated as MEDIUM manual risk |
| GitNexus query/manual blast radius | `npx gitnexus query -r LightFeeV2 "_dispatch_entry runtime entry dispatch caller tests"`; direct `rg "_dispatch_entry\\("` | bounded to `runtime.tick -> _dispatch_entry` and direct dispatch tests in runtime-entry, snapshot-freshness, startup-preflight, and exchange-admission harnesses |
| local RED | `pytest tests/test_runtime_entry_flow.py::TestPlannerDispatchIntegration::test_final_gate_blocks_fresh_bbo_with_excessive_leg_skew -q` before fix | failed as expected: V2 returned `True` and dispatched through 200ms skew over a 100ms budget |
| local GREEN focused | same test after fix | `1 passed` |
| local adjacent | `pytest tests/test_runtime_entry_flow.py -q` | `32 passed` |
| local adjacent | `pytest tests/test_entry_local_l2.py -q` | `138 passed` |
| local adjacent | `pytest tests/test_runtime_snapshot_freshness.py -q` | `20 passed` |
| local adjacent | `pytest tests/live_harness/test_exchange_admission_incidents.py -q` | `12 passed` |
| local adjacent | `pytest tests/test_live_startup_preflight.py -q` | `66 passed` |
| local hygiene | `python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | LOW, `4 files`, `6 symbols`, `0` affected processes. Scope included pre-existing `AGENTS.md`/`CLAUDE.md` edits that are not part of CL041. |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded `c63ec7d..0c8356e`; `.deploy_version=0c8356e`; manifest regenerated and checked | remote focused entry/config/snapshot/startup/admission suite `321 passed`; compileall passed; all 14 critical manifest files verified; services restarted active/running with `NRestarts=0` and `ExecMainStartTimestamp=Wed 2026-06-03 19:25:05 CST` |
| cloud health | `verify_production_services.py --json`; `diagnose_live.py --since-deploy --json` | `health.ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/close counts `0/0/0`, Local-L2 missing/stale/sequence-gap counts `0/0/0`, entry/open-position events `0/0`, `runtime.entry_blocked_final_gate=0`, `runtime.entry_blocked_quote_lease=0`, `version_mismatch=false`. Exchange truth was unavailable in this invocation, so consistency confidence stayed low. |

### Acceptance

V1 final-gate skew replication is deployed and short-window production health is
green for the Local-L2 dispatch path: the regression fails before the fix,
passes after the fix, adjacent entry dispatch/readiness/startup/admission tests
stay green, and the deploy window shows no Local-L2 stale rebuild, sequence-gap,
or entry/open-position events.

## Cluster CL-042-entry-perp-liquidity-qualification-v1-parity

### GitNexus Keys

- Fingerprint: `v2.entry-perp-liquidity-qualification.persisted-only-no-runtime-gate + v1-oi-floor-structural-memory-missing`
- Components: `live-runtime`, `config`, `sidecar-snapshot`,
  `entry-liquidity-qualification`, `v1-parity`, `production-observability`
- Symbols: `StrategyConfig`, `_load_strategy`,
  `LiveRuntime._candidate_snapshot_freshness_decisions`,
  `EntryLiquidityQualificationState`
- Files: `lightfee/config/schema.py`, `lightfee/config/loader.py`,
  `lightfee/engine/entry_liquidity_qualification.py`,
  `lightfee/engine/runtime.py`, `tests/test_config.py`,
  `tests/test_v1_config_defaults_parity_red.py`,
  `tests/test_entry_liquidity_qualification.py`,
  `tests/test_runtime_snapshot_freshness.py`,
  `tests/live_harness/test_snapshot_fallback_degraded_incident.py`
- Commits: working tree
- Watch: `execution.entry_liquidity_blocked`,
  `execution.entry_liquidity_advisory`,
  `runtime.snapshot_freshness_decision.reason=perp_open_interest_below_floor`,
  `runtime.snapshot_freshness_decision.reason=perp_open_interest_structural`,
  `entry_liquidity_qualification_records`

### Production Evidence

This CL is from the V1/V2 trading-preflight parity audit. V1 has an
`EntryLiquidityQualificationState` over venue/symbol liquidity observations:
perp open-interest below floor blocks entry, volume below floor is advisory,
three repeated OI failures become structural ineligibility, structural rows are
suppressed for 30 minutes, and structural probes are rate-limited to 60 seconds.
V2 had `entry_liquidity_qualification_records` only as a persisted field and
sidecar quotes carried `volume_24h_quote`/`open_interest`, but live runtime did
not apply the V1 per-leg OI hard gate or structural memory.

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2/V1 semantic drift, fixed locally | V2 persisted `entry_liquidity_qualification_records` but never advanced or consumed the V1 state machine at entry filtering time | replicate V1 entry perp-liquidity threshold defaults, config aliases, qualification memory, structural suppression, and probe cadence |
| V2 fixture evidence gap, fixed locally | Several snapshot-freshness tests used fresh bid/ask quotes with default `open_interest=0` and `volume_24h_quote=0`, which is not a fresh V1 liquidity fixture | update fixtures to include high OI/volume when the scenario intends liquidity to be fresh |
| Not WS BBO recurrence | The block is based on per-symbol sidecar liquidity evidence, not WS BBO quote lease subscription/budget state | do not widen WS BBO budget/TTL or subscription capacity for this CL |
| Not Local-L2 stale rebuild | No Local-L2 book lifecycle, data-plane rebuild, or WS owner semantics changed | keep Local-L2 stale rebuild as a separate evidence track |

### Fix

- Added V1 entry perp-liquidity threshold defaults to `StrategyConfig`:
  default volume floor `1_000_000`, venue volume overrides Bitget/Bybit
  `2_000_000` and Binance/OKX `5_000_000`, and default open-interest floor
  `1_000_000`.
- Added V1-compatible `entry_volume_floor_quote(venue)` and
  `entry_open_interest_floor_quote(venue)` helpers.
- Updated config loading to accept both map-based V1 fields and legacy
  `entry_min_perp_volume_24h_quote_<venue>` /
  `entry_min_perp_open_interest_quote` fields.
- Added `EntryLiquidityQualificationState` with V1 record/current/probe/persist
  semantics.
- Live runtime now applies the V1 per-leg entry liquidity qualification gate
  only in live mode and when `execution_liquidity_enabled` is true.
- Runtime records qualification state only inside the actual candidate filter;
  diagnostic/scope calls remain read-only so observability cannot increment
  failure counters.
- Open-interest below floor blocks with
  `execution.entry_liquidity_blocked` and
  `reason=perp_open_interest_below_floor`; after the V1 threshold it becomes
  `reason=perp_open_interest_structural`.
- Volume below floor emits `execution.entry_liquidity_advisory` and continues,
  matching V1.
- No WS BBO quote lease, Local-L2 lifecycle/data-plane, order submit, sizing,
  close, recovery, or residual-repair behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus impact | `npx gitnexus impact -r LightFeeV2 StrategyConfig --direction upstream --depth 3 --include-tests` | CRITICAL: broad config import surface; direct test/app imports, no process group hit |
| GitNexus impact | `npx gitnexus impact -r LightFeeV2 _load_strategy --direction upstream --depth 3 --include-tests` | CRITICAL: affects live/report/sidecar/probe/evolution/scheduler/replay config entry points |
| GitNexus impact | `npx gitnexus impact -r LightFeeV2 _candidate_snapshot_freshness_decisions --direction upstream --depth 3 --include-tests` | method-level lookup returned not found/UNKNOWN, so runtime blast radius was manually limited to snapshot filtering and entry tests |
| local RED | config default thresholds | failed before fix with missing `entry_volume_floor_default_quote` |
| local RED | map/legacy config loading | failed before fix with missing `entry_volume_floor_quote` and ignored legacy/global fields |
| local RED | V1 qualification state module | failed before fix with missing module |
| local RED | fresh quote but OI below V1 floor | failed before fix because V2 returned the candidate instead of blocking |
| local RED | structural suppression probe interval | failed before fix because persisted structural rows were ignored |
| local GREEN focused | new config/default/state/runtime tests | `9 passed` across the targeted RED set |
| local adjacent | `tests/test_runtime_snapshot_freshness.py -q` | `22 passed` |
| local adjacent | `tests/live_harness/test_snapshot_fallback_degraded_incident.py -q` | `3 passed` |
| local adjacent | `tests/test_runtime_entry_flow.py -q` | `32 passed` |
| local adjacent | `tests/test_entry_local_l2.py -q` | `138 passed` |
| local adjacent | `tests/test_live_startup_preflight.py -q` | `66 passed` |
| local adjacent | `tests/live_harness/test_exchange_admission_incidents.py -q` | `12 passed` |
| local adjacent | config/defaults/state/snapshot/harness aggregate | `73 passed` |
| local adjacent | sidecar schema/source/lifecycle/candidate identity | `42 passed` |
| local adjacent | venue market data and transport | `415 passed` |
| local full gate | `.venv/bin/python -m pytest -q` | `3390 passed`, `9 skipped`, `1 warning` |
| local hygiene | `python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | HIGH: `13 files`, `89 symbols`, `7` affected `Main -> Default_strategy` flows via `_load_strategy`; scope also included pre-existing `AGENTS.md`/`CLAUDE.md` edits outside this CL |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded `c63ec7d..0c8356e`; `.deploy_version=0c8356e`; manifest regenerated and checked | remote focused entry/config/snapshot/startup/admission suite `321 passed`; compileall passed; all 14 critical manifest files verified; services restarted active/running with `NRestarts=0` and `ExecMainStartTimestamp=Wed 2026-06-03 19:25:05 CST` |
| cloud health | `verify_production_services.py --json`; `diagnose_live.py --since-deploy --json` | `health.ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/close counts `0/0/0`, Local-L2 missing/stale/sequence-gap counts `0/0/0`, `execution.entry_liquidity_blocked=300` since deploy, entry/open-position events `0/0`, `runtime.entry_blocked_quote_lease=0`, `version_mismatch=false`. Exchange truth was unavailable in this invocation, so consistency confidence stayed low. |

### Acceptance

V1 entry liquidity qualification replication is deployed and short-window
production health is green for configured thresholds, legacy config
compatibility, OI hard blocking, volume advisory, structural memory, and
structural probe cadence. The deploy window shows the new liquidity block
branch active (`execution.entry_liquidity_blocked=300`) without opening entries
or positions.

## Cluster CL-043-pending-entry-maker-progress-open-order-truth

### GitNexus Keys

- Fingerprint: `post-bd12acd.pending-entry.live-position-progress-with-uncertain-maker-order + bybit-open-maker-orders-after-local-flat + okx-synthetic-recovery-ordid-51000`
- Components: `live-runtime`, `pending-entry-reconciliation`,
  `venue-transport`, `venue-bybit`, `venue-okx`, `production-observability`,
  `v1-parity`, `exchange-docs`
- Symbols: `LiveRuntime`, `OrderReconciler.reconcile_position`,
  `VenueTransport._fetch_order_status_okx`
- Files: `lightfee/engine/runtime.py`, `lightfee/engine/reconciliation.py`,
  `lightfee/venues/transport.py`, `tests/test_venues_transport.py`
- Commits: `bd12acd`, working tree follow-up
- Watch: `pending_entry.maker_progress_applied`,
  `pending_entry.finalize_deferred_incomplete_fill`,
  `pending_entry.finalize_fill_reconciliation_error`,
  `entry.cleanup_leg_exposure`,
  `recovery.live_mismatch_flattened`,
  `order.reconcile_result.status=stale_full_live_nonzero`,
  Bybit open maker orders, OKX `51000 Parameter ordId error`
- Cards: [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md)

### Production Evidence

Read-only production review after cloud deploy
`bd12acd883ab85fb9e0e41d2dbe69677835f75a7` found the services active and the
final all-venue exchange truth flat/no-open-orders, but the deploy window
contained one pending-entry maker-order recurrence and one OKX reconciliation
parameter variant.

| Track | Evidence | Current conclusion |
|---|---|---|
| Current service state | `verify_production_services.py --json`: `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/close/residual `0/0/0/0` | service is healthy now |
| Final exchange truth | credentialed all-venue truth across `aster`, `binance`, `bitget`, `bybit`, `gate`, `hyperliquid`, `okx`: nonzero positions `0`, open orders `0` | no current open positions or open orders |
| Old Local-L2/WS tracks | `runtime.local_l2_hot_stale_rebuild=0`, `runtime.local_l2_sequence_gap=0`, `entry_ws_bbo_quote_lease_waiting_for_subscription=0`, `live_tick_stale=0` | old Local-L2 and WS subscription shapes did not recur |
| Real entry/close | `entry.opened=1`, `runtime.position_opened=1`, `runtime.position_lifecycle_terminal=1` on `MEMEUSDT`; terminal flat via `pending_passive_close_flat_probe`, `recovery.flat`, `runtime.position_drift_corrected` | the normal open-to-terminal path completed |
| Persistent old-bug variant | `MEUSDT` had two Bybit non-reduce-only PostOnly maker orders still open after local pending state was absent: `668be726-46b4-4c68-a1ae-4257c10c6661` / `e0da5db734dba297d0b8904aaa39a65fd7a0`, and `f5d5cb2b-d069-4339-944f-16d3043f7832` / `8251e0d8f8e5324b3425bde57e44f241726e` | CL039 closed the passive-order identity surface, but this is a second maker-terminality layer |
| Runtime chain | Bybit execution reconciliation returned `execution_not_found`, while local reconciliation saw `live_position_delta.quantity=608` and applied `pending_entry.maker_progress_applied`; finalization later deferred with maker/hedge fill asymmetry and missing prices | V2 treated live-position quantity as maker fill progress even though the maker order was not proven terminal |
| Self-cleanup | Later events emitted `entry.cleanup_leg_exposure`, `order.reconcile_result.reason=duplicate_client_id`, `status=stale_full_live_nonzero`, and `recovery.live_mismatch_flattened`; final truth was flat/no-open-orders | production self-healed, but only after a transient orphan-maker/open-order window |
| New venue variant | OKX finalize probe failed with HTTP 400 `{"code":"51000","data":[],"msg":"Parameter ordId error"}` for synthetic order id `entry-1780487640389-MEUSDT-recovery-short` and client id `11bc04759b00cd7704a45b6accfb75dc` | same family as CL035 Binance placeholder order id, but on OKX `ordId` vs `clOrdId` semantics |

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| 顽固 bug, fixed locally | pending-entry maker terminality still had a second false-progress path: local runtime applied live-position snapshot quantity to maker progress while the maker order status was `uncertain` / execution-not-found and the maker order could still be open | gate maker position-snapshot progress behind explicit filled/terminal reconciliation and add skip evidence when live position exists but order terminality is not proven |
| V2/V1 semantic drift, fixed locally | V1 pending-entry semantics carry passive order identity and do not equate live-position existence with passive-maker terminality; open-order truth remains part of the pending-entry lifecycle | replicate the V1 rule that passive maker progress requires order/fill terminal evidence, while live-position truth can trigger cleanup/recovery evidence but not silently finalize maker fill |
| Exchange-doc root required | Bybit `execution/list` returned not found while open-order truth later showed the PostOnly orders still open; the exact Bybit execution-history/open-order-cache boundary needs official-doc/probe confirmation | use Bybit execution and open-order docs/probes to distinguish no-fill, open-resting, and recently-filled-but-not-yet-indexed cases |
| Exchange-doc root required, fixed locally | OKX order-status reconciliation sent a synthetic recovery id as `ordId`, causing `51000 Parameter ordId error` | follow OKX order query semantics: numeric exchange order ids go to `ordId`; synthetic/client ids go to `clOrdId` when client id truth exists |
| Evidence needs strengthening | The deploy window proves the open-order transient and later self-cleanup, but the exact Bybit maker fill/open-order chronology still needs order-history evidence around the two order ids/client ids | add targeted regression tests now; collect credentialed Bybit order/execution history in the next production verification window |
| Not old Local-L2/WS recurrence | Local-L2 stale rebuild, sequence gap, WS subscription waiting, quote-lease final gate, and live tick stale counts were zero in the inspected window | do not widen Local-L2/WS budgets or touch those data planes for this CL |

### Fix

- `VenueTransport._fetch_order_status_okx()` now sends OKX `ordId` only when
  the supplied order id is numeric. If recovery supplies a synthetic id and a
  client order id exists, it queries `/api/v5/trade/order` with `clOrdId`.
  If only a nonnumeric id exists, it is treated as `clOrdId` rather than a
  malformed exchange `ordId`.
- `LiveRuntime._reconcile_pending_state()` no longer applies live-position
  snapshot quantity/price/order-id progress to the passive maker leg unless
  reconciliation has classified that maker leg as `filled`.
- When a nonzero live position is visible but maker order terminality is not
  confirmed, runtime emits `pending_entry.live_position_progress_deferred` with
  entry, symbol, maker leg, venue, status, live quantity/price, current maker
  fill state, and `reason=order_terminality_not_confirmed`.
- Hedge live-position hydration remains unchanged so the PRL-style V1 recovery
  path, where maker terminality is already proven and live hedge truth completes
  a balanced pending entry, still finalizes.
- No production order submit/cancel, deploy, Local-L2, WS BBO, entry sizing,
  close, or residual-repair behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus analyze --index-only --name LightFeeV2 --drop-embeddings .`; `npx gitnexus status` | refreshed to current `bd12acd`, up to date |
| GitNexus impact | method-level `impact` for `VenueTransport._fetch_order_status_okx`, `LiveRuntime._reconcile_pending_state`, and `OrderReconciler.reconcile_position` | private/method lookup returned `UNKNOWN`/not found; risk manually treated as MEDIUM for runtime and LOW-to-MEDIUM for OKX transport |
| GitNexus query/manual blast radius | GitNexus query plus direct `rg` callers | OKX path bounded to `fetch_order_status`; runtime path bounded to `_reconcile_pending_state` from `tick` plus pending-entry tests/live harnesses |
| production read-only | service state, current runtime state, `verify_production_services.py --json`, `diagnose_live.py --since-deploy --json`, credentialed all-venue truth | current flat/no-open-orders; deploy-window CL039 variant plus OKX parameter variant documented |
| local RED | OKX synthetic recovery order id with client id | failed before fix with OKX `51000 Parameter ordId error` because V2 sent synthetic `entry-...-recovery-short` as `ordId` |
| local RED | uncertain maker order plus nonzero live position | failed before fix because `pending.maker_leg_filled` became `608.0` and `pending_entry.maker_progress_applied` was emitted |
| local GREEN focused | new OKX and maker-terminality regressions | `2 passed` |
| local adjacent | `tests/test_pending_entry_v1_semantic_drift.py` | `13 passed` |
| local adjacent | `tests/live_harness/test_pending_entry_v1_semantic_drift_incident.py` | `3 passed` |
| local adjacent | `tests/test_runtime_entry_flow.py` plus `TestRealPathAbortCleanupDeadline` | `65 passed` |
| local adjacent | `TestVenueSpecificOrderReconciliationEvidence` | `5 passed` |
| local adjacent | `tests/test_recovery_reconciliation.py` plus V1 client-order-id parity | `45 passed` |
| local impacted files | `tests/test_venues_transport.py`; `tests/test_live_entry_hedge_root_fix.py` | `382 passed`; `134 passed` |
| local full gate | `.venv/bin/python -m pytest -q` | `3392 passed`, `9 skipped`, `1 warning` |
| local hygiene | `.venv/bin/python -m compileall -q lightfee tests scripts`; `git diff --check` | passed |
| GitNexus detect changes | `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | HIGH: `8 files`, `24 symbols`, `6` affected `_fetch_order_status_okx` transport/rate-limit/metadata flows. Scope includes pre-existing `AGENTS.md`/`CLAUDE.md` edits outside this CL. Runtime private-method flow was not resolved by detect-changes but is covered by focused/adjacent tests. |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded `bd12acd..9d580d5`; `.deploy_version=9d580d5`; manifest generated and checked | manifest `370` files; all 14 critical files verified; remote compileall passed; focused pending-entry/venue/runtime suite `86 passed`; `lightfee-sidecar.service` and `lightfee-live.service` restarted active/running with `NRestarts=0`, `ExecMainStartTimestamp=Wed 2026-06-03 20:38:30 CST` |
| cloud health | `verify_production_services.py --json`; `check_process_singleton.py --strict`; warning journal since restart | health `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, local open/pending/close/residual `0/0/0/0`, `exchange_truth_mismatches=[]`; singleton PASS with one sidecar, one live, zero snapshot writers; warning log `-- No entries --` |
| cloud diagnose | service-env `diagnose_live.py --json --since-deploy --venues binance,bybit,aster,okx --max-events 200000` | `git_head=9d580d5`, `.deploy_version=9d580d5`, `version_mismatch=false`, `health.ok=true`, Local-L2 missing/stale/sequence-gap `0/0/0`, entry/open-position events `0/0`, local open/pending/close `0/0/0`. Diagnose exchange-truth was unavailable because no target symbols were present, so a separate all-account truth probe was run. |
| cloud exchange truth | credentialed all-account read-only adapter probe | all 7 venues (`aster`, `binance`, `bitget`, `bybit`, `gate`, `hyperliquid`, `okx`) had nonzero positions `0`; all 7 venues had open orders `0` using venue-specific all-open-order endpoints |

### Acceptance

Local root fix and short-window cloud deploy verification are green. Current
production is flat and healthy: no local open/pending/close/residual work, all
7 venues flat by credentialed position truth, and all 7 venues have zero open
orders by credentialed open-order truth. The post-deploy window has no Bybit
orphan maker recurrence and no OKX synthetic `ordId` `51000` recurrence. The
remaining evidence task is historical, not an active safety issue: collect
credentialed Bybit order/execution-history around the two pre-fix MEUSDT order
ids/client ids to document the exact open-order vs execution-history chronology.

## Cluster CL-044-recovery-catalog-and-hyperliquid-meta-ctx-root-fix

### GitNexus Keys

- Fingerprint: `post-9d580d5.recovery-probe-catalog-unavailable-bybit-gate + bybit-bulk-timeout-static-fallback-skipped + hyperliquid-zero-sidecar-quote`
- Components: `venue-bybit`, `venue-gate`, `venue-market-data`,
  `startup-recovery`, `sidecar-market-data`, `production-observability`,
  `exchange-docs`
- Symbols: `BybitAdapter.ensure_supported_symbols_loaded`,
  `GateAdapter.ensure_supported_symbols_loaded`,
  `MarketDataClient._fetch_hyperliquid_style`,
  `LiveRuntime._fetch_startup_live_position_snapshots`
- Files: `lightfee/venues/bybit.py`, `lightfee/venues/gate.py`,
  `lightfee/venues/market_data.py`, `tests/test_venues_transport.py`,
  `tests/venues/test_market_data_client.py`
- Commits: `9d580d5`, working tree follow-up
- Watch: `recovery.live_position_probe_catalog_unavailable`,
  `recovery.live_position_bulk_probe_error`,
  `recovery.live_position_static_config_probe_skipped`,
  `runtime.snapshot_freshness_decision reason=invalid_quote`

### Production Evidence

Post-`9d580d5` deploy-window review found the service healthy, flat, and with
all-account truth clean across 7 venues, but three real evidence tracks
remained:

| Track | Evidence | Current conclusion |
|---|---|---|
| Bybit/Gate catalog unavailable | `recovery.live_position_probe_catalog_unavailable=62`, mainly Bybit/Gate `supported_symbols_empty`, `decision=probe_unfiltered` | not a direct safety failure, but a real catalog gap that degrades recovery filtering |
| Bybit bulk probe timeout | `recovery.live_position_bulk_probe_error=1` from Bybit `/v5/position/list` `TimeoutError`; same window had `recovery.live_position_static_config_probe_skipped=1` because static universe `620` exceeded the CL036 fallback limit | CL036 protection worked, but Bybit had no catalog-backed smaller fallback set |
| Invalid sidecar quote | `runtime.snapshot_freshness_decision reason=invalid_quote`, current rescan `99`; by venue: Hyperliquid `96`, Aster `3`; Hyperliquid samples had `bid=0`, `ask=0`, sizes `0`, mark/index `0` | Hyperliquid parser produced zero quotes from valid `metaAndAssetCtxs` response shape; Aster samples were one-sided top-book and remained correctly fail-closed |

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2 semantic/protocol drift, fixed locally | Hyperliquid `metaAndAssetCtxs` official response returns `assetCtxs` parallel to `universe`; V2 only indexed ctx rows by optional `coin`, so official rows without `coin` became empty ctx and emitted bid/ask `0` | match ctx by universe index first, retain `coin` compatibility, skip delisted assets, and do not emit zero-price tickers when usable ctx/price is absent |
| Exchange catalog gap, fixed locally | Bybit adapter had no `supported_symbols()` / `ensure_supported_symbols_loaded()`, so recovery could not prefilter Bybit symbols and logged `probe_unfiltered` | load Bybit `/v5/market/instruments-info` with `category=linear`, `limit=1000`, cursor pagination, and keep active USDT perpetuals only |
| Exchange catalog gap, fixed locally | Gate adapter had no supported-symbol catalog, so recovery could not filter Gate futures symbols either | load Gate `/api/v4/futures/usdt/contracts`, keep active USDT contracts, and expose canonical LightFee symbols |
| Needs continued observation | One Bybit bulk position timeout | still can happen as exchange/network transport noise; after catalog fix, the next fallback path has a venue-supported symbol set instead of empty/unfiltered catalog evidence |
| Not an open-position safety issue | Current all-account truth had 7 venues flat and no open orders | no live position or open-order leak was found for this cluster |

### Fix

- `BybitAdapter` now exposes a recovery catalog and loads the full paginated
  Bybit linear instruments list, filtering to `Trading` USDT perpetuals.
- `GateAdapter` now exposes a recovery catalog from Gate USDT futures
  contracts, filtering active/non-delisting contracts and returning canonical
  symbols such as `BTCUSDT`.
- `MarketDataClient._fetch_hyperliquid_style()` now maps `assetCtxs` by
  official universe index, while keeping the previous `coin` lookup for wrapped
  responses. It skips delisted rows and refuses to publish bid/ask `0` quotes.
- No order submit/cancel, position parsing, CL036 static fallback limit,
  lifecycle, close, residual repair, Local-L2, or WS BBO behavior changed.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus analyze --index-only --name LightFeeV2 --drop-embeddings .`; `npx gitnexus status` | refreshed to current `15eef34`, up to date |
| GitNexus impact | `BybitAdapter` upstream | MEDIUM: registry, diagnose, smoke/capability/transport tests; no process hit |
| GitNexus impact | `GateAdapter` upstream | MEDIUM: registry, probe/live app imports and transport/capability tests; no process hit |
| GitNexus impact | `_fetch_hyperliquid_style` upstream | LOW: `fetch_funding_tickers` and `fetch_perp_liquidity` sidecar market-data path |
| local RED | Hyperliquid official parallel `assetCtxs` shape | failed before fix with bid/ask `0` |
| local RED | Hyperliquid missing ctx | failed before fix by emitting `hyperliquid:MERLUSDT` with zero quote |
| local RED | Bybit/Gate adapter catalogs | failed before fix because the adapters had no catalog loader |
| local GREEN focused | new Hyperliquid, Bybit, Gate regressions | `4 passed` |
| local adjacent | venue market-data plus catalog tests | `41 passed` |
| local adjacent | startup recovery preflight | `66 passed` |
| local adjacent | sidecar source/service quote paths | `24 passed` |
| local hygiene | `compileall` for changed venue modules | passed |

### Acceptance

Local root fixes are green. This cluster turns the appeared events into
actionable business fixes instead of log suppression: Hyperliquid invalid
quotes are corrected at parser semantics; Bybit/Gate recovery probes now have
real exchange catalogs; the single Bybit bulk timeout remains a transport event
to observe, but no longer leaves Bybit without a catalog-backed fallback path.
Cloud deploy and post-deploy recurrence scan are still pending for this
working-tree follow-up.

## Cluster CL-045-hyperliquid-passive-cancel-envelope-drift

### GitNexus Keys

- Fingerprint: `post-8fd0ef6.merlusdt-hyperliquid-maker-cancel-422 + pending-entry-retained + risk_only-fail_closed`
- Components: `venue-hyperliquid`, `venue-transport`,
  `pending-entry-reconciliation`, `passive-maintenance`,
  `production-observability`, `v1-parity`
- Symbols: `VenueTransport.cancel_passive_order`,
  `build_hyperliquid_exchange_payload`,
  `LiveRuntime._recover_cancel_maker_order`
- Files: `lightfee/venues/transport.py`, `tests/test_venues_transport.py`,
  `docs/bugs/daily/2026-06-03.md`
- Commits: `8fd0ef6`, working tree follow-up
- Watch: `passive_maintenance.cancel_error`,
  `pending_entry.maker_terminal_evidence_unavailable`,
  `reconciliation.entry_abandon_retained_unresolved_maker`,
  `entry.abort_maker_cancel_failed`, `risk_mode_fail_closed`

### Production Evidence

Post-CL044 deploy opened a new MERLUSDT passive incremental entry:
Hyperliquid long maker, Bybit short hedge. The maker order was accepted with
exchange order id `455070590535` and no hedge was submitted. Reconciliation
then repeatedly saw both venue positions at zero, but could not prove maker
terminality. Hyperliquid cancel attempts failed with
`HTTP 422: Failed to deserialize the JSON body into the target type`, causing
`pending_entry_count=1`, `lifecycle=risk_only`, and `risk_mode=fail_closed`.

Bybit scoped truth for `MERLUSDT` was clean: position probe succeeded, open
order probe succeeded, and Bybit had no position/open order. Hyperliquid
credential truth remained incomplete in `diagnose_live` because the diagnostic
credential mapping did not consume the production `LIGHTFEE_HYPERLIQUID_*`
environment names, but live reconciliation itself observed Hyperliquid position
quantity `0.0` and `raw_exchange_status=unknownoid`.

### Root Cause Split

| Category | Issue | Decision |
|---|---|---|
| V2/V1 semantic drift, fixed locally | Hyperliquid cancel used an unsigned raw body `{"type":"cancel","cancel":{"coin":...,"oid":...}}` | V1 uses signed `/exchange` envelope with action `{"type":"cancel","cancels":[{"a": asset_index, "o": oid}]}`; replicate that shape |
| Exchange-doc/V1 root | Hyperliquid `/exchange` expects signed action payloads for order management | Use the same `build_hyperliquid_exchange_payload()` path as passive order submit |
| Evidence gap | `diagnose_live` does not map production Hyperliquid env names into exchange truth | Separate diagnostic hardening; production event chain and V1 code comparison are sufficient for cancel payload root |
| Safety status | No local open position; Bybit side flat/no-open-order; Hyperliquid maker cancel failed and pending was retained | do not manually cancel without operator instruction; fix cancel path and redeploy |

### Fix

- Hyperliquid branch of `VenueTransport.cancel_passive_order()` now requires a
  numeric exchange order id, loads cached asset metadata, builds V1 cancel
  action `{"type":"cancel","cancels":[{"a": asset_index, "o": oid}]}`, signs it
  through `build_hyperliquid_exchange_payload()`, and POSTs that envelope to
  `/exchange`.
- Cancel response handling now follows V1 semantics: `status=ok` is accepted
  unless an entry in returned statuses carries an error.
- Other venue cancel paths are unchanged.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus index | `npx gitnexus analyze --index-only --name LightFeeV2 --drop-embeddings .` | refreshed to current `8fd0ef6`, up to date before edit |
| GitNexus impact | `VenueTransport.cancel_passive_order` upstream | MEDIUM: direct cancel tests; no process hit resolved by GitNexus |
| local RED | Hyperliquid cancel passive order expected signed V1 cancel action | failed before fix: sent raw `{"type":"cancel","cancel":...}` body |
| local GREEN focused | Hyperliquid signed cancel regression | `1 passed` |
| local adjacent | cancel absent/order endpoint tests | `20 passed` |
| local adjacent | pending-entry hard-ceiling cancel lifecycle tests | `3 passed` |

### Acceptance

Local root fix is green. Cloud deploy and post-deploy verification are pending
for this working-tree follow-up.
