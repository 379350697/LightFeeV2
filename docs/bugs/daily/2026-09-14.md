# Bug Ledger Daily - 2026-09-14

## CL-148 — Bitget Classic hedge close wire side was rejected by history evidence

Status: **closed** (production-verified).

### Symptom

Since the `faadf563` deployment (2026-09-05), every Bitget-leg funding close that
fell back to taker flattening ended as an irrecoverable `no_candidate` audit debt.
At inspection time (2026-09-14) thirteen `pending_close_reconciliation` owners were
stuck, the production health gate failed with `pending_close_owner_present`, and the
eleven blocking rows suppressed conflicting new risk on ONGUSDT/CYSUSDT/LAUSDT/TUSDT.

Observed input/state: `entry-1789066343561-ONGUSDT` closed its Bitget long leg on
2026-09-10 19:00 UTC; the flat probe confirmed both venues at zero, and the billing
debt terminalized `automatic_history_no_candidate` within 33 seconds.

Observed output/state transition: `reconciliation.automatic_historical_evidence_blocked
classification=no_candidate` → `exit.billing_evidence_debt_irrecoverable` (operator
import required), with no further automatic attempts for days.

### Root cause

When a Bitget close is submitted in Classic hedge mode, the order builder inverts
the business close side on the wire (`_build_bitget_order_request`: `buy` +
`tradeSide=close` + `posSide=long` closes a long; `sell` + `close` + `posSide=short`
closes a short). Exchange order history therefore reports the open-side direction
for our close fills. `find_bitget_historical_close_order_candidates` compared that
raw wire side against the business close side (`close_order_side_for_position`),
so the real closing row was filtered out of unique-history discovery even when the
exchange held a complete, filled, fee-bearing row; discovery returned `no_candidate`
and the debt terminalized. The 2026-09-05 repair (CL-147) wired the discovery and
accepted empty pages but preserved this side predicate, and its closure evidence
treated the resulting `no_candidate` as the correct terminal outcome.

Exchange-truth evidence gathered read-only against `/api/v2/mix/order/orders-history`
and `/api/v2/mix/order/detail` for the traced position:

- close order `1482019161004191745` (`lfexf948f037b1454a27`): `side=buy`,
  `tradeSide=close`, `posSide=long`, `status=filled`, `baseVolume=303`,
  `priceAvg=0.07912` — a unique complete candidate that the filter rejected;
- the stored maker identity `1482019066493939713` is a genuine zero-fill canceled
  order (detail now `40109`), i.e. the debt's known-leg identity was correct but
  pointed at the wrong (unfilled) attempt.

### Contract

A Bitget history row closes a position side when venue semantics prove ownership
(`posSide` matching the position plus a close marker, or one-way `reduceOnly`) and
its wire side follows either documented hedge convention: the Classic builder's
repeated open side, or the UTA/one-way opposite business side. Order-status
reconciliation for a proven close row reports the business close side so billing
evidence and `CloseRuntime` completeness checks compare like for like. Open and
one-way rows are unaffected.

### Counterexample matrix

- Classic hedge close long (wire `buy`+`close`) and close short (wire `sell`+`close`)
  are candidates for their position sides (RED on the pre-fix tree).
- A hedge opening row (`tradeSide=open`) and a posSide-mismatched close row are
  rejected.
- A marker-less UTA hedge opening row (posSide=long, no `tradeSide`, open-side
  wire direction) must not borrow the Classic repeated-open-side convention;
  only explicitly close-marked hedge rows may (review follow-up `3be8d703`,
  RED on `5baee72`).
- A one-way row must carry the business close side; a `buy` reduce-only row is a
  SHORT-close candidate only.
- The parser normalizes only proven close rows: Classic marker or UTA
  opposite-side rows flip to the business side; `tradeSide=open` and one-way rows
  keep the wire side.
- Future legitimate misses record `bitget_history_no_candidate`; the one-shot
  reactivation covers only the legacy `no_candidate` reason with a Bitget or Aster
  leg, so a repaired miss can never reopen.

### Repair (`5baee72`, review follow-up `3be8d703`)

- `find_bitget_historical_close_order_candidates` accepts both hedge close wire
  conventions for `posSide` rows via `_bitget_close_wire_side_matches`; the
  repeated-open-side convention is restricted to explicitly close-marked rows
  (`3be8d703`) so marker-less UTA opening rows cannot become false candidates;
  ownership and quantity/time/status filters unchanged.
- `_parse_order_status_bitget` reports the business close side for `posSide` rows
  proven close-owned; all other rows keep the wire side.
- Discovery's empty-result classification is renamed `bitget_history_no_candidate`.
- `_reactivate_history_discovery_upgrade` extends the one-shot legacy
  `no_candidate` reactivation to debts with a Bitget leg (the Aster branch is
  unchanged and shares the legacy reason constant).
- Passive one-sided/excess IOC retries mint per-attempt client order ids
  (`exit_live_one_sided_*`/`exit_live_excess_*`): a truth-gap retry previously
  resent the accepted order's deterministic clientOid and Bitget rejected it with
  `40786 Duplicate clientOid` (traced on `entry-1789105961703-ONGUSDT`,
  2026-09-11 06:20 UTC). This is recorded separately as CL-149.

### Verification

- RED on the pre-fix tree: six of seven new regressions fail (the seventh is the
  no-reopen guard, which passes on both trees by design).
- Full suite: `4,595 passed`, and the 25 failures are byte-identical to the
  pre-fix baseline (pre-existing snapshot-freshness/pending-entry families, out of
  scope here).
- GitNexus: impact analyzed before editing (`_parse_order_status_bitget` CRITICAL,
  `_flatten_live_one_sided_position` HIGH — both reported before the edit);
  `detect_changes` scope matched the intended files.
- Production: `5baee72` (+ manifest sync `07de958a`) deployed 2026-09-14 18:43 CST
  in a verified complete-flat/no-order window; both services restarted as
  singletons. Eight seconds later the journal recorded seven
  `automatic_historical_evidence_reactivated` under
  `bitget_classic_hedge_close_side_convention` and seven `exit.reconciled` in the
  same second, including both traced incidents. Owners fell 13 → 6. Seven-venue
  exchange truth remained high-confidence flat/no-order and state-consistent; no
  order was submitted or cancelled by verification.

### Remaining debt (outside this contract)

- Two `partial` debts (`entry-1788537122325`, `entry-1789066343561`) are
  operator-import by design (`unsupported_partial_evidence_debt`).
- `entry-1788403990217-CYSUSDT` is terminal `aster_v3_no_candidate`; its one
  repaired Aster re-scan already ran and found no row.
- `entry-1788537122325` and `entry-1788583981589` (Binance/Bybit legs, legacy
  `no_candidate`) have no Bitget/Aster leg and were not reactivated; their
  Binance-side discovery miss needs its own investigation.
- `entry-1789091700691-ONGUSDT` (Bybit long + Bitget short) remains an active
  retrier on backoff; the same repaired path is expected to clear it once a
  two-leg flat-truth probe succeeds.

### Host operations (same session)

- `lightfee-trade-optimization-report.timer` disabled on the host: its unit
  references `/opt/lightfee-v2/scripts/run_trade_optimization_report.sh`, which
  has never existed in the repository (last log 2026-08-03). The stale failed
  unit state was reset.

## CL-150 — Binance historical close discovery anchored on placement time instead of execution time

### Symptom

Two of the six remaining close-reconciliation debts after CL-148 are Binance-leg
`final` debts terminalized as `irrecoverable_audit_debt` / `no_candidate`:

- `entry-1788583981589-ONGUSDT`: Binance long leg close order `3277215179`
  (`lfexa38f361f087bc2c6`) is `FILLED` 247/247, `reduceOnly`, hedge
  `positionSide=LONG`, placed 2026-09-07, filled 2026-09-09 — 67 hours later
  (passive GTX). The flat probe confirmed closure 1.4 s after the fill.
- `entry-1788537122325-ONGUSDT`: Binance long leg close split across the GTX
  order `3274922181` (partially filled 203, then `EXPIRED`) plus residual-repair
  market order `3274923320` (FILLED 49); 203 + 49 = 252 owned.

Exchange truth (read-only probes, this session): both closes are complete
on-exchange with full fee evidence available.

### Root cause

`BinanceAdapter.discover_historical_close_fill_reconciliation` queried
`/fapi/v1/allOrders` with `startTime`/`endTime` anchored on `closed_at_ms`, but
that endpoint filters by order **placement** time while the candidate filter
anchored on `updateTime` (execution) versus `closed_at_ms`. The query window
therefore excluded exactly the rows the filter was designed to admit: a passive
close resting more than `time_window_ms` (300 s) before the flat confirmation is
invisible to the query. The automatic-history pass then classified
`no_candidate` and `_mark_automatic_history_evidence_irrecoverable` converted it
into a false terminal debt. A second face: `_fetch_close_leg_reconciliations`
voids the whole leg when any stored identity is absent on-exchange (the phantom
`lfex28f78da6a58d9101` leg, HTTP -2013 → `None` by V1 parity), which forces the
known-identity path into the same broken discovery.

The Aster V3 repair (CL-147/148 family) anchors on execution time via
`userTrades`; Binance was the family outlier. Existing tests missed it because
every fixture placed the order's creation time inside the window, equating the
creation and execution anchors.

### Contract

Binance historical close discovery must locate close executions by when they
**executed** relative to `closed_at_ms` (family standard: Aster V3 `userTrades`
execution window), group them by order, require one group carrying the full
owned close quantity, then re-check the order row's close-ownership contract
(hedge `positionSide`; one-way `BOTH` requires `reduceOnly`), and finally reuse
the adapter's exact order/execution reconciliation for fees and provenance. A
repaired-path miss must terminalize under a distinct reason that never re-opens.

### Counterexample matrix

- Passive close placed 67 h before `closed_at_ms`, executed 1.4 s before:
  resolves with exact recheck (production row shape of `3277215179`).
- Order created and executed inside the window (COTI shape): still resolves.
- Split close 203 + 49 ≠ 252 in one order: `binance_user_trades_no_candidate`
  distinct terminal, one scan, no loop.
- Two full-quantity groups: `ambiguous_candidates`, blocked.
- One-way `BOTH` trade rows: candidate selection only; the order row decides
  reduce-only ownership (fail-closed at recheck).
- Out-of-window execution / wrong side / wrong symbol / opposite hedge leg:
  excluded from grouping.
- ≥1000 executions in window: `history_incomplete`.
- Exact recheck identity mismatch / unavailable / incomplete fees: existing
  terminal classifications preserved.
- Legacy generic `no_candidate` with a Binance leg: exactly one repair re-scan
  (`binance_execution_window_anchor_repair`); current-reason debts never reopen.

### Repair

- `lightfee/venues/binance.py`: discovery rewritten onto `/fapi/v1/userTrades`
  (execution-time window) with the new
  `find_binance_historical_close_execution_candidates` grouper; per-candidate
  `/fapi/v1/order` row contract; exact fee recheck via the existing
  `fetch_order_fill_reconciliation`; miss classification
  `binance_user_trades_no_candidate`. The operator allOrders-export tool keeps
  `find_binance_historical_close_order_candidates` (different input contract).
- `lightfee/engine/close_runtime.py`: `_reactivate_history_discovery_upgrade`
  grants the legacy generic `no_candidate` debt one repair re-scan when a
  Binance leg is present (mirrors the Aster/Bitget arms), journal reason
  `binance_execution_window_anchor_repair`.
- `deploy/systemd/lightfee-production-health.service`: dropped `--json` from
  the 5-minute timer `ExecStart` — the full pretty-printed report (≈1,370
  lines per fire) flooded journald (829 MB / 9.5 days; 40,698
  `degraded_reason` lines since 2026-09-05). Operators keep the full report by
  running the script manually (deploy.sh already passes `--json`).

### Verification

- RED on the pre-fix tree: five of six new regressions fail (the sixth is the
  no-reopen guard, green on both trees by design).
- GREEN: `tests/test_automatic_historical_close_evidence.py` 47 passed
  (including the migrated COTI and takeover regressions); grouper matrix unit
  test; `tests/ops/test_production_health.py` + manifest + recovery files
  109 passed; `tests/test_venues_transport.py` 496 passed; binance-tagged 214
  passed.
- Full suite: `4,602 passed`, 25 failed, 9 skipped — the 25 failures are
  byte-identical to the clean `HEAD` baseline (verified by stash-diff on this
  tree), zero new failures.
- GitNexus: index rebuilt to `7c60b22` before editing; impact analyzed for
  both targets (async-dispatch edges weak in the graph, supplemented with
  working-tree caller search — only `close_runtime.py:888` consumes the
  discovery in production); `detect_changes` reports 9 files / risk critical,
  confined to the recovery/fee-snapshot flows, matching intent.

### Independent closure review (CL-150)

Verdict FAIL with one P1, fixed as a follow-up in the same session:

- **P1 (fixed)** — `_reactivate_history_discovery_upgrade` had no one-scan
  guard: `automatic_history_reactivated_at_ms` was write-only. A mixed debt
  with a repaired-family leg plus a bybit leg that still terminalizes with the
  legacy generic `no_candidate` re-armed the shared reason every cycle
  (reactivation → re-scan → re-terminalize → repeat, unbounded, with repeated
  critical journal events and exchange queries). The reviewer demonstrated the
  loop empirically against the real runtime (4 passes → 3 reactivations).
  Follow-up: the reactivation marker is now consumed — a debt that has already
  been reactivated once never re-opens, for the capability arm as well (a
  partial capability rollout cannot reactivate anyway because every non-zero
  leg's adapter must already support discovery). RED verified by stashing only
  the guard hunk (loop test fails without it); the mixed-debt regression joins
  the suite (48 passed in the evidence file).
- P2, accepted without code change: boundary-spanning partial fills
  undercount inside the bounded window (family-standard fail-closed, identical
  to the Aster V3 grouper); per-trade `commission` summing without a quote
  `commissionAsset` check (V1-identical, parity preserved); journal reason
  precedence labels a mixed bitget+binance repair as bitget (diagnostics
  only); `system_client_order_id` frozen before CID conflict-clearing
  (unreachable for one order, aster-identical pattern).
- Latent family risk recorded for a future entry: bybit (and bitget history)
  order-history windows are placement-time-shaped while candidate filters
  anchor on execution time — no production instance observed (both incident
  bybit legs filled same-second), so no repair is made without evidence.
- All other review areas OK: invariant enforced at the earliest shared
  boundary, no fuzzy-match promotion, no synthesized evidence, no allOrders
  orphans (operator export tool intact), systemd unit change safe (deploy.sh
  keeps manual `--json`, no journald JSON consumer), V1 parity holds
  (-2013 → None, userTrades execution truth, per-trade commission fees).
