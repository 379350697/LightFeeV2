# 2026-06-04 Bug Ledger

## Cluster CL-044-close-risk-force-v1-parity-gaps

### GitNexus Keys

- Fingerprint: `v2.force-close-helper-unwired + death-line-single-side-protection-dual-close + residual-repair-bybit-110072-loop + compensation-duplicate-no-reconcile + risk-protection-pnl-bucket-missing`
- Components: `live-runtime`, `exit-decision`, `risk-supervisor`,
  `close-executor`, `residual-repair`, `bybit-duplicate-reconcile`,
  `v1-parity`, `production-observability`
- Symbols: `LiveRuntime._maybe_process_normal_exits`,
  `build_risk_execution_plan`, `Supervisor._execute_single_side_protection`,
  `CloseExecutor.execute_single_side_protection`,
  `CloseExecutor._compensate_close_leg_exposure`,
  `CloseExecutor._writeback_to_state`,
  `LiveRuntime._recover_residual_repairs`
- Files: `lightfee/engine/runtime.py`, `lightfee/engine/risk_actions.py`,
  `lightfee/engine/supervisor.py`, `lightfee/engine/close_executor.py`,
  `tests/test_runtime_entry_flow.py`, `tests/test_risk_actions.py`,
  `tests/test_supervisor_execution.py`, `tests/test_close_execution.py`,
  `tests/test_live_entry_hedge_root_fix.py`
- Commits: working tree follow-up after `636ae11`
- Watch: `settlement_force_close`, `risk.single_side_protection_triggered`,
  `risk.single_side_protection_failed`,
  `risk.single_side_protection_unavailable`,
  `recovery.residual_repair_duplicate_client_order_reconcile_result`,
  `exit.compensation_duplicate_client_order_reconcile_result`,
  Bybit `110072 OrderLinkedID is duplicate`,
  `risk_delever_realized_price_pnl_quote`,
  `protection_realized_price_pnl_quote`
- Cards: existing Bybit duplicate and residual-repair families in
  `docs/bugs/BUG_INDEX.md`; no new card yet.

### Summary

V1/V2 source comparison found several close-path semantic gaps behind the
question "why did force close not trigger?" The deployed V2 code had helpers
for some V1 concepts, but several were not connected to the active runtime
path or did not propagate into inner force/compensation paths.

### Root Cause

| Issue | Classification | Root cause |
|---|---|---|
| `settlement_force_close` did not trigger | V2 semantic drift | `force_close_due()` existed only as a helper/test surface; runtime normal exit called `standard_close_reason()` directly, so aligned force-close deadlines degraded into `funding_capture`. |
| Death-line single-side protection touched both legs | V2 semantic drift | `RiskExecutionPlan` did not carry V1 `select_single_side_protection_target()` venue/side/stage. Supervisor logged single-side protection but called full `execute_close()`. |
| Residual repair repeated Bybit `110072` | Coverage split | Ordinary close and entry cleanup had duplicate-client reconciliation, but residual repair used deterministic `residual_repair` CID and only rescheduled on duplicate. |
| Close compensation/hard-stop duplicate lacked evidence | Coverage split | `_compensate_close_leg_exposure()` swallowed placement errors into flat rechecks and did not query Bybit order evidence or record a reconciled compensation leg. |
| Risk/protection PnL buckets stayed zero | V1 state fidelity drift | V2 had `risk_delever_*` and `protection_*` state fields but close writeback only updated generic realized PnL/fees. |
| Full V1 risk-delever L2 sizing | Still open | GitNexus impact for `build_risk_execution_plan` is HIGH. V2 still lacks the full V1 `risk_delever_sizing_plan()` integration with true-L2/top-book capacity, slippage, hot local-L2 requirement, and min-notional residual checks. This needs a dedicated high-risk change, not a cosmetic frequency reduction. |

### V1 / Exchange Decision

- V1 copy: `settlement_force_close` is evaluated outside standard close
  reasons and overrides ordinary funding close once force deadline is due.
- V1 copy: death-line protection selects exactly one venue/side/stage before
  entering fail-closed; it must not silently become a synchronized two-leg
  close.
- Exchange semantics: Bybit `110072 OrderLinkedID is duplicate` is an
  idempotency conflict. Existing V2 duplicate reconcile helper is reused for
  residual repair and compensation instead of creating a new exchange rule.
- V1 state fidelity: risk-delever and protection close PnL remain separately
  attributable in position state.

### Attempts

| Attempt | Status | Why |
|---|---|---|
| Treat force-close as ordinary `standard_close_reason` | ineffective | It returns `funding_capture` before `settlement_force_close`, losing V1 priority. |
| Reuse full `execute_close()` for death-line protection | ineffective | It submits both legs, which violates V1 single-side protection semantics. |
| Let residual/compensation duplicate paths retry blindly | ineffective | Deterministic CIDs can reproduce Bybit `110072` and leave insufficient order evidence. |
| Implement full risk-delever L2 sizing in this patch | deferred | Impact is HIGH and requires wiring runtime/local-L2 execution-liquidity snapshots into risk planning. |

### Fix

- Runtime normal exit now checks `force_close_due()` first and routes due
  aligned positions as `settlement_force_close`.
- `RiskExecutionPlan` now carries single-side protection target
  `protection_venue`, `protection_side`, and `protection_stage`, derived from
  degraded venue or health ratio.
- Supervisor now executes death-line protection through a single-leg reduce-only
  IOC close path, logs success/failure/unavailable evidence, and then enters
  fail-closed.
- `CloseExecutor.execute_single_side_protection()` submits one selected
  reduce-only IOC leg and reuses the existing close-leg retry and Bybit
  duplicate reconciliation machinery.
- Residual repair now reconciles Bybit duplicate CIDs with
  `reconcile_bybit_duplicate_client_order()`, clears live-flat/full evidence,
  retries partial live exposure with a fresh CID, and records structured
  `recovery.residual_repair_duplicate_client_order_reconcile_result`.
- Close compensation now reconciles Bybit duplicate compensation orders,
  records `exit.compensation_duplicate_client_order_reconcile_result`, and
  returns a reconciled fill leg when the old order is proven filled.
- Close writeback now fills V1 risk/protection realized PnL buckets based on
  close reason.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus pre-edit impact | `build_risk_execution_plan` upstream | HIGH: 24 impacted symbols across engine/tests; full L2 sizing deferred to separate change |
| GitNexus pre-edit impact | `_execute_single_side_protection` upstream | LOW: direct caller `execute_risk_plan` plus tests |
| GitNexus pre-edit impact | `_compensate_close_leg_exposure` upstream | LOW: affects compensation retry/hard-stop path and close tests |
| GitNexus pre-edit impact | `_writeback_to_state` upstream | LOW: affects `execute_close` and close tests |
| local RED | force-close, single-side target/execution, residual duplicate, compensation duplicate, PnL buckets | 8 failing tests before fix |
| local GREEN focused | same RED set | `8 passed` |
| local adjacent | risk actions, semantic parity, supervisor, runtime entry flow, close chunk/compensation, residual V1 parity | `151 passed` |

### Acceptance

Local focused and adjacent tests are green. Production deploy is verified on
`6138003`: services are active with `NRestarts=0`, manifest critical-file check
passed, remote focused tests passed, and post-deploy all-account exchange truth
is high-confidence flat/no-open-orders across all seven venues. Full V1
risk-delever L2 sizing remains an explicit open follow-up because it needs a
separate HIGH-impact change with its own RED/GREEN harness.

## Cluster CL-045-diagnose-empty-symbol-exchange-truth-gap

### GitNexus Keys

- Fingerprint: `diagnose-live.empty-local-symbols-skips-exchange-truth + exchange-truth-unavailable-after-healthy-deploy`
- Components: `production-diagnostics`, `exchange-truth`,
  `read-only-probes`, `deployment-acceptance`, `exchange-docs`
- Symbols: `_build_exchange_truth_async`, `_fetch_venue_positions`,
  `_fetch_venue_open_orders`, `_create_readonly_adapter`
- Files: `scripts/diagnose_live.py`, `tests/test_diagnose_live.py`
- Watch: `exchange_truth_unavailable`, `production_acceptance_gate.blocking_reasons=["exchange_truth_unavailable"]`,
  `position_probe_unfiltered_succeeded`,
  `open_order_probe_unfiltered_succeeded`

### Summary

Post-deploy diagnostics on `37d7b6f` showed runtime/service health green and
local state flat, but `diagnose_live.py --since-deploy --json` reported
`exchange_truth.available=false` and blocked the production acceptance gate with
`exchange_truth_unavailable`. The root cause was not an exchange failure:
because local open positions were empty, `diagnose_live` passed an empty symbol
list into exchange truth. Empty lists caused both position and open-order loops
to perform zero private REST calls, leaving `succeeded=set()` and classifying
the venue as `all_failed`.

### Root Cause

| Issue | Classification | Root cause |
|---|---|---|
| Empty local state could not prove exchange flat | Evidence gap | `diagnose_live` only queried per-symbol `fetch_position()` / open-order endpoints. With no local symbol candidates, it skipped the probe entirely. |
| Production default truth covered only two venues | Evidence gap | default `target_venues` was `["binance", "bybit"]`, while live trading/sidecar covers seven perp venues. |
| Bitget/Gate/Hyperliquid readonly adapters unavailable | Evidence gap | `_create_readonly_adapter()` only handled Binance, Bybit, Aster, and OKX. |
| Hyperliquid wallet/account credential ignored | Evidence gap | `_load_venue_credential()` required API key + secret and initially only loaded `LIGHTFEE_HYPERLIQUID_WALLET_PRIVATE_KEY`, while production uses `LIGHTFEE_HYPERLIQUID_PRIVATE_KEY` plus account address. |
| `asyncio.get_event_loop()` warning polluted diagnose output | Evidence hygiene | Cloud Python emitted a DeprecationWarning from the sync wrapper before JSON diagnostics. |
| Healthy no-error windows summarized as `evidence: missing` | Evidence hygiene | Evidence completeness only looked at order-error bodies; when there were no order errors it ignored high-confidence exchange truth and state consistency. |

### Exchange Docs Decision

- Bybit official V5 open-order docs allow linear all-symbol open orders through
  `GET /v5/order/realtime` with `category=linear` plus `settleCoin=USDT`.
- OKX official V5 order-list docs allow `GET /api/v5/trade/orders-pending`
  with `instType=SWAP` and no `instId` to retrieve incomplete swap orders.
- Bitget official contract docs list all pending futures orders through
  `GET /api/v2/mix/order/orders-pending?productType=USDT-FUTURES`.
- Gate official Futures API docs support listing pending futures orders with
  `status=open`.

### Fix

- Empty-symbol position truth now calls `adapter.fetch_all_positions()` or the
  transport fallback and records `position_probe_unfiltered_succeeded` under
  the `*` evidence key.
- Empty-symbol open-order truth now calls venue-documented all-account pending
  order endpoints and records `open_order_probe_unfiltered_succeeded` under
  the `*` evidence key.
- `diagnose_live` readonly adapter creation now covers Binance, Bybit, Aster,
  OKX, Bitget, Gate, and Hyperliquid.
- Default exchange truth venues now match the seven live perp venues.
- Hyperliquid credential loading now accepts both
  `LIGHTFEE_HYPERLIQUID_WALLET_PRIVATE_KEY` and production
  `LIGHTFEE_HYPERLIQUID_PRIVATE_KEY` aliases plus account address envs for
  read-only account probes.
- `_build_exchange_truth()` no longer calls deprecated `asyncio.get_event_loop()`;
  it uses `asyncio.run()` when no loop is running and a worker thread when a
  loop already exists.
- Evidence completeness now treats no-order-error windows with high-confidence
  exchange truth and consistent state as `complete` instead of `missing`.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus status | indexed commit matched current `37d7b6f` | fresh |
| GitNexus impact | `_build_exchange_truth_async`, `_fetch_venue_positions`, `_fetch_venue_open_orders`, `_create_readonly_adapter` | LOW, limited to `diagnose_live` CLI and tests |
| local RED | empty-symbol all-position probe, empty-symbol all-open-order probe, seven readonly adapters | 3 failing tests before fix |
| local GREEN focused | same RED set | `3 passed` |
| local diagnose suite | `tests/test_diagnose_live.py` | `38 passed` |
| local adjacent deploy gate | diagnose plus risk/runtime/close/residual focused suite | `189 passed` |
| local hygiene | compileall and diff check | passed |

### Acceptance

Local diagnose evidence is fixed and production verified on `6138003`.
Post-deploy `diagnose_live --since-deploy` now reports all seven venues with
`position_probe_unfiltered_succeeded` / `open_order_probe_unfiltered_succeeded`
and zero positions/orders, `state_verdict=consistent`,
`production_acceptance_gate.gate_passed=true`, `evidence_quality.overall=complete`,
and `conclusion.summary=no issues detected`. This change is read-only
diagnostic plumbing and does not submit orders, cancel orders, or mutate
runtime trading state.

## Cluster CL-046-bybit-entry-passive-close-v1-deadline-loop

### GitNexus Keys

- Fingerprint: `bybit.non-settlement-entry + pending-entry-nonterminal-zero-fill-finalized + stale-zero-reconciliation-erases-fill + passive-close-overdue-backoff-loop + passive-close-fallback-zero-fill-no-fail-closed`
- Components: `live-runtime`, `pending-entry`, `entry-reconciliation`,
  `exit-decision`, `passive-close`, `close-executor`, `bybit`,
  `v1-parity`, `production-risk`
- Symbols: `LiveRuntime._finalize_pending_entry`,
  `LiveRuntime._pending_entry_has_terminal_maker_zero_fill_evidence`,
  `passive_close_fallback_due`,
  `LiveRuntime._arm_overdue_passive_close_fallbacks`,
  `PassiveCloseExecutor._passive_close_hedge_deadline_decision`,
  `PassiveCloseExecutor._fallback_to_aggressive_close`,
  `PassiveCloseExecutor.drive_pending_passive_close`
- Files: `lightfee/engine/runtime.py`,
  `lightfee/engine/exit_decision.py`,
  `lightfee/engine/passive_close.py`,
  `tests/test_live_entry_hedge_root_fix.py`,
  `tests/test_runtime_entry_flow.py`,
  `tests/test_exit_decisions.py`,
  `tests/test_passive_close.py`
- Commits: working tree after local full gate; cloud deployment pending in this session
- Watch: `pending_entry.finalize_deferred_unresolved_maker_zero_fill`,
  `pending_entry.finalize_fill_reconciliation_ignored_stale_zero`,
  `runtime.passive_close_deadline_fallback_armed`,
  `execution.hedge_deadline_breached`,
  `execution.close_deadline_breached`,
  `exit.passive_close_hedge_deadline_fail_closed`,
  `exit.passive_close_fallback_zero_fill_no_pending`,
  `exit.passive_close_fallback_unavailable`
- Cards:
  [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md),
  [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md)

### Summary

Bybit account history showed a serious V2/V1 parity gap family: entries such
as `MEUUSDT` opened around `2026-06-03 20:12`, outside the expected Bybit
settlement-close window; multiple Bybit legs closed after seconds; and at
least one position stayed exposed for more than eight hours before close. The
incident is dual-leg by definition: Bybit maker/fill evidence must correspond
to the opposite venue leg before V2 can finalize an entry or drive close.

### Root Cause

| Issue | Classification | Root cause |
|---|---|---|
| Bybit maker zero-fill finalized while order was not terminal | V2 semantic drift | `_finalize_pending_entry()` treated a zero maker reconciliation as removable even when the maker order still had nonterminal `new` / unresolved evidence. V1 keeps the pending entry until terminal no-fill/fill evidence exists. |
| Later stale zero reconciliation erased known hedge fill | V2 semantic drift | A subsequent zero order reconciliation could overwrite a confirmed positive leg fill. That converted a real two-leg position/residual into a false unmatched branch and could trigger one-sided cleanup. |
| Pending passive close ignored settlement force deadline | V2 semantic drift | Passive close retry state could keep a future `next_retry_at_ms` after the V1 force window, so overdue close work was not armed into DUAL_TAKER promptly. |
| Passive close fallback retried forever after executor/zero-fill failure | V2 semantic drift | `_fallback_to_aggressive_close()` kept applying short backoff when `close_executor` was unavailable, invalid, returned `None`, or returned zero fill without a pending close, even after the V1 exit hedge deadline was hard-breached. |
| Hedge gap kept retrying after hard breach | V2 semantic drift | Unhedged maker/hedge gaps were allowed to attempt more hedge/retry work instead of entering fail-closed with compensation and exchange-truth flat probing. |

### V1 / Exchange Decision

- V1 pending-entry terminality is order/fill evidence based. A zero quantity
  result is not enough if maker status is nonterminal or unresolved.
- V1 never lets later stale zero evidence erase already-confirmed positive
  fill progress.
- V1 passive exit has a hedge/fallback hard deadline. After hard breach the
  system must stop passive retry backoff, enter fail-closed, compensate where
  possible, and probe live flat truth.
- V1 passive close fallback is dual-leg close work: Bybit close actions must
  correspond to the opposite leg and cannot be accepted as isolated local
  terminality.

### Attempts

| Attempt | Status | Why |
|---|---|---|
| Let zero maker reconciliation finalize as passive unfilled | ineffective | It can drop a live/open maker order and leave later Bybit fills to recovery/cleanup. |
| Let zero reconciliation overwrite known fill | ineffective | It can erase a confirmed opposite-leg fill and create false residual/cleanup. |
| Only arm DUAL_TAKER after normal passive phase retry time | ineffective | A future retry can miss the V1 settlement-force deadline and create long exposure. |
| Keep fallback retrying on no executor / zero aggressive fill | ineffective | This preserves retry noise but does not close risk after the V1 hard deadline. |

### Fix

- Pending-entry finalize now defers zero-balanced finalization when maker order
  evidence exists but terminal no-fill evidence is missing, and emits
  `pending_entry.finalize_deferred_unresolved_maker_zero_fill`.
- Fill reconciliation now ignores stale zero results when a positive fill was
  already known for that leg, and records
  `pending_entry.finalize_fill_reconciliation_ignored_stale_zero`.
- `passive_close_fallback_due()` mirrors V1 settlement force timing for
  pending passive closes, including staggered second-stage semantics.
- Runtime now arms overdue pending passive closes into `DUAL_TAKER`, resets
  phase/cycle timestamps, clears retry delay, and records
  `runtime.passive_close_deadline_fallback_armed`.
- Passive close executor now carries `maker_hedge_deadline_ms`, computes V1
  adaptive hedge/fallback hard-deadline decisions, and converts hard breaches
  into fail-closed events instead of continuing retry backoff.
- Hedge hard breaches call compensation through `compensate_failed_full_close`
  when available, then use live-flat probing before clearing local state.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| GitNexus status | index matched current local `c5a0f0f` before edits | fresh |
| GitNexus impact | `_fallback_to_aggressive_close`, `drive_pending_passive_close` | HIGH, core passive close flow; guarded by focused and full regression |
| GitNexus impact | `_submit_hedge_for_delta` | MEDIUM, hedge catch-up surface |
| local RED/GREEN | unresolved maker zero-fill, stale zero reconciliation, overdue passive fallback, fallback no-executor hard breach, zero-fill no-pending hard breach, hedge gap hard breach | targeted tests now pass |
| local passive close suite | `tests/test_passive_close.py -q` | `110 passed` |
| local focused adjacent | runtime entry, exit decision, zero-fill finalize, passive deadline tests | `16 passed` |
| local historical JCT regression | terminal maker missing-price compensation path | `1 passed` |
| local full gate | `.venv/bin/python -m pytest -q` | `3423 passed`, `9 skipped`, `1 warning` |
| local hygiene | `py_compile`, `git diff --check`, GitNexus `detect-changes` | compile/check clean; GitNexus risk HIGH as expected for close flow |

### Acceptance

Local V1 parity and regression gates are green. Cloud deployment and
post-deploy credentialed flat/no-open-orders verification are pending in this
session. Closure requires the remote host to fast-forward to this commit, write
the short `.deploy_version`, pass manifest/compile/test gates, restart services
cleanly, and show healthy runtime plus high-confidence exchange truth.
