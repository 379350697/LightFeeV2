# Bug Ledger Daily - 2026-05-27

## GitNexus Index

| Cluster ID | Status | Severity | Fingerprint | Components | Symbols | Files | Commits | Outcome |
|---|---|---:|---|---|---|---|---|---|
| CL-013-pending-entry-v1-terminality-drift-live-single-sided | fixed in code and remote harness/probe; live remains stopped pending operator restart/state decision | critical | `v2.pending-entry.stale-accepted-flat-cleared + planned-hedge-cid-pre-submit-query + exchange-truth-nonzero-local-flat + pending-finalize-live-position-hydration-gap` | `live-runtime`, `pending-entry`, `order-reconciliation`, `diagnose-live`, `live-harness`, `v1-parity` | `LiveRuntime._reconcile_pending_state`, `LiveRuntime._ensure_pending_entry_open_fill_details`, `LiveRuntime._pending_entry_flat_clear_has_terminal_maker_evidence`, `LiveRuntime._order_status_is_terminal_no_fill`, `OrderReconciler._status_from_reconciliation` | `lightfee/engine/runtime.py`, `lightfee/engine/reconciliation.py`, `tests/test_pending_entry_v1_semantic_drift.py`, `tests/live_harness/test_pending_entry_v1_semantic_drift_incident.py`, `tests/probes/test_false_green_exchange_truth_gate.py`, `tests/fixtures/live_incidents/2026-05-27/pending_entry_v1_semantic_drift.jsonl` | working tree / remote hot patch | V2 no longer treats momentary flat position plus stale accepted maker evidence as terminal no-fill, no longer queries a preplanned hedge CID before submit, and hydrates balanced live-position evidence into pending-entry fill quantities/prices before finalizing like V1. |
| CL-008-passive-maker-zero-fill-and-live-acceptance | fixed in runtime commit `bc44c72`; latest cloud main harness/probe passed | high | `v2.passive-maker-zero-fill.fail-closed-abort + low-maker-fill-rate + production-acceptance-gate` | `live-runtime`, `passive-entry`, `v1-parity`, `diagnose-live`, `live-harness` | `LiveRuntime._maintain_pending_entry_passive_orders`, `LiveRuntime._handle_pending_passive_zero_fill_completion`, `LiveRuntime._maker_try_window_fill_shortfall`, `LiveRuntime._finalize_pending_entry`, `run_diagnose` | `lightfee/engine/runtime.py`, `scripts/diagnose_live.py`, `scripts/analyze_production_blockers.py`, `tests/live_harness/test_passive_maker_zero_fill_incident.py`, `tests/fixtures/live_incidents/2026-05-27/passive_maker_zero_fill.jsonl` | runtime `bc44c72` | Zero-fill passive maker cancels are treated as V1 retry/repost cycles, not fail-closed health failures. Harness proves cancel-window zero fill records evidence, reposts within budget, and terminalizes as `entry.passive_unfilled` after repost exhaustion without `entry.aborted` or fail-closed. |
| CL-009-okx-recovery-probe-rate-limit-and-catalog-gate | fixed in runtime commit `bc44c72`; latest cloud main harness/probe passed | high | `v2.okx-recovery-probe.full-universe-fanout + 50011-rate-limit + unsupported-instrument-noise` | `startup-recovery`, `venue-okx`, `venue-transport`, `exchange-docs`, `diagnose-live` | `LiveRuntime._startup_position_probe_symbols`, `LiveRuntime._fetch_startup_live_position_snapshots`, `LiveRuntime._filter_symbols_supported_by_venue`, `LiveRuntime._position_probe_exception_payload`, `OkxAdapter.supported_symbols`, `VenueTransport.fetch_all_positions`, `VenueTransport.fetch_position` | `lightfee/engine/runtime.py`, `lightfee/venues/okx.py`, `lightfee/venues/transport.py`, `tests/live_harness/test_okx_recovery_probe_rate_limit_incident.py`, `tests/fixtures/live_incidents/2026-05-27/okx_recovery_probe_noise.jsonl` | runtime `bc44c72` | Startup recovery no longer expands clean state into the full config universe. OKX prefers one bulk positions request, catalog-gates unsupported instruments, and classifies official `50011`/HTTP 429 as venue-level private-position cooldown evidence. |
| CL-010-local-l2-official-rebuild-evidence | fixed in runtime commit `bc44c72`; current rebuilds classified by gate | medium | `v2.local-l2-buffered-replay-mismatch.evidence-gap` | `local-l2-data-plane`, `exchange-docs`, `live-harness`, `diagnose-live` | `LocalL2DataPlane._buffered_replay_failure_evidence`, `classify_local_l2_incident`, `run_diagnose` | `lightfee/marketdata/local_l2_data_plane.py`, `tests/live_harness/test_local_l2_incident_replay.py`, `tests/fixtures/live_incidents/2026-05-27/binance_local_l2_replay_mismatch.jsonl`, `scripts/diagnose_live.py` | runtime `bc44c72` | Buffered replay failures now carry enough sequence evidence to classify Binance/Aster rebuilds as official-doc orderbook continuity resets when `pu`/previous-sequence evidence is complete; missing evidence remains an explicit harness failure/evidence gap. |
| CL-011-snapshot-fallback-candidate-scoped-gate | fixed in runtime commit `bc44c72`; production current state healthy | high | `v2.snapshot-fallback-degraded.global-blocking-ambiguity` | `live-runtime`, `sidecar-snapshot`, `entry-gate`, `diagnose-live`, `v1-parity` | `LiveRuntime._snapshot_fallback_duration_ms`, `LiveRuntime._snapshot_health_candidate_freshness_scope`, `LiveRuntime._filter_candidates_by_snapshot_freshness`, `LiveRuntime._emit_scan_no_entry_diagnostics` | `lightfee/engine/runtime.py`, `tests/live_harness/test_snapshot_fallback_degraded_incident.py`, `tests/test_runtime_snapshot_freshness.py`, `scripts/diagnose_live.py` | runtime `bc44c72` | Snapshot fallback/degraded diagnostics are candidate/domain scoped. Last-good fallback and coarse market-observed staleness are not global entry blockers when required quote/local-L2 evidence is fresh; blocking cases now include candidate, domain, venue, source age, fallback duration, and reason. |
| CL-012-okx-single-symbol-position-regression | fixed in runtime commit `bc44c72`; latest cloud main harness/probe passed | high | `v2.okx-fetch-position.single-symbol-instid-query-bypassed-by-bulk-positions` | `venue-okx`, `venue-transport`, `contract-units`, `regression-review` | `VenueTransport.fetch_position`, `VenueTransport.fetch_all_positions` | `lightfee/venues/transport.py`, `tests/test_venues_transport.py` | runtime `bc44c72` | Acceptance review found that OKX `fetch_position("CHIPUSDT")` had been changed to call all-positions first, bypassing the original `instId=CHIP-USDT-SWAP` single-symbol path and breaking `ctVal` normalization tests. The fix keeps all-position cache reuse only when a fresh bulk cache already exists; otherwise `fetch_position` uses the original single-symbol request. |

## Cluster CL-013-pending-entry-v1-terminality-drift-live-single-sided

### Summary

After the 2026-05-27 deploy, production opened live single-sided positions while local V2 state later reported no open positions and no active pending entry. Read-only exchange truth showed nonzero live positions including Binance `MUBARAKUSDT` long `1758`, Binance `EDENUSDT` long `710`, Bybit `INUSDT` long `750`, and Aster `BEATUSDT` long `23`, with no matching local open-position accounting.

This is V2 semantic drift from V1. V1 does not clear a pending maker entry merely because both venue positions are momentarily flat after an accepted maker order. It keeps probing maker progress/order state and drives the hedge only after real maker fill evidence. V2 instead allowed a `stale_accepted_order` plus zero position snapshot to become `live_no_effect_confirmed`, emitted `order.reconcile_resolution clears_uncertain_state=true`, and then cleared the pending entry as `reconciliation.entry_cleared_flat`. A related V2 drift queried a preplanned hedge client id before the hedge order had been submitted; on BEATUSDT this produced repeated Binance `-2013 Order does not exist` reconciliation errors after Aster maker fill evidence existed.

### Root Fix

- `OrderReconciler._status_from_reconciliation()` no longer converts `stale_accepted_order` / accepted-order evidence into `not_found` or `live_no_effect_confirmed` just because the current live position snapshot is zero.
- Pending-entry reconciliation now passes an empty hedge lookup CID until `pending.hedge_inflight` exists. A preallocated/planned `hedge_client_order_id` is not exchange evidence and is not queried before submit.
- Flat pending-entry cleanup now requires actual maker terminal no-fill evidence: `canceled`, `cancelled`, `expired`, or `rejected`.
- `not_found`, `missing`, `notfound`, and `not_found_or_closed` are no longer treated as terminal no-fill for a pending maker order with accepted/submitted evidence.
- PRLUSDT follow-up root fix: pending-entry reconciliation now copies live position snapshot quantity and `entry_price` into the corresponding maker/hedge pending fill, and assigns a V1-style synthetic recovery order id (`<entry_id>-recovery-long/short`) when the exchange proves live balanced exposure without order-fill details.
- `_ensure_pending_entry_open_fill_details()` now matches V1 `PendingEntryLegFill` semantics: a balanced entry needs positive fill prices before `entry.opened`; order ids are useful evidence but are not a V1 hard prerequisite for opening a balanced pending entry.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-27 | cloud RED before fix | `PATH=/opt/lightfee-v2/.venv/bin:$PATH .venv/bin/python3 -m pytest -q tests/test_pending_entry_v1_semantic_drift.py` | failed 3 tests: flat reconcile cleared pending, planned hedge CID was queried before submit, and stale accepted order became terminal no-effect |
| 2026-05-27 | cloud GREEN after fix | same command after patching `runtime.py` and `reconciliation.py` | `3 passed` |
| 2026-05-27 | cloud focused adjacent tests | `pytest -q tests/test_recovery_reconciliation.py tests/test_live_entry_hedge_root_fix.py tests/test_runtime_entry_flow.py -k 'live_no_effect or stale_accepted or flat_reconcile or unresolved_maker or missing_hedge or pending_entry'` | `16 passed, 172 deselected` |
| 2026-05-27 | cloud harness/probe | `pytest -q tests/test_pending_entry_v1_semantic_drift.py tests/live_harness/test_pending_entry_v1_semantic_drift_incident.py tests/probes/test_false_green_exchange_truth_gate.py` | `5 passed, 1 skipped`; the skip is an unrelated credential-gated live probe module side effect |
| 2026-05-27 | cloud live-harness profile | `PATH=/opt/lightfee-v2/.venv/bin:$PATH .venv/bin/python3 scripts/validate_change.py --profile live-harness --keep-going` | passed compileall, `git diff --check`, and `pytest -q tests/live_harness`; `47 passed` |
| 2026-05-27 | cloud explicit offline probe | `LIGHTFEE_RUN_LIVE_PROBES=1 .venv/bin/python3 -m pytest -q tests/probes/test_false_green_exchange_truth_gate.py` | `1 passed`; verifies local-flat/exchange-nonzero is a critical probe failure without touching live trading APIs |
| 2026-05-27 | cloud live restart | `systemctl restart lightfee-live` after backing up `runtime/live-state.json` | clean stop/start at 13:51:17 CST; no KILL/timeout. Startup recovery reduced live mismatches: Binance `MUBARAKUSDT` 1758, Binance `EDENUSDT` 710, Bybit `INUSDT` 750, and Aster `BEATUSDT` 23 were flattened by reduce-only runtime recovery. |
| 2026-05-27 | cloud read-only account truth | `/tmp/lightfee_readonly_positions.py` and `/tmp/lightfee_exchange_positions.py` with systemd env | all 7 venues reported `nonzero_positions=[]`; Binance/Bybit/Aster/OKX open-order counts were `0`; Bitget/Gate/Hyperliquid open-order probes are not implemented in that helper. |
| 2026-05-27 | cloud safety action | `systemctl stop lightfee-live` | live runtime stopped after exchange truth was flat, because a new PRLUSDT pending-entry finalization issue appeared after restart; sidecar left running. |
| 2026-05-27 | cloud RED for PRLUSDT second-layer drift | `pytest -q tests/test_pending_entry_v1_semantic_drift.py -k live_position_hydrates_balanced_pending_entry_and_finalizes_like_v1` before the PRL hydration patch | failed: pending entry was removed but no `open_position` was created, reproducing the live-recovery fallback gap. |
| 2026-05-27 | cloud GREEN for PRLUSDT second-layer drift | same command after the PRL hydration patch | `1 passed`; balanced live position snapshots hydrate pending fills and finalize into an open position without `pending_entry.finalize_deferred_incomplete_fill`. |
| 2026-05-27 | cloud focused adjacent tests | `pytest -q tests/test_pending_entry_v1_semantic_drift.py tests/test_recovery_reconciliation.py tests/test_live_entry_hedge_root_fix.py tests/test_runtime_entry_flow.py -k 'live_position_hydrates or live_no_effect or stale_accepted or flat_reconcile or unresolved_maker or missing_hedge or pending_entry'` | `20 passed, 172 deselected` |
| 2026-05-27 | cloud live harness | `.venv/bin/python -m pytest -q tests/live_harness` | `48 passed`; includes PRLUSDT fixture replay for balanced live-position hydration. |
| 2026-05-27 | cloud live-harness profile partial | `.venv/bin/python scripts/validate_change.py --profile live-harness --keep-going` | compileall and `git diff --check` passed; script failed only because it executes `pytest` by name and non-interactive SSH PATH did not include the venv executable. Manual `.venv/bin/python -m pytest -q tests/live_harness` passed. |
| 2026-05-27 | cloud explicit probe rerun | `LIGHTFEE_RUN_LIVE_PROBES=1 .venv/bin/python -m pytest -q tests/probes/test_false_green_exchange_truth_gate.py` | `1 passed` |
| 2026-05-27 | cloud credentialed read-only account truth rerun | `set -a && . /etc/lightfee/lightfee.env && set +a && .venv/bin/python /tmp/lightfee_exchange_positions.py` | Binance/Bybit/Aster/OKX/Bitget/Gate/Hyperliquid all returned `nonzero_positions=[]`; Binance/Bybit/Aster/OKX open-order counts were `0`; Bitget/Gate/Hyperliquid open-order fetch remains unimplemented in this helper. |
| 2026-05-27 | cloud automatic cleanup attempt | start `lightfee-live` with state temporarily marked `risk_mode=reduce_only`, then poll `runtime/live-state.json` | automatic runtime cleanup succeeded: legacy PRLUSDT pending entry emitted `entry.aborted` with `pending_entry_max_lifetime_exhausted`, maker/hedge quantities `0.0/0.0`; state became `open=0`, `pending=0`, residuals `0`; live was stopped immediately after cleanup. |
| 2026-05-27 | cloud deploy startup watch | normal `systemctl start lightfee-live` after cleanup | first normal start hit existing operational issue: `oom-kill`, 7.2G memory peak, because `runtime/live-events.jsonl` had grown to 1.7G and startup replay/diagnostics exhausted RAM. No positions or pending entries appeared. |
| 2026-05-27 | cloud event-log archive mitigation | archived `runtime/live-events.jsonl` to `runtime/archive/live-events.pre-deploy-oom-20260527-193109.jsonl`, created a fresh empty `runtime/live-events.jsonl`, then `systemctl reset-failed` and restarted live | live started cleanly; new event log 43K; `ActiveState=active`, `SubState=running`, `NRestarts=0`; state `open=0`, `pending=0`, residuals `0`, tick fresh. |
| 2026-05-27 | cloud final deployment health | `scripts/verify_production_services.py --json` after restart settled | `ok=true`, `critical_count=0`, `warning_count=0`, sidecar/live active, `risk_mode=running`, `lifecycle=running`, all local position/pending/residual counts `0`, no exchange-truth mismatches. |
| 2026-05-27 | cloud final read-only account truth | `set -a && . /etc/lightfee/lightfee.env && set +a && .venv/bin/python /tmp/lightfee_exchange_positions.py` | all venues returned `nonzero_positions=[]`; Binance/Bybit/Aster/OKX open-order counts `0`; Bitget/Gate/Hyperliquid open-order fetch remains not implemented in helper. Fresh `runtime/live-events.jsonl` had no new `execution.entry_selected`, `entry.opened`, `runtime.position_opened`, pending-finalize, or reconcile-error events in the deployment watch window. |

### Recorded-Only Post-Deploy Issues

These were observed during the same deployment audit but intentionally not fixed in this task because the user requested trading semantics first.

- `lightfee-live` shutdown/restart still needs a production restart acceptance watch for the prior timeout/KILL family.
- OKX/Binance private listen-key or position probe rate-limit noise should stay on watch, especially HTTP 429 / exchange cooldown evidence.
- Local-L2 rebuild / `clock_skew` / sequence-reset evidence still appears and should be handled as the existing CL-010 watch item unless it blocks entries without evidence.
- Snapshot fallback/degraded diagnostics remain a separate CL-011 watch item; no new snapshot code was changed in this fix.
- Post-restart PRLUSDT exposed a second-layer trading residual: the fixed code retained `stale_accepted_order` correctly, later saw maker fill and submitted/filled the hedge, but position-only hedge evidence lacked price/order-id details and V2 deferred finalize before live recovery found the balanced position. This branch is now covered by unit and live-harness PRLUSDT replay tests.
- The pre-fix PRLUSDT pending entry was later auto-cleaned by the fixed runtime under a reduce-only cleanup window; no manual state deletion was needed. Normal live deployment initially hit a separate operational OOM due a 1.7G live event log; the old log was archived and a fresh event log was created before restarting live.

### Regression Watch

- `order.reconcile_query.response_classification=stale_accepted_order` followed by `order.reconcile_resolution clears_uncertain_state=true`.
- `reconciliation.entry_cleared_flat` for a pending entry that still has maker order id/client id but no maker terminal no-fill evidence.
- `reconciliation.entry_reconcile_error` with exchange `Order does not exist` for a preplanned hedge CID before `hedge_inflight` exists.
- `open_position_count=0` and `pending_entry_count=0` while credentialed `exchange_truth.has_nonzero_position=true`.
- `pending_entry.finalize_deferred_incomplete_fill` with only `hedge_order_id` / `maker_order_id` missing after both legs have positive balanced quantity and fill prices.
- `recovery.live_detected` for the same entry immediately after balanced pending-entry maker/hedge progress, which means pending finalization did not consume live-position evidence.
- `runtime/live-events.jsonl` growing large enough to make startup replay/diagnostics exceed memory; archive/compaction should be handled as an operational maintenance item, not mixed with trading semantic fixes.

## Cluster CL-008-passive-maker-zero-fill-and-live-acceptance

### Summary

Production after the 2026-05-27 deploy showed repeated passive maker entries that selected, submitted a post-only maker order, received zero fill inside the maker try window, canceled the maker order, and then emitted `entry.aborted` with `passive_maker_canceled_zero_fill`. That made the engine safe-flat, but it also depressed maker fill rate and caused noisy fail-closed/risk-mode recovery cycles.

The root behavior should match V1: zero-fill inside the maker try window is a passive-entry lifecycle outcome. It should cancel stale maker liquidity, record the zero-fill cycle, wait the configured retry delay, repost/reprice within the repost budget, and only terminalize cleanly as unfilled after budget exhaustion. It is not itself a health failure.

### Root Fix

- `LiveRuntime._maintain_pending_entry_passive_orders()` no longer skips recovered terminal zero-fill pending orders behind the `cancel_requested && maker_completed` guard.
- New zero-fill completion handling records `passive_maintenance.zero_fill_cycle`, waits `maker_cycle_retry_delays_ms`, respects maker venue budget, reposts with a fresh compact client order id, and increments `repost_count`.
- Repost-budget exhaustion calls `_finalize_pending_entry()`, which already has the V1 zero-fill branch that emits `entry.passive_unfilled` and `pending_entry.pending_entry_finalized` without creating an open position.
- Zero-fill cancel/repost paths no longer call `_abort_pending_entry_fail_closed()` when there is no fill and exchange truth remains flat.
- `diagnose_live.py` and `analyze_production_blockers.py` expose `passive_maker_zero_fill_count`, fill rate, fail-closed abort count, and production acceptance gate fields.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-27 | local harness | `.venv/bin/python -m pytest tests/live_harness/test_passive_maker_zero_fill_incident.py tests/test_live_entry_hedge_root_fix.py tests/test_runtime_maker_event_local_l2.py tests/live_harness/test_okx_recovery_probe_rate_limit_incident.py tests/live_harness/test_local_l2_incident_replay.py tests/live_harness/test_snapshot_fallback_degraded_incident.py tests/test_runtime_snapshot_freshness.py tests/test_diagnose_live.py tests/test_live_startup_preflight.py -q` | `258 passed` |
| 2026-05-27 | local validation profile | `.venv/bin/python scripts/validate_change.py --profile live-harness --keep-going` | passed: compileall, `git diff --check`, and `pytest -q tests/live_harness`; `45 passed` |
| 2026-05-27 | GitNexus | `detect_changes(scope=unstaged, repo=LightFeeV2)` | `risk_level=high`, `changed_files=10`, `affected_count=8`; high risk is due to broad `runtime.py`/diagnose changes and must be reviewed as a high-risk main push |
| 2026-05-27 | cloud read-only current state | `.venv/bin/python scripts/verify_production_services.py --json` on `/opt/lightfee-v2` | `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, `open_position_count=0`, `pending_entry_count=0`, `pending_close_count=0`, `pending_residual_repair_count=0` |
| 2026-05-27 | cloud read-only symbol probe | `diagnose_live.py --since-deploy --symbol RIVERUSDT --venues aster,okx --max-events 100000 --json` | high-confidence exchange truth: positions `{}`, open orders `[]`; production acceptance gate `gate_passed=true`, `passive_maker_zero_fill_count=13`, `abort_fail_closed_count=0` |
| 2026-05-27 | cloud read-only symbol probe | `diagnose_live.py --since-deploy --symbol BULLAUSDT --venues aster --max-events 100000 --json` | high-confidence exchange truth: positions `{}`, open orders `[]`; production acceptance gate `gate_passed=true`, `passive_maker_zero_fill_count=1`, `abort_fail_closed_count=0` |
| 2026-05-27 | cloud read-only symbol probe | `diagnose_live.py --since-deploy --symbol MONUSDT --venues bybit --max-events 100000 --json` | high-confidence exchange truth: positions `{}`, open orders `[]`; production acceptance gate `gate_passed=true` |
| 2026-05-27 | cloud latest main (runtime fix `bc44c72`) | `PATH=/opt/lightfee-v2/.venv/bin:$PATH .venv/bin/python3 scripts/validate_change.py --profile live-harness --keep-going` | passed: compileall, `git diff --check`, and `pytest -q tests/live_harness`; `45 passed` |
| 2026-05-27 | cloud latest main (runtime fix `bc44c72`) | `scripts/verify_production_services.py --json` after restart settled | `ok=true`, `critical_count=0`, `warning_count=0`, tick fresh, `risk_mode=running`, `lifecycle=running`, all local position/pending/residual counts `0` |

### Regression Watch

- `entry.aborted` reason `passive_maker_*_zero_fill` immediately after a cancel-window zero-fill with no fill.
- `runtime.risk_mode_changed` fail-closed/running churn caused only by passive maker zero-fill.
- `passive_maintenance.cancel_try_window` without subsequent `passive_maintenance.zero_fill_cycle`, repost, clean terminalization, or explicit retained-uncertain evidence.
- Maker fill rate staying zero for many cycles is still a strategy/liquidity quality signal even when lifecycle safety is correct.

## Cluster CL-009-okx-recovery-probe-rate-limit-and-catalog-gate

### Summary

Recovery probing generated high OKX private-position noise: timeouts, `50011`/HTTP 429 rate limits, and unsupported/instrument-missing symbols such as OKX contracts without usable `ctVal`. V2 drifted from V1 by allowing a clean startup state to expand toward the full configured universe and by falling back to per-symbol probes after bulk OKX failure.

### Root Fix

- Clean local state no longer contributes the full configured/resolved symbol universe to startup recovery probing.
- Recovery probe symbols come from actual recovery work: open positions, pending entries/closes/passive closes, residual repairs, live-recovery pairs, and explicitly touched/recent symbols.
- OKX `supported_symbols()` and `ensure_supported_symbols_loaded()` expose the official SWAP instrument catalog for recovery catalog gating.
- OKX bulk position probe failures classified as rate-limited emit venue-level cooldown evidence and do not fan out to per-symbol position probes in the same window.
- Unsupported symbols are aggregated in `recovery.live_position_probe_unsupported_symbols` with endpoint, requested symbols, skipped catalog symbols, and symbol counts.

### Official Docs Used

- OKX REST account positions endpoint rate limits are by User ID; production `50011`/HTTP 429 is classified as rate-limited private-position evidence.
- OKX public instruments metadata supplies `ctVal` and `ctType`; missing or unsupported contracts are catalog/metadata evidence, not retryable live-position truth.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-27 | local harness | `tests/live_harness/test_okx_recovery_probe_rate_limit_incident.py` in the 258-test focused set | passed |
| 2026-05-27 | local regression review | `.venv/bin/python -m pytest tests/test_venues_transport.py -k "okx or fetch_all_positions or supported_symbols or position_cache" tests/venues/test_v1_capability_matrix.py -q` | first run exposed 2 OKX single-symbol regressions; after fix, `68 passed, 331 deselected` |
| 2026-05-27 | cloud latest main (runtime fix `bc44c72`) | symbol-scoped probes for RIVERUSDT, MONUSDT, CHIPUSDT on OKX-involved or incident venues | all high-confidence exchange truth flat/no-open-orders; `okx_recovery_probe_rate_limited_count=0`, `okx_instrument_missing_skipped_count=0`, `gate_passed=true` |

### Regression Watch

- OKX `recovery.live_position_probe_error` count climbing due to full-universe per-symbol fanout.
- OKX bulk `50011`/429 followed by immediate per-symbol position probes.
- Unsupported OKX symbols appearing as retryable live-position failures instead of catalog skips.
- `VenueTransport.fetch_position()` for OKX must preserve single-symbol `instId` query semantics unless a fresh all-position cache already exists.

## Cluster CL-010-local-l2-official-rebuild-evidence

### Summary

Current production logs still include local-L2 rebuild/snapshot-error counts, but many samples have the official continuity evidence needed to classify them as exchange orderbook sequence resets rather than parser drift. Binance official local orderbook semantics require a diff-depth event's `pu` to match the previous event `u`; otherwise the book must be reinitialized from snapshot.

### Root Fix

- Buffered replay failure evidence now includes a direct `reason` field alongside existing rebuild fields.
- Harness classification requires closed evidence: previous sequence present, expected previous sequence, raw `U`, raw `u`, and raw `pu`.
- `diagnose_live.py` and `analyze_production_blockers.py` count official local-L2 rebuilds separately from evidence gaps.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-27 | local harness | `tests/live_harness/test_local_l2_incident_replay.py` in the 258-test focused set | passed |
| 2026-05-27 | local validation profile | `.venv/bin/python scripts/validate_change.py --profile live-harness --keep-going` | `45 passed` for all live harness tests |
| 2026-05-27 | cloud latest main (runtime fix `bc44c72`) | `diagnose_live.py --since-deploy --symbol MONUSDT --venues bybit --max-events 80000 --json` | `local_l2_official_rebuild_count=1`, classified as `official_doc`; exchange truth flat/no-open-orders; `gate_passed=true` |

### Regression Watch

- `runtime.local_l2_snapshot_error` without sequence/reset payload fields required by the harness.
- Binance/Aster sequence rebuilds classified as official-doc behavior without `previous_sequence_present` and raw sequence fields.
- `runtime.entry_blocked_local_l2_selection` with no candidate/venue/symbol readiness diagnostics.

## Cluster CL-011-snapshot-fallback-candidate-scoped-gate

### Summary

Snapshot fallback and degraded domains previously risked being interpreted as global entry blockers. The 2026-05-27 closure adds candidate/domain scoped evidence so operators can tell whether last-good fallback was advisory, whether a specific candidate was blocked, and which source age/domain caused the decision.

### Root Fix

- `runtime.snapshot_fallback_last_good` and `runtime.snapshot_degraded` include `candidate_freshness_scope`.
- Candidate samples include `candidate_pair_id`, `candidate_symbol`, `domain`, `venue`, `source`, `source_age_ms`, `fallback_duration_ms`, `blocked`, and `block_reason`.
- Runtime freshness filter emits the same candidate-scoped fields for `runtime.snapshot_freshness_decision`.
- `diagnose_live.py` counts `snapshot_fallback_blocking` only when candidate scope proves blocking.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-27 | local harness | `tests/live_harness/test_snapshot_fallback_degraded_incident.py` in the 258-test focused set | passed |
| 2026-05-27 | local unit tests | `tests/test_runtime_snapshot_freshness.py` in the 258-test focused set | passed |
| 2026-05-27 | cloud latest main (runtime fix `bc44c72`) | symbol-scoped production acceptance gates for RIVERUSDT, BULLAUSDT, MONUSDT, and CHIPUSDT | all `snapshot_fallback_blocking_count=0` and `gate_passed=true` |

### Regression Watch

- `runtime.snapshot_fallback_last_good` without candidate/domain scope.
- `market_observed_stale` or coarse liquidity staleness producing a global no-entry when the selected candidate has fresh quote/local-L2 evidence.
- `snapshot_fallback_blocking_count > 0` without a candidate-scoped `block_reason`.

## Cluster CL-012-okx-single-symbol-position-regression

### Summary

Acceptance review found a new regression introduced while reducing OKX recovery-probe fanout: `VenueTransport.fetch_position()` called `fetch_all_positions()` when no OKX position cache existed. That bypassed the existing single-symbol private request with `params={"instId": "CHIP-USDT-SWAP"}` and broke tests that prove OKX instrument metadata/`ctVal` is used for base-quantity normalization.

### Root Fix

- Keep `_all_positions_cache` as an optimization only after an explicit successful `fetch_all_positions()` call.
- If no fresh all-position cache exists, `fetch_position()` follows the original single-symbol request path and sends `instId`.
- A missing symbol in a fresh all-position cache still returns a cached zero snapshot, avoiding repeated private-position calls during recovery windows.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-27 | local RED | `.venv/bin/python -m pytest tests/test_venues_transport.py -k "okx or fetch_all_positions or supported_symbols or position_cache" tests/venues/test_v1_capability_matrix.py -q` before the acceptance-review fix | failed 2 tests: `test_okx_fetch_position_uses_instrument_ct_val_for_base_quantity` and `test_okx_fetch_position_preloads_swap_instrument_metadata`; both saw `{}` params instead of `{"instId": "CHIP-USDT-SWAP"}` |
| 2026-05-27 | local GREEN | same command after the fix | `68 passed, 331 deselected` |
| 2026-05-27 | local focused suite | 258-test harness/regression command above | `258 passed` |
| 2026-05-27 | local profile | `.venv/bin/python scripts/validate_change.py --profile live-harness --keep-going` | passed |
| 2026-05-27 | cloud latest main (runtime fix `bc44c72`) | `diagnose_live.py --since-deploy --symbol CHIPUSDT --venues binance,okx --max-events 80000 --json` | high-confidence exchange truth flat/no-open-orders; production gate `gate_passed=true`; no OKX recovery probe rate limit or instrument-missing skips in window |

### Regression Watch

- OKX `fetch_position(symbol)` issuing `/api/v5/account/positions` without `instId` unless it is intentionally using a previously populated fresh all-position cache.
- `ctVal`/`ctType` missing from OKX instrument metadata for position quantity conversion.
- Acceptance tests around `CHIPUSDT` or prefixed OKX contracts failing with empty params.

## Production Acceptance Notes

The cloud `/opt/lightfee-v2` deployment was fast-forwarded from `8174b6a` to the runtime fix commit `bc44c72`, then to the latest main documentation commit. `.deploy_version` was updated to the latest cloud HEAD, and the deploy manifest check passed for all 14 critical files. `lightfee-sidecar.service` and `lightfee-live.service` were restarted at 2026-05-27 03:50:31 CST for the runtime deployment; singleton check passed with one sidecar process, one live process, and no snapshot writers.

An immediate post-restart `verify_production_services.py --json` sampled before startup/tick had fully settled and reported `live_tick_stale`. That was a timing false positive, not a startup deadlock: the same run then emitted `runtime.candidates_tradeable`, `scan.no_entry_diagnostics`, and private WS startup events. The settled check passed with `ok=true`, `critical_count=0`, `warning_count=0`, `risk_mode=running`, `lifecycle=running`, and all local position/pending/residual counts at `0`.

Remote harness was run on the deployed tree after adding the minimal test-only dependencies `pytest` and `pytest-asyncio` to the existing venv. The first attempt failed only because non-interactive SSH did not include `.venv/bin` in `PATH`; rerunning with `PATH=/opt/lightfee-v2/.venv/bin:$PATH` passed `scripts/validate_change.py --profile live-harness --keep-going` with `45 passed`.

Symbol-scoped probes are the acceptance evidence for live trading state:

- `RIVERUSDT` on Aster/OKX: exchange flat, no open orders, `gate_passed=true`.
- `BULLAUSDT` on Aster: exchange flat, no open orders, `gate_passed=true`.
- `MONUSDT` on Bybit: exchange flat, no open orders, `local_l2_official_rebuild_count=1` classified as official-doc behavior, `gate_passed=true`.
- `CHIPUSDT` on Binance/OKX: exchange flat, no open orders, no OKX recovery probe rate-limit/instrument-missing events in window, `gate_passed=true`.
