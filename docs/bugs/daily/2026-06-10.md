# Bug Ledger Daily - 2026-06-10

## GitNexus Index

| Cluster ID | Status | Severity | Fingerprint | Components | Symbols | Files | Commits | Outcome |
|---|---|---:|---|---|---|---|---|---|
| CL-065 | fixed locally, deploy pending | critical | `hyperliquid-clearinghouseState-signer-false-empty + configured-account-overwritten + diagnose-false-green-flat` | `venue-hyperliquid`, `diagnose-live`, `exchange-truth`, `production-health`, `v1-parity`, `exchange-docs` | `_normalize_hyperliquid_credential`, `_load_venue_credential`, `_build_exchange_truth_async`, `_hyperliquid_credential_identity` | `lightfee/venues/transport.py`, `scripts/diagnose_live.py`, `tests/test_venues_transport.py`, `tests/test_diagnose_live.py` | working tree | Production read-only truth showed the configured Hyperliquid account still had 18 nonzero positions, while V2 diagnose repeatedly queried the signer/API-wallet address and returned empty `assetPositions`, creating a false-green flat conclusion. Root cause: Hyperliquid credential normalization overwrote explicit `account_address` with the signer-derived address for account-wallet mode, and `diagnose_live.py` did not load `LIGHTFEE_HYPERLIQUID_WALLET_MODE`, so service-env diagnostics lost the configured account identity. V2 now preserves explicit account address truth, only derives account from the wallet when no account address is configured, loads wallet mode for diagnose, normalizes `agent_wallet` to `api_wallet`, fails closed on account-wallet signer/account mismatch for trading preflight, and emits sanitized credential identity so account/signer drift is visible without leaking secrets. Local RED/GREEN covered configured-account query, account-wallet mismatch fail-closed, private-key alias wallet mode, and sanitized exchange-truth identity; related diagnose/health suite reported `104 passed`, full Hyperliquid transport file reported `401 passed`, `git diff --check` passed, and GitNexus detect-changes reported low risk with no affected processes. Deployment acceptance must prove the cloud diagnose no longer says healthy/flat when Hyperliquid has nonzero positions. |
| CL-064 | fixed, deployed, cloud verified | critical | `pending-entry-terminal-no-fill-open-maker-order-owner-drop + both_venues_zero_abandon + recovery-release-with-live-open-order` | `live-runtime`, `pending-entry`, `entry-reconciliation`, `recovery-ledger`, `bybit`, `hyperliquid`, `v1-parity`, `production-risk` | `LiveRuntime._try_abandon_stale_entry`, `LiveRuntime._pending_entry_has_unresolved_maker_order`, `LiveRuntime._ensure_pending_entry_maker_not_open_before_abort`, `LiveRuntime._force_terminalize_pending_entry_if_budget_exhausted`, `LiveRuntime._abort_pending_entry_fail_closed`, `PendingEntryTerminalizer.decide_supervision_stale_clear` | `lightfee/engine/runtime.py`, `lightfee/engine/pending_entry_terminalizer.py`, `tests/test_live_entry_hedge_root_fix.py`, `tests/engine/test_pending_entry_terminalizer.py` | `66a3688` | Production showed local flat/no pending work while Bybit still had non-reduce-only maker open orders for `SUSHIUSDT` and `MEUSDT`. The run first recorded deferred maker-open-order evidence, then stale abandon with `reason=both_venues_zero` cleared the pending owner and refreshed recovery as if the account was only an evidence gap. Root cause: terminal/no-fill passive progress such as `execution_not_found` or `canceled` short-circuited maker terminality before realtime open-order truth, and abort cleanup also skipped the open-order guard when `pending.maker_completed()` was true. V2 now treats terminal no-fill as unresolved until realtime open-order truth is checked: matching open orders or unavailable open-order truth retain pending and keep recovery blocked; only explicit no matching open order can release the flat abandon path. The 2026-06-10 follow-up also closes PE-14 by putting V1 supervision stale-backlog clear behind terminalizer authority: zero-fill/resting/progress-absent/no-live-artifact may clear, while fill, inflight hedge, cancel, non-resting progress, live open order, live position, or unavailable truth retains pending. Follow-up PE-14 RED/GREEN reports `4 passed`, the terminalizer suite reports `11 passed`, and full pytest reports `3782 passed`, `9 skipped`, `1 warning`. Cloud deployed runtime commit `66a3688`; manifest critical files passed, `verify_production_services.py --json` returned `ok=true` with `0` critical and `0` warning, `diagnose_live.py --json --since-deploy` returned `healthy` / `risk=low` with `gate_passed=true`, all configured exchange truth was flat/no-open-orders, sidecar/live were active with `NRestarts=0`, and deploy-window warning logs were empty. No manual order cancel, order submit, or runtime-state edit was used. |
| CL-063 | fixed, deployed, cloud verified | high | `ws-bbo-entry-quote-evidence-post-only-reprice-snapshot-diagnose-split` | `live-runtime`, `entry-readiness`, `snapshot-freshness`, `post-only-bbo`, `diagnose-live`, `local-l2-legacy` | `LiveRuntime._candidate_snapshot_freshness_decisions`, `LiveRuntime._post_only_maker_bbo_guard`, `_build_l2_evidence`, `_build_snapshot_evidence`, `_build_conclusion` | `lightfee/engine/runtime.py`, `scripts/diagnose_live.py`, `tests/test_runtime_snapshot_freshness.py`, `tests/test_runtime_entry_flow.py`, `tests/test_diagnose_live.py` | `9e92718` | WS BBO production provider now uses fresh WS BBO quote evidence to resolve stale sidecar quote blockers before entry candidate filtering, while sidecar missing/invalid quote remains fail-closed and resolved quote metrics are recorded as fresh WS BBO evidence, not stale sidecar evidence. Post-only maker BBO guard keeps the official/V1 fail-closed semantics, but reprices one fresh WS BBO crossing maker quote to the current maker-side BBO before rejecting. Diagnose separates `snapshot_evidence` from `l2_evidence`, so `runtime.snapshot_stale` / `runtime.snapshot_degraded` no longer creates false Local L2 stale-rebuild recurrence. Cloud deployment verified on 2026-06-10: `.deploy_version=9e92718`, critical runtime hashes matched local, services active, singleton PASS, production services `ok=true`, since-deploy diagnose `healthy` / `risk=low`, exchange truth flat and no open orders, journal warning/error scan empty. |

## Cluster CL-065: Hyperliquid exchange-truth account identity false-green

### GitNexus Keys

- Fingerprint:
  `hyperliquid-clearinghouseState-signer-false-empty + configured-account-overwritten + diagnose-false-green-flat`
- Components:
  `venue-hyperliquid`, `diagnose-live`, `exchange-truth`,
  `production-health`, `v1-parity`, `exchange-docs`
- Symbols:
  `_normalize_hyperliquid_credential`,
  `_load_venue_credential`,
  `_build_exchange_truth_async`,
  `_hyperliquid_credential_identity`
- Watch:
  `exchange_truth.positions.hyperliquid`,
  `exchange_truth.credential_identity.hyperliquid.account_matches_signer`,
  `LIGHTFEE_HYPERLIQUID_ACCOUNT_ADDRESS`,
  `LIGHTFEE_HYPERLIQUID_WALLET_MODE`,
  `assetPositions=[]`,
  `diagnose_live healthy` with known Hyperliquid live positions

### Production Symptom

Cloud Hyperliquid had 18 positions that were not closed, but repeated
post-deploy checks reported no issue because the V2 exchange-truth probe was
reading the signer/API-wallet address instead of the configured trading
account address. Direct read-only Hyperliquid `/info` `clearinghouseState`
against the configured account returned nonzero positions; the signer-derived
address returned an empty `assetPositions` array. That made production
diagnose/health evidence look flat even though exchange truth was not flat.

### Root Cause

This is an exchange-truth identity bug, not a closing-strategy bug.
Hyperliquid account data queries are keyed by the actual master/sub-account
address. API/agent wallets can sign requests, but their signer address is not
the same thing as the account whose positions must be read.

V2 violated that contract in two places:

- `_normalize_hyperliquid_credential()` overwrote an explicit
  `account_address` with the address derived from `wallet_private_key` unless
  the credential was already normalized as `api_wallet`.
- `diagnose_live.py` loaded Hyperliquid keys and account aliases but did not
  load `LIGHTFEE_HYPERLIQUID_WALLET_MODE`, so service-env diagnostics could
  silently enter the wrong normalization branch.

The result was a false-green production gate: local recovered state and the
wrong Hyperliquid account both looked flat, while the configured account still
had live exposure.

### Fix

- Hyperliquid `api_wallet` / `agent_wallet` mode now preserves the configured
  account address and signer private key for exchange truth.
- Hyperliquid account-wallet mode now preserves an explicit `account_address`;
  it derives the account from `wallet_private_key` only when no account address
  is configured.
- Account-wallet trading preflight fails closed when configured account and
  signer-derived account differ, instead of authorizing a mismatched signer.
- `diagnose_live.py` loads `LIGHTFEE_<VENUE>_WALLET_MODE`, normalizes
  `agent_wallet` to `api_wallet`, and passes wallet mode into `LiveCredential`.
- Hyperliquid exchange truth now includes sanitized credential identity:
  masked account/signer, short hashes, wallet mode, and
  `account_matches_signer`, without leaking full addresses or private keys.

### RED / GREEN Evidence

| Scope | Command | Result |
|---|---|---|
| configured Hyperliquid account is used for exchange truth | `.venv/bin/pytest tests/test_diagnose_live.py -k hyperliquid_queries_configured_account_when_signer_differs -q` | failed before the fix, passed after |
| account-wallet mismatch fails closed | `.venv/bin/pytest tests/test_venues_transport.py -k account_wallet_preflight_fails_closed_on_signer_account_mismatch -q` | failed before the fix, passed after |
| diagnose loads Hyperliquid private-key alias and wallet mode | `.venv/bin/pytest tests/test_diagnose_live.py -k exchange_truth_loads_hyperliquid_private_key_alias -q` | passed |
| related diagnose / production health gates | `.venv/bin/pytest tests/test_venues_transport.py::TestHyperliquidLiveOrderNowSupported tests/test_diagnose_live.py tests/ops/test_production_health.py -q` | `104 passed` |
| full venue transport regression | `.venv/bin/pytest tests/test_venues_transport.py -q` | `401 passed` |
| hygiene / impact | `git diff --check`; `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | diff clean; low risk, affected processes `0` |

### Acceptance

Local acceptance is closed; cloud acceptance is expected to be non-green until
the existing Hyperliquid positions are handled by the live recovery/close path
or by explicit operator action. The important acceptance condition for this
fix is narrower and safety-critical: after deployment, diagnose must query the
configured Hyperliquid account and surface the 18 nonzero positions instead of
reporting a false healthy/flat state from the signer address.

## Cluster CL-064: Pending-entry terminal no-fill maker open-order owner retention

### GitNexus Keys

- Fingerprint:
  `pending-entry-terminal-no-fill-open-maker-order-owner-drop + both_venues_zero_abandon + recovery-release-with-live-open-order`
- Components:
  `live-runtime`, `pending-entry`, `entry-reconciliation`,
  `recovery-ledger`, `bybit`, `hyperliquid`, `v1-parity`,
  `production-risk`
- Symbols:
  `LiveRuntime._try_abandon_stale_entry`,
  `LiveRuntime._pending_entry_has_unresolved_maker_order`,
  `LiveRuntime._ensure_pending_entry_maker_not_open_before_abort`,
  `LiveRuntime._force_terminalize_pending_entry_if_budget_exhausted`,
  `LiveRuntime._abort_pending_entry_fail_closed`
- Watch:
  `pending_entry.finalize_deferred_maker_open_order`,
  `pending_entry.maker_open_order_retained`,
  `pending_entry.maker_terminal_evidence_unavailable`,
  `reconciliation.entry_abandon_retained_unresolved_maker`,
  `reconciliation.entry_abandoned_flat`,
  `recovery.ledger_clear`,
  `orphan_maker_order`,
  `exchange_truth_mismatch`,
  `live_open_order`

### Production Symptom

Production read-only evidence showed local `open_position`, `pending_entry`,
`pending_close`, and residual state flat, while Bybit still had two
non-reduce-only maker open orders:

- `SUSHIUSDT` buy `144.2 @ 0.1664`,
  order `f37adbb4-caa1-4044-9e36-ba897bbba795`;
- `MEUSDT` buy `408.0 @ 0.0588`,
  order `57a9c1b4-0b73-4a73-ad24-080a424f2ed5`.

The same run first emitted deferred maker-open-order evidence, then later
emitted `reconciliation.entry_abandoned_flat` with
`reason=both_venues_zero` and refreshed recovery as if the account only had an
evidence gap. That dropped the pending owner before exchange open-order truth
proved the maker artifact was gone.

### Root Cause

This is a pending-entry terminality/live-truth recurrence, not a RecoveryLedger
classification bug. V2 had already learned that missing passive progress and
unavailable open-order truth must retain pending work, but one bypass remained:
when `query_passive_order_progress()` returned terminal no-fill progress
(`execution_not_found` represented as `canceled` / no fill), V2 treated
`pending.maker_completed()` or terminal passive state as proof that the maker
order could no longer be live.

That was too weak for Bybit-like eventual consistency. Execution history can
return no fill while `/v5/order/realtime` still returns the live maker order.
V1's semantic contract is that stale pending-entry clearing requires stronger
terminal exchange evidence; local flat position and terminal/no-fill order
history alone do not prove that a maker owner is gone.

### Fix

- `_pending_entry_has_unresolved_maker_order()` no longer returns terminal for
  `pending.maker_completed()` unless the maker fill quantity proves the maker
  target was actually filled.
- Terminal no-fill passive progress now checks realtime open-order truth. A
  matching order id or client id retains pending and emits
  `pending_entry.maker_open_order_retained`.
- If realtime open-order truth is unavailable, the runtime retains pending and
  emits `pending_entry.maker_terminal_evidence_unavailable`.
- Only explicit open-order truth with no matching maker order allows the
  terminal no-fill flat abandon path to continue.
- `_ensure_pending_entry_maker_not_open_before_abort()` uses the same
  actual-fill-only fast path before abort cleanup, so a canceled/rejected
  no-fill pending entry cannot bypass the maker-open-order guard.
- `_abort_pending_entry_fail_closed()` latches operator-requested fail-closed
  before abort cleanup so a subsequent recovery refresh cannot immediately
  clear the fail-closed state during the same abort path.

### RED / GREEN Evidence

Follow-up PE-14 root-closure evidence:

- RED first: `tests/engine/test_pending_entry_terminalizer.py -k "supervision_stale_clear"` failed with `AttributeError` because the supervision stale-clear authority did not exist.
- GREEN: `tests/engine/test_pending_entry_terminalizer.py -k "supervision_stale_clear" -q` -> `4 passed`.
- Terminalizer regression: `tests/engine/test_pending_entry_terminalizer.py -q` -> `11 passed`.
- Contract update: PE-14 is now covered locally in `docs/bugs/contracts/pending-entry-live-truth-contract.md`.

| Scope | Command | Result |
|---|---|---|
| RED / GREEN - production-shaped terminal no-fill with matching open order | `.venv/bin/pytest tests/test_live_entry_hedge_root_fix.py -k "terminal_no_fill_when_open_order_matches or terminal_pending_entry_open_order_match_does_not_clear_recovery_core"` | failed before the fix, then passed |
| focused terminal no-fill/open-order branches | `.venv/bin/pytest tests/test_live_entry_hedge_root_fix.py -k "terminal_no_fill_when_open_order_matches or terminal_no_fill_when_open_order_truth_unavailable or terminal_pending_entry_open_order_match_does_not_clear_recovery_core or allows_terminal_maker_order or allows_missing_progress_when_open_order_absent or requires_cancel_before_missing_progress_no_open_order_abandon"` | `6 passed` |
| recovery core and ledger | `.venv/bin/pytest tests/engine/test_recovery_decision_core.py tests/engine/test_recovery_ledger.py` | `34 passed` |
| target regression suite | `.venv/bin/pytest tests/test_pending_entry_v1_semantic_drift.py tests/test_live_entry_hedge_root_fix.py tests/test_venues_transport.py tests/engine/test_recovery_decision_core.py` | `609 passed` |
| compile | `python -m compileall lightfee scripts tests` | PASS |
| full suite | `.venv/bin/pytest` | `3782 passed`, `9 skipped`, `1 warning` |

### Acceptance

Local and production acceptance are closed for runtime commit `66a3688`.

- Local gates: PE-14 RED/GREEN `4 passed`; terminalizer suite `11 passed`;
  target pending-entry/recovery/Bybit regression gates passed; compileall
  passed; full pytest reported `3782 passed`, `9 skipped`, `1 warning`.
- Cloud deploy: `/opt/lightfee-v2` was updated to `66a3688`, `.deploy_version`
  was `66a3688`, and deploy manifest critical-file verification passed. The
  remote git metadata was aligned with `git -c safe.directory=/opt/lightfee-v2`
  because the remote subprocess otherwise hit Git's dubious-ownership guard.
- Production services: `lightfee-live.service` and
  `lightfee-sidecar.service` were `active/running`, both with `NRestarts=0`,
  and deploy-window `journalctl -p warning` had no entries.
- Production verifier: `scripts/verify_production_services.py --json`
  returned `ok=true`, `critical_count=0`, `warning_count=0`,
  `RUNNING_CLEAN`, and no `exchange_truth_mismatch` / `live_open_order`.
- Since-deploy diagnose: `scripts/diagnose_live.py --json --since-deploy`
  returned `healthy`, `risk=low`, `gate_passed=true`, high-confidence
  exchange truth, and all configured venues flat/no-open-orders. The checked
  venues included `binance`, `bybit`, `aster`, `okx`, `bitget`, `gate`, and
  `hyperliquid`; open-order truth was empty for the account, covering
  `SUSHIUSDT`, `MEUSDT`, `MOVEUSDT`, `STABLEUSDT`, `TRXUSDT`, and
  `LAYERUSDT`.
- No manual order submission, order cancellation, or runtime-state edit was
  used. The pre-deploy cloud dirty runtime/diagnose/test diff was classified
  as the already-known CL-063 WS BBO/post-only/snapshot-diagnose deployment
  line; after alignment, runtime dirty diff was gone and only generated
  manifest/docs-excluded artifacts remained.

## Cluster CL-063: WS BBO entry quote evidence and snapshot diagnose split

### GitNexus Keys

- Fingerprint:
  `ws-bbo-entry-quote-evidence-post-only-reprice-snapshot-diagnose-split`
- Components:
  `live-runtime`, `entry-readiness`, `snapshot-freshness`,
  `post-only-bbo`, `diagnose-live`, `local-l2-legacy`
- Symbols:
  `LiveRuntime._candidate_snapshot_freshness_decisions`,
  `LiveRuntime._ws_bbo_entry_quote_resolution`,
  `LiveRuntime._post_only_maker_bbo_guard`,
  `_build_l2_evidence`, `_build_snapshot_evidence`,
  `_build_conclusion`
- Watch:
  `runtime.quote_stale`,
  `runtime.entry_quote_evidence_resolved_by_ws_bbo`,
  `runtime.entry_blocked_ws_bbo_selection`,
  `runtime.entry_blocked_post_only_bbo`,
  `runtime.entry_post_only_bbo_repriced`,
  `runtime.snapshot_stale`,
  `runtime.snapshot_degraded`,
  `l2_evidence.stale_rebuild_count`,
  `snapshot_evidence.stale_or_degraded_count`

### Production Symptom

Post-deploy review showed no service crash and no current Local L2 mainline
blocker, but the deploy window still had several safety/diagnostic events:

- `runtime.quote_stale` from sidecar quote evidence just over the quote budget;
- low-volume `runtime.entry_blocked_ws_bbo_selection`;
- one `runtime.entry_blocked_post_only_bbo`;
- `runtime.snapshot_fallback_last_good` / `runtime.snapshot_degraded`;
- `l2_evidence.stale_rebuild_count=1` even though no
  `runtime.entry_blocked_local_l2_selection` recurrence existed.

### Root Cause

This is a CL-061 follow-up, not a rollback to Local L2. Production already uses
`entry_readiness_provider=ws_bbo_quote_lease`; the remaining gap was quote
evidence ownership and diagnosis vocabulary:

- candidate snapshot freshness still treated stale sidecar quotes as direct
  entry blockers even when the active provider had fresher WS BBO for the same
  venue and symbol;
- post-only BBO protection correctly blocked would-take maker prices, but did
  not first reprice from the same fresh WS BBO evidence;
- `diagnose_live.py` counted `runtime.snapshot_stale` and
  `runtime.snapshot_degraded` as Local L2 stale rebuild evidence.

V1 and exchange-doc semantics still require fail-closed behavior for new
opening risk when fresh BBO is unavailable. Bybit PostOnly orders may be
cancelled if they would execute immediately, and Hyperliquid documents
post-only / ALO price errors separately from quote-provider health. Therefore
the fix reduces false blockers and false Local L2 attribution without deleting
the entry safety gates.

### Fix

- Added a source-aware WS BBO entry quote resolver. Under
  `ws_bbo_quote_lease`, stale sidecar quote evidence is resolved only when the
  WS BBO cache has fresh valid BBO for the same venue/symbol. The runtime
  records `runtime.entry_quote_evidence_resolved_by_ws_bbo`, marks that quote
  metric as fresh WS BBO evidence, and keeps the candidate in the entry path.
- Sidecar missing/invalid quote remains fail-closed even if WS BBO has a fresh
  quote. If WS BBO evidence is missing, stale, invalid, or outside the
  quote-lease age budget, stale sidecar quote evidence also remains
  fail-closed through the existing quote-stale path.
- The WS BBO post-only guard now attempts one maker-side reprice when a fresh
  BBO proves the current maker price would cross. BUY reprices to best bid;
  SELL reprices to best ask. If the repriced price would still cross, or BBO
  evidence is missing/stale/invalid, the guard still emits
  `runtime.entry_blocked_post_only_bbo`.
- Dispatch writes the repriced maker hint back to the eventual order context and
  emits nonblocking `runtime.entry_post_only_bbo_repriced` evidence.
- `diagnose_live.py` now returns `snapshot_evidence` separately. Local L2
  evidence no longer counts snapshot stale/degraded events, and conclusion
  next-actions say "snapshot stale/degraded" instead of "L2 stale/rebuild" when
  there is no true Local L2 event.

### RED / GREEN Evidence

| Scope | Command | Result |
|---|---|---|
| RED / GREEN - WS BBO resolves sidecar stale quote | `.venv/bin/pytest tests/test_runtime_snapshot_freshness.py::test_ws_bbo_provider_resolves_stale_sidecar_quote_for_entry_freshness tests/test_runtime_snapshot_freshness.py::test_ws_bbo_provider_keeps_entry_fail_closed_when_bbo_cannot_resolve_stale_sidecar_quote -q` | `2 passed` after implementation |
| RED / GREEN - post-only repricing | `.venv/bin/pytest tests/test_runtime_entry_flow.py -k "reprices_crossing_maker_quote_once" -q` | `1 passed, 54 deselected` after implementation |
| RED / GREEN - snapshot/L2 diagnose split | `.venv/bin/pytest tests/test_diagnose_live.py::test_snapshot_stale_and_degraded_are_reported_outside_l2_evidence tests/test_diagnose_live.py::test_l2_missing_tick_stats_tracked -q` | `2 passed` after implementation |
| review follow-up - stale-only WS BBO resolver and metric evidence | `.venv/bin/pytest tests/test_runtime_snapshot_freshness.py::test_ws_bbo_provider_resolves_stale_sidecar_quote_for_entry_freshness tests/test_runtime_snapshot_freshness.py::test_ws_bbo_provider_does_not_resolve_missing_or_invalid_sidecar_quote -q` | `2 passed` |
| adjacent snapshot freshness | `.venv/bin/pytest tests/test_runtime_snapshot_freshness.py -q` | `31 passed` |
| adjacent entry WS BBO/post-only | `.venv/bin/pytest tests/test_runtime_entry_flow.py -k "ws_bbo or post_only_bbo or quote_lease" -q` | `8 passed, 47 deselected` |
| adjacent diagnose | `.venv/bin/pytest tests/test_diagnose_live.py -q` | `56 passed` |
| entry / WS BBO | `.venv/bin/pytest tests/test_entry_local_l2.py tests/test_ws_bbo.py -q` | `152 passed` |
| snapshot / passive close | `.venv/bin/pytest tests/test_runtime_snapshot_freshness.py tests/test_passive_close.py -q` | `156 passed` |
| diagnose / offline | `.venv/bin/pytest tests/test_diagnose_live.py tests/test_offline_analysis.py -q` | `104 passed` |
| live harness business lines | `.venv/bin/pytest tests/live_harness -k "snapshot or passive or residual or admission or recovery" -q` | `85 passed, 14 deselected` |
| entry execution focused | `.venv/bin/pytest tests/test_runtime_entry_flow.py -k "ws_bbo or post_only_bbo or quote_lease" -q` | `8 passed, 47 deselected` |
| compile | `python -m compileall lightfee scripts` | PASS |
| GitNexus change scope | `gitnexus detect-changes --repo LightFeeV2 --scope all` | `6 files, 32 symbols`, `6` affected processes, `high` risk |

### Acceptance

Local focused acceptance passed. Cloud deployment acceptance also passed for
code commit `9e92718`: remote `.deploy_version=9e92718`, critical runtime
file hashes matched local, `lightfee-sidecar.service` and
`lightfee-live.service` were active after restart, singleton check passed,
`verify_production_services.py --json` returned `ok=true` with zero critical
and warning reports, and `diagnose_live.py --json --since-deploy` returned
`healthy` / `risk=low`. Exchange truth was high confidence, all venues were
flat, all venues had no open orders, `l2_evidence.stale_rebuild_count=0`,
`snapshot_evidence.stale_or_degraded_count=0`, and the service journal warning
/ error scan for the deploy window had no entries.

GitNexus refresh was attempted before edits
but the local GitNexus npx cache has a native `tree-sitter-dart` binding
startup failure under both Node 26 and Node 22, so the index remains at
`af40809` while local `HEAD` is `27059b4`. `detect-changes` ran and reported a
high-risk scope, but the implementation still used manual caller/symbol
impact review and should rerun full GitNexus freshness after the toolchain is
repaired.

Future deployment acceptance must continue proving:

- `runtime.entry_quote_evidence_resolved_by_ws_bbo` appears only for fresh WS
  BBO resolution and does not hide missing/stale WS BBO entry blockers;
- `runtime.entry_blocked_post_only_bbo` remains for true stale/missing/invalid
  or still-crossing BBO;
- `l2_evidence.stale_rebuild_count` stays zero unless a true Local L2 event
  occurs;
- `snapshot_evidence` reports snapshot stale/degraded separately;
- production remains running, flat, no open orders, and no pending-entry,
  passive-close, residual-repair, or recovery blocker.
