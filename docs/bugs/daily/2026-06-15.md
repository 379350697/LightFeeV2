# Bug Ledger Daily - 2026-06-15

## GitNexus Index

| Cluster ID | Status | Severity | Fingerprint | Components | Symbols | Files | Commits | Outcome |
|---|---|---:|---|---|---|---|---|---|
| CL-081 | fixed, deployed; current cloud watch | critical | `unified-order-truth-resolver + ack-detail-not-fill + lifecycle-event-closure` | `order-truth-ledger`, `pending-entry`, `passive-close`, `venue-reconciliation`, `v1-lifecycle-closure`, `diagnose-health` | `OrderTruthLedger.resolve_order_success`, `LiveRuntime._recover_drive_missing_hedge`, `LiveRuntime._drive_missing_hedge_live`, `LiveRuntime._ensure_pending_entry_open_fill_details`, `PassiveCloseExecutor._submit_hedge_for_delta`, `map_lifecycle_event_kind`, `build_v1_lifecycle_closure_table` | `lightfee/engine/order_truth_ledger.py`, `lightfee/engine/runtime.py`, `lightfee/engine/passive_close.py`, `lightfee/engine/v1_lifecycle_closure.py`, `tests/test_order_submit_uncertainty.py`, `tests/engine/test_v1_lifecycle_closure_table.py`, `docs/bugs/daily/2026-06-15.md`, `docs/bugs/BUG_INDEX.md` | `5fa66ae`, `3be7210`, `d22ada2`, `571f832`, deployed via `793d28d` | CL-078/CL-079/CL-080 closed individual HOME false-position layers, but the underlying order-success decision was still distributed across entry recovery, pending finalize, passive close ACK reconciliation, duplicate cleanup, diagnose, and health surfaces. CL-081 introduces `OrderTruthLedger.resolve_order_success()` as the shared local semantic gate: fill/execution truth can produce `confirmed_fill`; ACK/order-detail/realtime/open-status evidence without fill truth becomes `truth_gap` or `unsupported_fail_closed`; lifecycle closure maps the newly observed events instead of leaving them as unmapped diagnostics. Cloud `/opt/lightfee-v2` is on `793d28d911508d6e5791521129190982ea126da9` and manifest verification passed. The pending auto-repair evidence is closed for the old recurrence, but a fresh docs-backfill recheck saw a new HOME matched open position, so current cloud acceptance is a watch state until that active exposure closes. |

## Cluster CL-081: Unified order truth resolver and lifecycle closure event mapping

### GitNexus Keys

- Fingerprint:
  `unified-order-truth-resolver + ack-detail-not-fill +
  lifecycle-event-closure`
- Components:
  `order-truth-ledger`, `pending-entry`, `passive-close`,
  `venue-reconciliation`, `v1-lifecycle-closure`, `diagnose-health`
- Symbols:
  `OrderTruthLedger.resolve_order_success`,
  `LiveRuntime._recover_drive_missing_hedge`,
  `LiveRuntime._drive_missing_hedge_live`,
  `LiveRuntime._ensure_pending_entry_open_fill_details`,
  `PassiveCloseExecutor._submit_hedge_for_delta`,
  `map_lifecycle_event_kind`, `build_v1_lifecycle_closure_table`
- Watch:
  `accepted_ack_without_execution`, `detail_found;fills_empty`,
  `stale_accepted_order`, `positive_quantity_missing_side`,
  `account_identity_mismatch`,
  `exit.accepted_order_truth_gap_registered`,
  `exit.pending_close_reconciliation_registered`, `exit.reconciled`,
  `runtime.position_drift_corrected`,
  `reconciliation.entry_abandon_retained_unresolved_maker`,
  `reconciliation.entry_flat_not_found_terminal_cleared`

### Summary

The HOME fixes on 2026-06-14 proved the immediate live artifact can be owned and
cleaned, but the evidence stack still had a systemic weakness: different
runtime surfaces could interpret order status, ACK, local fill fields, live
position truth, and fill/execution reconciliation independently. That made it
too easy for an exchange-specific partial fix to close one symptom while a
neighboring lifecycle path kept treating weak evidence as success or terminal
flatness.

CL-081 makes order success a shared resolver decision. Consumers must read an
explicit result instead of inferring truth from `None`, ACK presence, order
detail status, or positive non-fill counters.

The follow-up pending auto-repair audit keeps the proof boundary explicit:
positive-fill/live-truth cleanup is production-proven by the cloud
`positive_fill_live_truth_conflict -> owned_live_conflict_cleanup_attempt ->
owned_live_conflict_cleanup_succeeded -> removed_by_v1_lifecycle_closure`
event chain. Zero-fill live-artifact cleanup is closed in code and tests, and
the deployed cloud line contains that fix, but the latest post-deploy window did
not contain a fresh zero-fill sample that hit the specialized direct-cleanup
event path. The stale HOME zero-fill artifact was cleared through the abort
cleanup path and recovery core returned to `RUNNING_CLEAN`. A later read-only
docs-backfill recheck saw a separate HOME matched open position, so the docs
must not overstate either direct production exercise of the zero-fill-specific
branch or current flat/no-open-orders acceptance.

### Root Cause

The old shape had two layers of drift:

1. Venue adapters were moving toward official exchange evidence, but runtime
   callers still had local predicates like `reconciliation is not None and
   quantity > 0`. That predicate is only safe if every adapter always guarantees
   the reconciliation came from a fill/execution endpoint. It is unsafe when an
   exchange order detail can contain cumulative quantities without matching
   fills, or when ACK/realtime status is accepted but not executed.
2. Lifecycle closure had new production events that were meaningful, but not
   mapped. Unmapped events made diagnose/health reason about evidence closure
   differently from runtime cleanup and pending-entry retention.

### Exchange Evidence Matrix

| Venue | Fill success evidence | Weak evidence that cannot confirm fill | Fail-closed / truth-gap rule |
|---|---|---|---|
| Binance / Aster | `FILLED` plus positive executed quantity, or adapter-normalized fill truth | `NEW`, zero executed quantity, ACK/order status alone | `NEW/zero` remains `truth_gap` until open-order/live-position truth closes it. |
| OKX | `/api/v5/trade/fills` or fills-history with `ctVal` conversion where needed | `/trade/order` or history detail with positive `accFillSz` but empty fills | Empty fills or missing `ctVal` is `truth_gap`, not a fake maker fill. |
| Bybit | `/v5/execution/list` positive execution quantity | ACK, `/v5/order/realtime`, or order history without execution | ACK/realtime-only remains pending reconciliation. |
| Bitget | Family-resolved fill truth with valid side | Detail quantity without side or unresolved account family | Missing side/family is `unsupported_fail_closed`. |
| Gate | Order detail/open-order/position truth jointly proving fill or no-fill | Ambiguous detail without fill/open/live closure | Ambiguity remains `truth_gap`. |
| Hyperliquid | Configured account `orderStatus`/historical order/open-order/clearinghouse evidence with unambiguous oid/cloid | Agent/account mismatch or unclear oid/cloid | Account identity mismatch is `unsupported_fail_closed` / `truth_gap`. |

### Fix

- Added `OrderTruthFillStatus`, `OrderTruthEvidenceStatus`, and
  `OrderTruthSuccessDecision` to the shared order truth ledger.
- Added `OrderTruthLedger.resolve_order_success()` as the single local decision
  point for order success evidence:
  - `confirmed_fill` requires positive fill/execution reconciliation.
  - `truth_gap` retains work for ACK/detail/realtime evidence without fill truth.
  - `unsupported_fail_closed` covers missing side, unresolved account family,
    or Hyperliquid account identity mismatch.
  - `live_position_present` and `live_flat` are explicit outputs, not implied
    by `None`.
- Runtime pending-entry hedge recovery, live hedge driving, and finalize-open
  reconciliation now consume the resolver. Weak reconciliation can add evidence
  and retain pending work, but cannot mutate leg-filled state as a confirmed
  open.
- Pending-entry live-truth cleanup remains centralized in the finalizer path:
  unresolved live artifacts route through the shared owned single-leg cleanup
  helper, and pending removal still goes through the V1 lifecycle closure
  removal authority rather than scattered `pending_entries.pop()` calls.
- Passive close ACK/error reconciliation now consumes the same resolver before
  recording a filled hedge. ACK-only and realtime-only evidence registers
  pending reconciliation rather than terminal success.
- Lifecycle closure maps the newly observed production events:
  `exit.accepted_order_truth_gap_registered`,
  `exit.pending_close_reconciliation_registered`, `exit.reconciled`,
  `runtime.position_drift_corrected`, and
  `reconciliation.entry_abandon_retained_unresolved_maker`.
- The docs-backfill cloud recheck exposed one more mapped-pending-entry event:
  `reconciliation.entry_flat_not_found_terminal_cleared` is the flat/not-found
  branch after `_finalize_pending_entry()` authoritatively clears a zero-fill
  pending entry, so it maps to `PENDING_ENTRY` rather than remaining an unmapped
  lifecycle diagnostic.

### Non-Changes

- No strategy threshold, quote/OI threshold, sizing, leverage, order-submit,
  cancel, close lifecycle, residual-repair lifecycle, or cloud trading-state
  mutation changed.
- No production order was submitted or cancelled during this local fix.
- This does not weaken CL-078/CL-079/CL-080 live-truth cleanup rules; it moves
  their evidence assumptions into one reusable resolver.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| local RED | `tests/test_order_submit_uncertainty.py -k "order_truth_resolution"` before implementation | Import failed for missing `OrderTruthEvidenceStatus`, proving the resolver contract did not exist. |
| local RED | `tests/engine/test_v1_lifecycle_closure_table.py -k recent_cloud_event_kinds` before implementation | Failed with five `unmapped_event_kinds`. |
| local GREEN | `.venv/bin/pytest tests/test_order_submit_uncertainty.py -q` | `17 passed`; weak OKX/Bybit/Binance evidence cannot confirm fill, Bitget/Hyperliquid fail closed, Bybit execution-list fill confirms. |
| local GREEN | `.venv/bin/pytest tests/engine/test_v1_lifecycle_closure_table.py -q` | `19 passed`; new production event kinds map to lifecycle phases. |
| local GREEN | `.venv/bin/pytest tests/test_venues_transport.py tests/test_pending_entry_v1_semantic_drift.py tests/test_passive_close.py tests/engine/test_v1_lifecycle_closure_table.py tests/test_diagnose_live.py tests/ops/test_production_health.py tests/test_live_entry_hedge_root_fix.py -q` | `912 passed`; transport, pending-entry, passive-close, lifecycle, diagnose, health, and live-entry hedge root regressions stayed green. |
| local GREEN | `.venv/bin/pytest tests/test_pending_entry_v1_semantic_drift.py tests/test_order_submit_uncertainty.py tests/engine/test_v1_lifecycle_closure_table.py tests/test_diagnose_live.py tests/ops/test_production_health.py -q` | 2026-06-15 docs-backfill recheck: `204 passed`; pending-entry drift, shared order truth, lifecycle closure, diagnose, and production health stayed green. |
| GitNexus GREEN | `npx gitnexus status` | Indexed and current commit both `793d28d`; repository status up to date before docs-only backfill. |
| cloud DEPLOYED | `scripts/verify_deploy_manifest.py --check /opt/lightfee-v2` | Remote `/opt/lightfee-v2` is `793d28d911508d6e5791521129190982ea126da9`, `.deploy_version=793d28d`, and manifest verification passed. |
| cloud WATCH | `scripts/verify_production_services.py --json` | Fresh docs-backfill recheck found live/sidecar active but current state not settled-green because a new HOME matched open position was active: `open_position_count=1`, `pending_entry_count=0`, `pending_entry_live_conflicts.count=0`, lifecycle `risk_only`. |
| cloud WATCH | `scripts/diagnose_live.py --json --since-deploy` | Fresh diagnose showed `gate_passed=false` because of `local_open_positions_present`, `exchange_truth_nonzero_position`, and `entry_or_position_opened_without_fixture_finalized_evidence`; this is a new active HOME open position, not a pending-entry live-conflict recurrence. Deployed code also reported unmapped `reconciliation.entry_flat_not_found_terminal_cleared`, now covered locally as a lifecycle mapping follow-up. |
| cloud EVIDENCE | `grep -F entry-1781463230496-HOMEUSDT runtime/live-events.jsonl` | The new HOME entry followed a strong matched-open chain: maker progress filled `1500`, hedge result filled `1500`, terminalizer outcome `open_position`, `entry.opened`, `pending_entry.pending_entry_finalized`, `runtime.position_opened`, then `pending_entry.removed_by_v1_lifecycle_closure`. |
| local RED | `.venv/bin/pytest tests/engine/test_v1_lifecycle_closure_table.py -k recent_cloud_event_kinds -q` after adding `reconciliation.entry_flat_not_found_terminal_cleared` | Failed with that single unmapped event kind, reproducing the docs-backfill cloud diagnostic gap. |
| local GREEN | `.venv/bin/pytest tests/engine/test_v1_lifecycle_closure_table.py -k recent_cloud_event_kinds -q` | `1 passed, 18 deselected`; `reconciliation.entry_flat_not_found_terminal_cleared` now maps to `PENDING_ENTRY`. |
| local GREEN | `git diff --check`, `.venv/bin/python -m compileall -q lightfee tests scripts`, `npx gitnexus detect-changes --repo LightFeeV2 --scope all` | Diff check and compileall passed; GitNexus reported low risk, `5` changed files, `14` changed symbols, and `0` affected processes. |

### Acceptance

CL-081 is accepted for the deployed `793d28d` code path and old pending
auto-repair recurrence evidence. Current cloud acceptance remains in watch while
the new HOME open position is active: it is not a pending-entry live-conflict
regression, but it prevents a flat/no-open-orders production-green claim in this
docs-backfill pass. Future recurrences must preserve the same semantic boundary:
positive-fill/live-truth conflicts may release pending only after reduce-only
cleanup plus fresh flat truth, and zero-fill live artifacts must not be reported
as production-proven direct-cleanup cases until a fresh deployed window actually
emits that specialized path.
