# 2026-05-31 Production Regression Notes

## GitNexus Index

GitNexus CLI impact analysis was run before editing the affected symbols.
`build_open_position` and `LiveRuntime._dispatch_entry` reported HIGH blast
radius because they sit on the main entry path; the fix below is intentionally
scoped to preserving already-selected funding lifecycle semantics and refusing
to treat missing funding timestamps as elapsed settlement evidence.

| Cluster ID | Status | Severity | Fingerprint | Components | Symbols | Files | Commits | Outcome |
|---|---|---:|---|---|---|---|---|---|
| CL-021 | fixed, deployed, cloud verified | critical | `v2.staggered-first-stage-funding-captured-not-advanced + exit-after-first-stage-config-not-persisted + recovered-prl-open-held-past-first-funding` | `live-runtime`, `entry-dispatch`, `normal-exit`, `funding-capture-state`, `v1-parity` | `LiveRuntime._dispatch_entry`, `LiveRuntime._maybe_process_normal_exits`, `standard_close_reason`, `update_position_funding_capture_state` | `lightfee/engine/runtime.py`, `tests/test_runtime_entry_flow.py` | `c66f470` after production `7704238` | Local RED/GREEN reproduced the post-CL-020 PRL hold: first funding had elapsed, but runtime did not call V1 `update_position_funding_capture_state()` before `standard_close_reason()`, and `staggered_exit_mode=after_first_stage` was not copied into `EntryContext.exit_after_first_stage`. Fix wires both V1 semantics and backfills recovered stale open positions from strategy config before close decision. Cloud deployed `c66f470`; `PRLUSDT` backfilled first-stage exit semantics, advanced funding state, routed `first_stage_capture`, and cleared flat with no pending close/residual work. |
| CL-020 | fixed, deployed, cloud verified | critical | `v2.entry-open-position.funding-timestamp-zero + funding-capture-before-settlement + candidate-opportunity-type-dropped` | `live-runtime`, `entry-sync`, `exit-decision`, `state-persistence`, `v1-parity` | `LiveRuntime._dispatch_entry`, `LiveRuntime._finalize_pending_entry`, `EntryContext`, `build_open_position`, `PendingEntry`, `standard_close_reason`, `aligned_settlement_delay_elapsed`, `force_close_due`, `update_position_funding_capture_state` | `lightfee/engine/runtime.py`, `lightfee/engine/entry.py`, `lightfee/engine/entry_sync.py`, `lightfee/engine/exit_decision.py`, `lightfee/engine/state.py`, `lightfee/engine/recovery.py`, `tests/test_exit_decisions.py`, `tests/engine/test_entry_semantic_parity.py`, `tests/test_runtime_entry_flow.py`, `tests/persistence/test_v1_state_snapshot_semantics.py` | `053cc8a` after production `d3e8d12` | Local RED/GREEN reproduced MAGMA-style early `funding_capture`: missing funding timestamp on `OpenPosition` made aligned close timing treat `0 + delay` as already elapsed. Fix preserves candidate funding lifecycle fields through runtime dispatch, pending entry state, pending finalization, open-position construction, and snapshot restore; zero funding timestamps now fail closed. Cloud deployed `053cc8a`, focused regression passed, services are active with `NRestarts=0`, health is ok, and a real post-deploy `PRLUSDT` open carries first/second funding timestamps instead of closing immediately. |

## Cluster CL-021-staggered-first-stage-capture-runtime-drift

### GitNexus Keys

- Fingerprint: `v2.staggered-first-stage-funding-captured-not-advanced + exit-after-first-stage-config-not-persisted + recovered-prl-open-held-past-first-funding`
- Components: `live-runtime`, `entry-dispatch`, `normal-exit`, `funding-capture-state`, `v1-parity`
- Symbols: `LiveRuntime._dispatch_entry`, `LiveRuntime._maybe_process_normal_exits`, `standard_close_reason`, `update_position_funding_capture_state`
- Files: `lightfee/engine/runtime.py`, `tests/test_runtime_entry_flow.py`
- Watch: `runtime.funding_capture_state_updated`, `runtime.staggered_exit_mode_backfilled`, `runtime.normal_close_routing_passive reason=first_stage_capture`, `entry.opened.exit_after_first_stage=true`

### Summary

After CL-020, production correctly stopped closing before settlement, but the
post-deploy `PRLUSDT` position remained open after first funding settlement:

- It opened at `2026-05-31 02:56:25.971 CST`.
- First funding timestamp was `2026-05-31 03:00:00 CST`.
- Second funding timestamp was `2026-05-31 04:00:00 CST`.
- Production config had `staggered_exit_mode=after_first_stage`.
- The persisted position had `exit_after_first_stage=false` and
  `funding_captured=false`, so `standard_close_reason()` returned `None`.

Root cause:

- V2 had the V1 pure helper `update_position_funding_capture_state()`, but
  runtime normal-exit ticks never called it before `standard_close_reason()`.
- `LiveRuntime._dispatch_entry` copied candidate funding timestamps into
  `EntryContext`, but did not copy the V1 config-derived
  `staggered_exit_mode=after_first_stage` decision into
  `EntryContext.exit_after_first_stage`.
- Existing recovered positions opened before the fix could keep the stale
  `exit_after_first_stage=false` latch even though the current V1 config says
  to exit after first stage.

### V1 Decision

This is another V2/V1 semantic drift. V1 updates funding-capture state during
normal runtime housekeeping before evaluating close reasons, and staggered
first-stage exit is a strategy-config decision that must be present on the open
position used by close decision logic.

### Fix

- `LiveRuntime._dispatch_entry` now sets `EntryContext.exit_after_first_stage`
  from `strategy.staggered_exit_mode == "after_first_stage"` for staggered
  candidates and includes the value in entry-selection evidence.
- `LiveRuntime._maybe_process_normal_exits` now backfills recovered staggered
  positions from the same config when the persisted latch is missing, with
  `runtime.staggered_exit_mode_backfilled` evidence.
- The same normal-exit path now calls V1 `update_position_funding_capture_state()`
  before `standard_close_reason()`, and emits
  `runtime.funding_capture_state_updated` whenever stage state changes.
- No close reason ordering changed: `standard_close_reason()` remains the V1
  authority for first-stage and second-stage close decisions.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| local RED | New PRL-style tests before the fix: dispatch must set `exit_after_first_stage`; normal exit must advance funding state and route `first_stage_capture`; recovered stale PRL position must backfill first-stage semantics | failed with `exit_after_first_stage=false`, `funding_captured=false`, and no passive close route |
| local GREEN focused | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_runtime_entry_flow.py::TestPlannerDispatchIntegration::test_dispatch_entry_sets_first_stage_exit_from_v1_config tests/test_runtime_entry_flow.py::TestPlannerDispatchIntegration::test_normal_exit_updates_funding_state_and_routes_first_stage_capture tests/test_runtime_entry_flow.py::TestPlannerDispatchIntegration::test_normal_exit_backfills_recovered_first_stage_exit_semantics` | `3 passed` |
| local adjacent entry/exit/persistence | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_exit_decisions.py tests/test_runtime_entry_flow.py tests/engine/test_entry_semantic_parity.py tests/persistence/test_v1_state_snapshot_semantics.py` | `132 passed` |
| local runtime/replay | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_runtime_smoke.py tests/test_live_full_closure.py tests/test_engine_entry_exit.py tests/test_persistence_replay.py` | `109 passed` |
| local passive close | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_passive_close.py` | `105 passed` |
| local recovery | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_live_entry_hedge_root_fix.py::TestPendingEntryPersistenceRoundtrip tests/recovery/test_restart_recovery_semantics.py tests/test_engine_recovery.py` | `50 passed` |
| local compile/diff | `python3 -m compileall -q lightfee tests`; `git diff --check` | passed |
| local full suite | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q` | `3247 passed, 9 skipped, 32 failed`. Failures are outside CL-021: Hyperliquid signing/fixture tests require missing optional `Crypto` dependency, and two shutdown tests monkeypatch `stop_ws_streams()` without the current `per_client_timeout_s` parameter. |
| GitNexus CLI | `npx gitnexus detect-changes --scope all --repo LightFeeV2` after the code fix | `4 files`, `11 symbols`, `0` affected processes, `low` risk |
| cloud deploy | `/opt/lightfee-v2` fast-forward `7704238..c66f470`; `python3 scripts/verify_deploy_manifest.py --local`; `python3 -m compileall -q lightfee tests`; focused 3-test CL-021 regression; deploy marker `.deploy_version=c66f470`; manifest check; service restart | pull, manifest, compileall, focused regression `3 passed`, marker, manifest check, and restart all passed; `lightfee-sidecar.service` and `lightfee-live.service` active with `NRestarts=0` |
| cloud health | post-restart checks at `2026-05-31 03:30:22 CST` and `2026-05-31 03:31:17 CST` | `verify_production_services.py` passed with `ok=True`, `critical=0`, `warning=0`; final state `open=0`, `pending_entry=0`, `pending_close=0`, `passive=0`, `residual=0`, `risk=running`, `lifecycle=running`, `last_error=None` |
| cloud real close path | `PRLUSDT` post-deploy event chain at `2026-05-31 03:30:00-03:30:27 CST` | emitted `runtime.staggered_exit_mode_backfilled`, `runtime.funding_capture_state_updated funding_captured false->true`, `runtime.normal_close_routing_passive reason=first_stage_capture`, `exit.passive_close_created target_quantity=116`, `exit.passive_close_missing_l2_tick_escalated`, `exit.passive_close_dual_taker_drive`, `exit.passive_close_live_matched_close`, `exit.passive_close_recovery_probe_flat`, and `exit.passive_close_fallback_terminal_flat` |

### Acceptance

Accepted. Code is pushed and deployed at `c66f470`; local focused and adjacent
harnesses pass; cloud manifest, compileall, and focused regression pass;
services are active with `NRestarts=0`; and the real post-deploy `PRLUSDT`
position routed through V1 `first_stage_capture` semantics and cleared flat.

## Cluster CL-020-funding-capture-before-settlement-timestamp-drift

### GitNexus Keys

- Fingerprint: `v2.entry-open-position.funding-timestamp-zero + funding-capture-before-settlement + candidate-opportunity-type-dropped`
- Components: `live-runtime`, `entry-sync`, `exit-decision`, `state-persistence`, `v1-parity`
- Symbols: `LiveRuntime._dispatch_entry`, `LiveRuntime._finalize_pending_entry`, `EntryContext`, `build_open_position`, `PendingEntry`, `standard_close_reason`, `aligned_settlement_delay_elapsed`, `force_close_due`, `update_position_funding_capture_state`
- Files: `lightfee/engine/runtime.py`, `lightfee/engine/entry.py`, `lightfee/engine/entry_sync.py`, `lightfee/engine/exit_decision.py`, `lightfee/engine/state.py`, `lightfee/engine/recovery.py`
- Watch: `entry.opened`, `entry.opened.funding_timestamp_ms=0`, `exit.close_submitted reason=funding_capture`, `funding_capture` before candidate first funding timestamp, `runtime.pending_entry_registered.funding_timestamp_ms=0`

### Summary

Production did open and close real positions after deployment. The key counterexample was `MAGMAUSDT`:

- Entry opened at `2026-05-31 01:58:28 CST`.
- It closed at `2026-05-31 01:58:40 CST` with reason `funding_capture`.
- That was before the funding settlement time visible in the candidate lifecycle evidence, so the close reason was not V1-compatible.

Root cause:

- `CandidateInput` already had funding lifecycle fields: `opportunity_type`, `funding_timestamp_ms`, `first_funding_timestamp_ms`, per-leg funding timestamps, and `second_funding_timestamp_ms`.
- `LiveRuntime._dispatch_entry` did not carry those fields into `EntryContext`.
- `EntrySyncExecutor._make_pending_entry` and `LiveRuntime._finalize_pending_entry` could not preserve those fields through the pending entry path.
- `build_open_position` therefore created `OpenPosition` with default `funding_timestamp_ms=0` and default `opportunity_type="aligned"`.
- `aligned_settlement_delay_elapsed()` treated `now_ms >= 0 + settlement_delay_ms` as true, so a newly opened position could close immediately as `funding_capture`.

### V1 Decision

This is a V2/V1 semantic drift, not an exchange-rule or market-data threshold issue.

V1 close semantics require a real funding timestamp before aligned settlement delay or force-close timing can be considered elapsed. Missing funding time is missing evidence, not proof that settlement happened.

### Fix

- Added funding lifecycle fields to `EntryContext` and `PendingEntry`.
- `LiveRuntime._dispatch_entry` now copies funding lifecycle fields from the selected `CandidateInput` into `EntryContext` and entry-selection journal evidence.
- `EntrySyncExecutor._make_pending_entry` persists those fields for passive/pending entry flows.
- `LiveRuntime._finalize_pending_entry` restores those fields when building `OpenPosition` from a completed pending entry.
- `build_open_position` now writes `funding_timestamp_ms`, `second_funding_timestamp_ms`, `opportunity_type`, `second_stage_enabled_at_entry`, and entry edge fields onto `OpenPosition`.
- `EngineState.to_dict()`, `build_persistent_state_view()`, and `_restore_state_from_snapshot_dict()` now preserve the pending/open funding semantics.
- `remaining_close_delay_active()`, `aligned_settlement_delay_elapsed()`, `force_close_due()`, and `update_position_funding_capture_state()` now return fail-closed when `funding_timestamp_ms <= 0`.

### Verification

| Environment | Evidence | Result |
|---|---|---|
| local RED | Focused new tests for zero funding timestamp close timing, MAGMA-style open-position construction, runtime candidate-to-context propagation, and snapshot roundtrip before the fix | failed with immediate `funding_capture`, missing `EntryContext` fields, missing runtime context fields, and missing snapshot keys |
| local GREEN | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_exit_decisions.py::TestStandardCloseReason::test_zero_funding_timestamp_does_not_trigger_funding_capture tests/test_exit_decisions.py::TestRemainingCloseDelayActive::test_zero_funding_timestamp_not_active tests/test_exit_decisions.py::TestAlignedSettlementDelayElapsed::test_zero_funding_timestamp_not_elapsed tests/test_exit_decisions.py::TestForceCloseDue::test_zero_funding_timestamp_not_due tests/test_exit_decisions.py::TestUpdateFundingCaptureState::test_stage1_not_captured_without_funding_timestamp tests/engine/test_entry_semantic_parity.py::TestEntryExecutionIdempotency::test_build_open_position_preserves_funding_semantics tests/test_runtime_entry_flow.py::TestPlannerDispatchIntegration::test_dispatch_entry_preserves_candidate_funding_semantics tests/persistence/test_v1_state_snapshot_semantics.py::TestOpenPositionFieldCompleteness::test_enginestate_to_dict_preserves_open_position_funding_semantics tests/persistence/test_v1_state_snapshot_semantics.py::TestPendingEntryFieldCompleteness::test_pending_entry_funding_semantics_roundtrip` | `9 passed` |
| local adjacent | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_exit_decisions.py tests/engine/test_entry_semantic_parity.py tests/test_runtime_entry_flow.py tests/persistence/test_v1_state_snapshot_semantics.py` | `129 passed` |
| local recovery | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_live_entry_hedge_root_fix.py::TestPendingEntryPersistenceRoundtrip tests/recovery/test_restart_recovery_semantics.py tests/test_engine_recovery.py` | `50 passed` |
| local persistence/replay | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q tests/test_persistence_replay.py tests/offline/replay/test_replay_semantic_equivalence.py tests/test_engine_entry_exit.py tests/test_domain_contracts.py` | `94 passed` |
| local compile/diff | `python3 -m compileall -q lightfee tests`; `git diff --check` | passed |
| local full suite | `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra dev pytest -q` | `3244 passed, 9 skipped, 32 failed`. Failures are outside CL-020: Hyperliquid signing tests require missing optional `Crypto` dependency, and two shutdown tests monkeypatch `stop_ws_streams()` without the current `per_client_timeout_s` parameter. |
| GitNexus CLI | impact before edits; `npx gitnexus detect-changes --scope all --repo LightFeeV2` after staging | HIGH for entry construction/dispatch, LOW or MEDIUM for state/exit helpers. Final detect-changes reported `12 files`, `79 symbols`, `3` affected processes, `medium` risk; the reported processes are from existing runtime file flow grouping and no HIGH/CRITICAL post-change drift was reported. |
| cloud pre-deploy | `python3 scripts/verify_production_services.py --json` on production `d3e8d12` | passed: services active, `risk_mode=running`, `lifecycle=running`, local open/pending/residual counts zero |
| cloud deploy | `/opt/lightfee-v2` fast-forward `d3e8d12..053cc8a`; `python3 scripts/verify_deploy_manifest.py --local`; `python3 -m compileall -q lightfee tests`; focused 9-test regression; deploy marker `.deploy_version=053cc8a`; manifest check; service restart | pull, manifest, compileall, focused regression `9 passed`, marker, manifest check, and restart all passed; `lightfee-sidecar.service` and `lightfee-live.service` active |
| cloud health | post-restart `python3 scripts/verify_production_services.py --json` after tick settled | `ok=true`, `critical_count=0`, `warning_count=0`, services active/running, `NRestarts=0`, `risk_mode=running`, `lifecycle=running` |
| cloud real entry path | post-deploy `PRLUSDT` pending finalized into open position | `entry.opened` has `funding_timestamp_ms=1780167600000` (`2026-05-31 03:00:00 CST`), `second_funding_timestamp_ms=1780171200000` (`2026-05-31 04:00:00 CST`), `opportunity_type=staggered`, `second_stage_enabled_at_entry=true`, and no post-open `funding_capture` close event before first funding. Current state at verification: `open_position_count=1`, `pending_entry_count=0`, `pending_close_count=0`, `pending_residual_repair_count=0`, `last_error=null`. |
| cloud latest watch | production recheck at `2026-05-31 03:05:24 CST` after docs deployment marker `8cf2b9f` | `/opt/lightfee-v2` `HEAD=8cf2b9f`, `.deploy_version=8cf2b9f`; `lightfee-sidecar.service` and `lightfee-live.service` active with `NRestarts=0`; `verify_production_services.py` passed. Full state still has one `PRLUSDT` open position with `funding_timestamp_ms=1780167600000`, `second_funding_timestamp_ms=1780171200000`, `opportunity_type=staggered`, `second_stage_enabled_at_entry=true`, `funding_captured=false`, `second_stage_funding_captured=false`, and zero pending entry/close/residual work. `journalctl -p warning` since deploy returned no entries. Tail event scan showed no post-open `funding_capture` or close event; the only fresh error-like entry was a separate symbol-scoped `STGUSDT` Aster `leverage_admission_blocked` rule reject, not a CL-020 close-timing regression. |

### Acceptance

Accepted for the early-funding-close regression. Code is deployed at `053cc8a`;
docs and deployment marker are synced at `8cf2b9f`; local and cloud focused
harnesses pass; production health is green after restart; and the real
post-deploy `PRLUSDT` entry preserved first/second funding timestamps and
remained open instead of closing immediately as `funding_capture` at CL-020
acceptance time. The later first-stage staggered close path drift for that same
position is tracked and closed as CL-021 above.
