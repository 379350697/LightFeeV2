# Bug Ledger Daily - 2026-09-04

## Working-tree batch — evidence-sufficient repairs and evidence completion

Status at investigation time: **local-green only**. The changes below were not
committed or deployed in this turn. Production exchange truth remains
authoritative; no order or cancel was submitted while collecting evidence or
running tests.

### Prior deployment addendum — 2026-09-04 (`47c94b24`)

The preceding batch is deployed at `47c94b24`. The remote manifest verified all 16
critical files; sidecar/live restarted active with one process each; all seven
venue sources were fresh; FD and `CLOSE_WAIT` counts were healthy; and
`diagnose_live --since-deploy` returned `healthy`, `risk=low`, and
`gate_passed=true`. Exchange truth was high-confidence flat with no open
orders. No natural eligible order was forced, so scenario-level proof remains
pending.

The preceding batch was split deliberately: four items had a reproducible
contract break in the production path (three fixed there and one inherited
from the 2026-09-03 batch); the remaining observations did not yet prove an
independent root cause. They receive diagnostics and regression coverage, not
speculative retries, pool changes, or venue-specific resets.

### Current working-tree follow-up — WT-20260904-5

This section supersedes no deployed claim. The checked-out production version
is still `47c94b24`; the changes below are local and have not been deployed.

Three evidence-sufficient items are now handled at the shared contract level
locally (the second is an inherited implementation verified end-to-end; the
third is the ledger-drift repair):

1. The documented Bitget `40786 Duplicate clientOid` response now uses the
   same client-id idempotency path as Bybit `110072` in close, passive-close,
   residual-repair, and entry-cleanup callers. The original client id is
   reconciled first; a retry, when justified by exchange truth, receives a
   fresh id. The production-path regression proves a Bitget partial fill is
   consumed without resubmitting the same id.
2. The existing close-evidence owner is verified durable across live-flat
   cleanup, restart replay, exact history recheck, and immutable terminal
   debt. No second billing queue or fuzzy matcher was added. Historical debts
   remain accounting work, not positions or orders; this batch does not invent
   a fill for an old debt that the exchange can no longer identify uniquely.
3. The active bug ledger now records this local batch and distinguishes it
   from the historical `47c94b24` deployment record. No local status is
   presented as a cloud deployment fact.

Four evidence-only gaps receive structured fields without changing trading
decisions: typed Aster/OI request identity and transport cause; Local-L2
stream generation and sequence-gap delta; per-domain source observation time
and age in snapshot health; and two-sample FD/CLOSE_WAIT deltas. The fields
are asserted through the production-facing tests. The standard verifier now
persists the immediately previous FD/CLOSE_WAIT sample atomically at
`runtime/production-resource-baseline.json`, so the production timer emits
real deltas on its second run; Aster-specific resets, OI retries, freshness
blocking, and resource thresholds remain unchanged until a deployed correlated
sample supplies an independent root cause.

The diagnosis path also consumes the shared duplicate-client-id predicate. A
Bitget `40786 Duplicate clientOid` event is therefore resolved by the same
truth-gap evidence as Bybit `110072`; it cannot remain as a false unresolved
error solely because the reporter used to recognize Bybit only.

Local status: `local-green` for the three repairs and instrumentation. Cloud
status: unchanged at `47c94b24`; re-run deployment acceptance and a read-only
observation window after deployment before moving this batch to
`deployed-awaiting-verification`.

### WT-20260904-1 — matched live pair was kept in `risk_only`

**Evidence and contract.** A TUSDT pair matched Binance/Bybit account truth,
but a stale risk-only latch remained because V2 treated every local open
position as recovery work. V1 permits a matched live pair to be checked against
complete account truth; only pending entries/closes, residual repairs, or
reservations require the recovery gate.

**Repair.** The existing account-truth refresh owner now runs for a matched open
pair, bounded by the private-position probe interval. It clears the latch only
after both account positions and open orders are complete and matching; partial
or unavailable truth still retains `risk_only/fail_closed`.

**Regression.** A runtime-path test covers block → complete paired truth →
`running`, including the interval guard. Existing incomplete-truth and
terminal-flat cases remain green. A deployed observation is still required.

### WT-20260904-2 — Bitget contract precision was guessed from static defaults

**Evidence and contract.** Production returned Bitget `40020` for a low-price
TUSDT order. The old builder formatted `0.0044` as `0.00`; V1 and Bitget's
contract catalog require the exchange precision and quantity contract. The
shared rule owner now reads
`/api/v2/mix/market/contracts` (`productType=USDT-FUTURES`), parses
`priceEndStep`/`pricePlace`, `sizeMultiplier`/`volumePlace`, and
`minTradeNum`, and refuses live submission when authoritative rules are
missing or malformed. Classic and UTA adapter paths use the same preflight and
decimal wire formatter for maker and taker orders; zero-valued decimal-place
counts are accepted because they are valid integer-price/integer-size catalog
values.

**Regression.** Catalog parsing, malformed fallback, Classic/UTA wire bodies,
and the live Classic adapter path preserve `0.0044` instead of `0.00`.
No retry or alternate precision source was added. Production must observe a
natural eligible low-price candidate; no forced order is allowed.

### WT-20260904-3 — Bitget literal heartbeat was decoded as JSON

**Evidence and contract.** Bitget Local-L2 can send literal `ping`/`pong`.
V2 attempted JSON decoding first and recorded a decode error. The parser now
handles the control frame before JSON while retaining the existing keepalive
and transport diagnostics.

**Regression.** Text and bytes `ping`/`pong` produce keepalive events and no
decode error. No heartbeat loop or reconnect policy was changed.

### WT-20260904-4 — terminal automatic-history debt remains immutable

The immutable terminal-owner repair is already in the 2026-09-03 batch and is
verified here as an inherited contract: an ordinary late close observation
cannot reopen a terminal automatic-history debt; only an explicit
terminal/import path may enrich it. It is included in `47c94b24`; a natural
terminal-debt observation is still required for closure.

## Evidence completed without speculative behavior changes

### Aster transport/OI evidence

The earlier Aster `CLOSE_WAIT`/OI failures did not prove that an Aster-specific
reset, retry, or concurrency change was the cause. The shared
`MarketDataClient` now preserves request phase, transport exception type,
deepest cause, client generation, and whether the failed client was retired;
targeted OI diagnostics carry those fields through to runtime events. The
shared `TransportError` accepts the same request identity fields, so private
transport failures cannot raise a secondary constructor error while recording
the original network failure. Aster is
now covered by the same production-shaped reset/retirement regression as
Binance. The next production sample must include venue/path, these typed
fields, client generation/retirement, and before/after socket counts. Until
that sample correlates the failure with a shared transport boundary, the issue
stays **root-cause-unconfirmed** and no Aster-only fix is claimed.

### Local-L2 readiness and sidecar resources

Readiness/stale events already record venue, symbol, sequence, status, source
age, and websocket stream; health already checks per-source freshness, FD, and
`CLOSE_WAIT`. The prior missing-L2/stale and short resource samples are
therefore diagnosable, but not yet closure evidence. Require a fresh deployed
sample plus two separated resource observations showing no FD/CLOSE_WAIT slope.
Do not aggregate-fallback on a missing count or treat a fresh wrapper snapshot
as fresh underlying data.

### Rotated logs and ledger drift

`diagnose_live` already discovers all rotated `*.jsonl*` files, de-duplicates
records, globally orders them, and reports scope metadata. Production output
and the actual `.deploy_version` still must be captured before rewriting
`ACTIVE.md`; the prior deployment record is preserved as historical fact, and
the new `47c94b24` evidence is recorded separately.

## Verification and closure gate

Focused checks for this batch:

```text
live-startup/risk-only path: 155 passed
Bitget transport/rules/adapter: 105 selected tests passed
Local-L2 parser: 81 passed
Aster/Binance typed transport + targeted OI: 3 passed
```

Run the broader market-data, health, rotated-diagnosis, close-evidence, and
full validation profiles before a fixing commit. This working-tree batch has
not been deployed; until the required natural production observations are
recorded after deployment, the correct status is **local-green**.

After the shared `TransportError` contract was completed, the focused private
transport suite is `494 passed`, and the new evidence-path regressions are
`6 passed` (including Bitget diagnosis and baseline persistence). The latest
full local run is `4579 passed, 25 failed, 9 skipped`;
the remaining failures are the known snapshot-entry fixtures, older pending-
close semantic-drift expectations, the operator import/persistence fixtures,
and the pre-existing runtime-split registry check. They are not being hidden
or made green by changing the trading contract, so this batch is not claimed
as full-suite closed or production-verified.

## Rejected patterns retained as maintenance guidance

- Do not reintroduce static Bitget two-decimal formatting or treat an explicit
  zero fill as a missing field.
- Do not create an Aster-only reset/retry path without typed production
  correlation; repair the shared transport boundary first.
- Do not loosen OI admission or infer health from wrapper publication time.
- Do not reopen terminal accounting debt on ordinary observations or poll
  history forever.
