# 2026-06-28 Bug Ledger

## Cluster CL-131 - ACT Funding Close Lifecycle Recovery Metadata and Symbol Identity

Status: `904bb23` deployed/cloud verified. Production is acceptance-green after
ACT closed through the normal lifecycle: `lifecycle=running`,
`risk_mode=running`, exchange truth flat, no open orders, no pending
entry/close, no order-truth gaps, and V1 closure `RUNNING_CLEAN`.

### GitNexus Keys

- Fingerprints:
  `act_recovered_position_missing_funding_timestamp`,
  `okx_raw_symbol_unpaired_live_position_drift`,
  `live_recovered_funding_close_not_due`,
  `risk_only_unpaired_live_position_after_managed_pair`,
  `terminal_flat_requires_current_exchange_truth`
- Components: `startup-live-recovery`, `recovery-ledger`,
  `recovery-owner-index`, `v1-lifecycle-closure`, `passive-close`,
  `diagnose-live`
- Symbols: `_hydrate_balanced_startup_live_positions`,
  `RecoveryLedger`, `RecoveryOwnerIndex`,
  `UnpairedLivePositionRecoveryRuntime.register_from_ledger`,
  `canonical_recovery_symbol`
- Files: `lightfee/engine/runtime.py`,
  `lightfee/engine/recovery_symbol_identity.py`,
  `lightfee/engine/recovery_ledger.py`,
  `lightfee/engine/recovery_owner_index.py`,
  `lightfee/engine/unpaired_live_position_recovery.py`,
  `lightfee/engine/v1_lifecycle_closure.py`
- Watch: `live-recovered:ACTUSDT:binance->okx`,
  `recovery.recovered_position_funding_timestamp_missing`,
  `recovery.unpaired_live_position_owner_reassociated`,
  `runtime.normal_close_routing_passive`,
  `exit.passive_close_resolved`, `runtime.position_lifecycle_terminal`
- Cards: [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md),
  [passive-close-terminal-flatness](../cards/passive-close-terminal-flatness.md)

### Production Symptom

After the funding settlement time passed, `ACTUSDT` remained open for more than
20 minutes instead of entering close. Cloud truth before the fix showed a
managed Binance long / OKX short position, no pending close, and no open
orders, while current state stayed `lifecycle=risk_only` with
`recovery_blocked_reason=unpaired_live_position`.

The same ACT exposure was split semantically: local state had
`live-recovered:ACTUSDT:binance->okx`, but V1 closure also replayed OKX raw
symbol `ACT-USDT-SWAP` as an orphan `unpaired_live_position`.

### Root Cause

1. Startup live recovery created balanced `live-recovered:*` positions without
   close-critical funding metadata. A recovered position could therefore look
   managed but lose `funding_timestamp_ms`, venue funding timestamps,
   opportunity type, and maker-leg hints needed by the funding close decision.
2. Recovery identity did not share one canonical symbol rule across ledger,
   owner index, unpaired recovery, and V1 closure. OKX raw symbols such as
   `ACT-USDT-SWAP` could fail to match local strategy symbol `ACTUSDT`, so the
   same exchange leg appeared both managed and orphaned.
3. Terminal-flat cleanup for old unpaired records could be emitted from missing
   ledger ownership alone. It needed to distinguish "owner re-associated" from
   true exchange-terminal flatness.

### Fix

- Added a shared recovery symbol identity helper so recovery ledger, owner
  index, V1 closure inputs, and unpaired recovery compare venue raw symbols
  through the same canonical strategy symbol while retaining raw venue symbols
  as exchange evidence.
- `_hydrate_balanced_startup_live_positions()` now rehydrates funding metadata
  from the same owner/pending journal first, then from current quote/sidecar
  funding truth. It populates funding timestamps, opportunity type, first
  funding leg, entry/exit maker-leg hints, and notional on recovered positions.
- If neither journal nor quote truth has a positive funding timestamp, startup
  does not create a "normal-looking but never closeable" recovered position. It
  emits `recovery.recovered_position_funding_timestamp_missing` and stays
  fail-closed.
- Active stale unpaired recovery records are marked
  `owner_reassociated` when current ledger truth proves the same canonical
  venue/symbol/side is now owned, instead of incorrectly emitting
  terminal-flat without current exchange flat/open-order proof.
- V1 lifecycle closure maps the new recovery events so diagnostics do not
  become unmapped residue.

### Verification

| Scope | Evidence | Result |
|---|---|---|
| local RED/GREEN | Binance `ACTUSDT` + OKX `ACT-USDT-SWAP` balanced recovered position must not create `unpaired_live_position` | passed |
| local RED/GREEN | journal funding timestamp `1782633600000` rehydrates recovered position and makes funding close due after settlement | passed |
| local RED/GREEN | missing funding metadata emits `recovered_position_funding_timestamp_missing` and does not create a silent stuck position | passed |
| local RED/GREEN | stale unpaired record with current owner becomes `owner_reassociated`, not `terminal_flat` | passed |
| local focused | recovery owner index, recovery ledger, unpaired recovery runtime, V1 lifecycle closure, diagnose, exit decisions, startup preflight | passed |
| local full | `.venv/bin/pytest -q` | 4482 passed, 9 skipped, 1 warning |
| GitNexus | freshness after main fast-forward and `detect_changes` | indexed `9077fbd`/`904bb23`, no unindexed changes before push |
| cloud pre-deploy | read-only diagnosis on `ba15b0a` | reproduced ACT managed position + stale `unpaired_live_position`; no open orders; journal contained ACT funding timestamp `1782633600000` |
| cloud deploy | manifest, singleton, services, diagnose on `904bb23` | PASS; services healthy |
| cloud ACT close | post-deploy events | `runtime.normal_close_routing_passive`, `exit.passive_close_created`, Binance maker submit, OKX fallback fill, `exit.passive_close_resolved`, `runtime.position_lifecycle_terminal`, `recovery.flat` |
| cloud final truth | `diagnose_live.py --json --since-deploy` | healthy; exchange flat; no open orders; pending entry/close 0; ownerless open order 0; lifecycle/risk running; V1 closure `RUNNING_CLEAN` |

### Current Production Truth

Final cloud summary at `904bb23`:

- `deploy_version=904bb23`, `git_head=904bb23`, `version_mismatch=false`
- `health.ok=true`, `critical_count=0`, `warning_count=0`
- `lifecycle=running`, `risk_mode=running`,
  `recovery_blocked_reason=null`
- `open_position_count=0`, `pending_entry_count=0`,
  `pending_close_count=0`
- `exchange_truth_flat=true`, `exchange_truth_no_open_orders=true`
- `ownerless_open_order_count=0`,
  `pending_entry_order_truth_gap_count=0`
- V1 closure:
  `recovery_decision_kind=RUNNING_CLEAN`, `blocking_row_count=0`,
  `entry_allowed=true`
- Hyperliquid remains intentionally disabled for trading under
  `api_wallet_authorization_not_verified_strict_readonly`; this is separate
  strict-readonly policy, not an ACT lifecycle blocker.

### Acceptance

This cluster does not change strategy thresholds, risk limits, spread filters,
or order-truth semantics. It closes the ACT lifecycle root cause by making
recovered live-position ownership, funding-close metadata, and terminal-flat
truth use one business vocabulary: a position is managed only when symbol/owner
identity agrees, close timing must come from recovered funding truth, and flat
cleanup requires current exchange truth.

## Cluster CL-130 - Lifecycle Closure Drift, Aster Admission, Hyperliquid Identity Strict-Readonly

Status: `7e058b0` deployed/cloud verified for the lifecycle/Aster/Hyper
strict-readonly and wallet-mode source closure. Production is not
acceptance-green because current exchange truth still has one ownerless Bitget
open order.

### GitNexus Keys

- Fingerprints:
  `terminalized_pending_entry_live_conflict_replayed_as_current_blocker`,
  `aster_5018_symbol_admission_block`,
  `hyperliquid_api_wallet_authorization_not_verified_strict_readonly`,
  `hyperliquid_wallet_mode_env_runtime_diagnose_split`,
  `bitget_ownerless_open_order`
- Components: `v1-lifecycle-closure`, `recovery-owner-index`,
  `diagnose-live`, `venue-transport`, `venue-registry`, `hyperliquid`,
  `aster-admission`
- Symbols: `RecoveryOwnerIndex.active_journal_owner_events`,
  `RecoveryOwnerIndex._add_state`,
  `LiveRuntime._refresh_v1_lifecycle_closure_state`,
  `_export_current_state_snapshot`,
  `resolve_hyperliquid_credential_identity`,
  `VenueTransport.verify_live_trading_preflight`,
  `build_adapter`,
  `_build_resolved_contained_entry_admission_summary`,
  `_build_production_acceptance_gate`
- Files: `lightfee/engine/recovery_owner_index.py`,
  `lightfee/engine/runtime.py`, `lightfee/engine/loop_control.py`,
  `lightfee/venues/transport.py`, `lightfee/venues/registry.py`,
  `lightfee/config/schema.py`, `scripts/diagnose_live.py`,
  `tests/test_venues_transport.py`, `tests/test_diagnose_live.py`,
  `tests/engine/test_v1_lifecycle_closure_table.py`
- Watch: `owned_live_conflict_cleanup_succeeded`,
  `terminalized_after_single_leg_recovery`, `orphan_maker_order`,
  `account_wallet_signer_mismatch`,
  `api_wallet_authorization_not_verified_strict_readonly`,
  `max_notional_admission_blocked`,
  `pending_entry_order_truth_gap_count`
- Cards: [pending-entry-terminality-live-truth](../cards/pending-entry-terminality-live-truth.md),
  [pending-entry-hedge-admission](../cards/pending-entry-hedge-admission.md)

### Production Symptom

The latest production state was not an entry failure. `LABUSDT` had previously
opened as a managed Binance long / OKX short pair and later flattened, but
diagnose/state could replay stale lifecycle evidence as current `risk_only`.
Post-deploy truth later showed no positions, no pending entries, no pending
closes, and no order-truth gaps, while one Bitget `LABUSDT` non-reduce-only
open order remained live without a current owner:
`order_id=1454883357194874881`, side `buy`, quantity `1.0`, price `15.39`.

Hyperliquid was disabled with identity evidence, not because the account type
is categorically unsupported. Hyperliquid account data is read through
`/info` for the real account address, while signed trading uses `/exchange`.
API wallets sign for an account; they are not the account address itself. The
strict production policy for this incident is read-only: do not send a signed
noop just to prove authorization.

After the first deployed fix, cloud evidence also exposed a source split:
diagnose loaded `LIGHTFEE_HYPERLIQUID_WALLET_MODE` from systemd environment
files, while live runtime defaulted `config/live.toml` to `account_wallet`
when the TOML credential omitted `wallet_mode`. That made diagnose and runtime
describe the same identity with different wallet modes.

### Root Cause

1. Diagnose-style current-state exports can contain `positions` while
   `RecoveryOwnerIndex` only indexed `open_positions`. That allowed historical
   journal owner facts from a terminalized pending-entry live-truth conflict to
   be considered again when rebuilding V1 closure.
2. `owned_live_conflict_cleanup_succeeded` and
   `terminalized_after_single_leg_recovery` were not treated as owner-terminal
   events for current closure indexing, so old `owned_pending_entry_live_conflict`
   evidence could survive past cleanup.
3. Exported `lifecycle=risk_only` and `recovery_blocked_reason` could stay
   latched after current V1 closure had already allowed entry.
4. Aster real `-5018` needs symbol-scoped admission blocking. The existing
   runtime block was valid, but diagnose resolution required whole-account
   flatness and could keep old contained Aster rejects unresolved when another
   managed exposure existed elsewhere.
5. Hyperliquid identity had two separate proof classes: account-state read
   proof and signed trade authorization proof. `api_wallet` mode must not use
   the signer as the account, and strict-readonly production must not silently
   do a signed noop.
6. Hyperliquid credential mode loading was not fully shared: diagnose read the
   standard environment mode, but registry/runtime only consumed TOML
   `wallet_mode`.

### Fix

- Recovery owner indexing now accepts both `open_positions` and `positions` and
  skips historical journal position facts for pending-entry owners that already
  emitted terminal cleanup/recovery events.
- Runtime closure refresh now clears stale clearable recovery block reasons
  only when V1 closure says entry is allowed, there is no pending work, risk
  mode is still running, and the reason is in the core clearable set.
- Current-state export derives effective lifecycle and recovery block from the
  current V1 closure summary, without hiding real fail-closed recovery blocks.
- Diagnose distinguishes managed active exposure from abnormal exposure and
  resolves contained admission rejects by the same `(venue, symbol)` exchange
  truth identity instead of requiring global flatness.
- Hyperliquid transport now uses a shared credential identity resolver. In
  `account_wallet` mode, account/signer mismatch fails closed as
  `account_wallet_signer_mismatch`. In `api_wallet` mode, the signer is not
  treated as the account; by default no signed noop is sent and trading remains
  untrusted with
  `api_wallet_authorization_not_verified_strict_readonly`.
- Hyperliquid registry credential construction now reads either
  `wallet_mode_env` or the standard `LIGHTFEE_HYPERLIQUID_WALLET_MODE` fallback,
  matching diagnose's systemd environment-file source while preserving
  strict-readonly default.
- Hyperliquid strict-readonly now closes the diagnostic/admission loop:
  diagnose exports `account_state_readable`, `signer_matches_account`, and
  `trading_authorization_trusted`; strict-readonly is tagged as a policy block;
  retained logs are scanned for canonical Hyperliquid `order.*` /
  `entry.opened` evidence; and entry admission prunes Hyperliquid candidates
  before real submit when trading authorization is not trusted.

### Hyperliquid Timeline Addendum

- `cafdf0a` introduced identity preflight and correctly separated account-state
  reads from signed trading authorization, but the surfaced reason could still
  look like an account-wallet mismatch when runtime and diagnose disagreed on
  wallet mode.
- `7e058b0` fixed the wallet-mode source split so the same credentials are
  described as `api_wallet` by runtime and diagnose. From that point the
  production reason became the accurate strict-readonly policy block:
  `api_wallet_authorization_not_verified_strict_readonly`.
- This is not evidence that the Hyperliquid interface or account type suddenly
  stopped working. It means the currently configured account and signer are
  different, account state is readable, and signed API-wallet authorization has
  not been proven under the selected no-noop policy.
- Recovery path remains explicit: either use a signer that is the account
  wallet, or add a separately approved signed authorization proof to the state
  machine. Until then Hyperliquid is quote/exchange-truth only and must not
  reach real order submission.

### Verification

| Scope | Evidence | Result |
|---|---|---|
| local RED/GREEN | terminalized pending-entry conflict no longer appears as current V1 closure blocker when state uses diagnose-style `positions` | passed after fix |
| local RED/GREEN | managed open position with balanced exchange truth and no open orders is not diagnosed as abnormal exposure | passed |
| local RED/GREEN | stale `recovery_blocked_reason` clears only after current V1 closure allows entry; unavailable truth remains fail-safe | passed |
| local RED/GREEN | Aster real `-5018` arms symbol-scoped admission block; advisory-zero remains nonblocking | passed |
| local RED/GREEN | Hyperliquid `account_wallet` mismatch fail-closed; `api_wallet` strict-readonly does not send noop and is not trading trusted | passed |
| local follow-up | registry/runtime consumes standard `LIGHTFEE_HYPERLIQUID_WALLET_MODE` and `wallet_mode_env` consistently with diagnose | `tests/test_venues_transport.py` passed |
| local focused | `tests/test_venues_transport.py`, `tests/test_diagnose_live.py`, `tests/test_live_entry_hedge_root_fix.py`, `tests/engine/test_v1_lifecycle_closure_table.py`, `tests/live_harness/test_exchange_admission_incidents.py` | 882 passed before follow-up; transport/startup/diagnose follow-up 459 + 292 passed |
| local full | `.venv/bin/pytest -q` | 4468 passed, 9 skipped, 1 warning |
| cloud deploy | `7e058b0` pulled to `/opt/lightfee-v2`; manifest check and singleton check | PASS |
| cloud services | `scripts/verify_production_services.py --json` | services active but health failed because exchange truth had ownerless Bitget open order |
| cloud diagnose | `scripts/diagnose_live.py --json --since-deploy` | git/deploy version `7e058b0`, no positions, no pending entry/close, no order-truth gaps, Hyper `wallet_mode=api_wallet`, `allow_api_wallet_authorization_probe=false`, disabled reason `api_wallet_authorization_not_verified_strict_readonly`, ownerless Bitget open order remains blocking |

### Current Production Truth

The code-side lifecycle and identity fixes are active on the deployed code
version, but production is correctly fail-closed because one current exchange
artifact has no owner:

- Venue: `bitget`
- Symbol: `LABUSDT`
- Order id: `1454883357194874881`
- Side/quantity/price: `buy`, `1.0`, `15.39`
- Reduce-only: `false`
- Current local work: no pending entry, no pending close, no order-truth gap
- Current exchange truth: no nonzero positions, open order present
- V1 closure: `entry_allowed=false`,
  `recovery_block_reason=orphan_maker_order`

Do not call this a normal managed position. Do not call the system acceptance
green while this order exists. The remaining operational solution is either
explicit operator approval to cancel that ownerless Bitget order, or waiting
until exchange truth proves the order terminal/no-longer-open and rerunning the
production proof chain.

### Acceptance

This cluster does not change strategy thresholds, risk limits, spread-reversion
filters, or order-truth semantics. It tightens the boundary: V1 closure owns
current lifecycle truth, Aster real max-notional rejects become symbol-scoped
admission evidence, Hyperliquid account read proof and signed trading proof are
separate, and strict-readonly production does not submit signed authorization
probes.

## ACT Close Statement Accounting Backfill

### Evidence

- Production ACTUSDT had already reached terminal flat: no current positions, no
  ownerless open orders, no pending entry/close, no blocking close
  reconciliation queue, and V1 closure `RUNNING_CLEAN`.
- The historical accounting gap came from
  `exit.reconciled evidence_gap=true` for
  `live-recovered:ACTUSDT:binance->okx`: OKX short close statement was found,
  but the Binance long close statement was missing at final reconcile time.
- The passive close maker leg had Binance order identity
  `order_id=3734866560`, `client_order_id=lfexc794d080ed776a0d`. A later
  read-only Binance reconciliation query returned `FILLED`, quantity `5385`,
  average price `0.0087`, filled at `1782637797309`.
- `reconciliation.pending_close_backfill_archived` was already present, so this
  was not live trading risk. It was an audit/accounting completion item.

### Fix

- Diagnose now recognizes owner-scoped
  `reconciliation.pending_close_backfill_archived` and
  `reconciliation.pending_close_backfill_completed` events when summarizing
  terminal-flat close accounting gaps.
- Historical `exit.reconciled evidence_gap=true` remains counted for audit
  truth, but a backfilled item reports `audit_status=terminal_flat_backfilled`
  and `next_action=none_backfill_recorded` instead of continuing to advertise
  `backfill_trade_statement_or_archive_gap`.
- No order-truth, lifecycle, risk, or strategy threshold semantics were changed.

### Verification

| Scope | Evidence | Result |
|---|---|---|
| local RED | archived + backfilled terminal-flat accounting gap still reported as pending backfill | failed before fix |
| local GREEN | `test_business_progression_quality_marks_archived_backfilled_accounting_gap_done` | passed |
| local focused | `tests/test_diagnose_live.py` | 190 passed |
| cloud audit backfill | appended `reconciliation.pending_close_backfill_completed` for ACT Binance long close `3734866560 / lfexc794d080ed776a0d` after read-only exchange reconciliation returned `FILLED` | event present in `runtime/live-events.jsonl`, quantity `5385`, average price `0.0087`, filled at `1782637797309` |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded to `9ddbe34`; `.deploy_version=9ddbe34`; manifest, singleton, services, and diagnose checked | PASS; `risk_mode=running`, `lifecycle=running`, exchange truth flat, no open orders, no pending entry/close, no blocking close reconciliation |

## Passive Close Cost And Diagnostic Noise Root Fix

### Evidence

- Post-deploy LABUSDT close completed flat with no residual live exposure, but
  the close path paid extra cost: maker close attempts terminalized with zero
  fill before fallback, and an ACK-only reduce-only close needed later exchange
  truth to prove the account was flat.
- The source issue was not strategy threshold selection or order-truth semantics.
  It was close lifecycle readiness: after a maker leg already produced zero
  fill in the same phase, the next drive could still treat that same leg as
  maker-ready; and one-sided reduce-only recovery did not resolve an existing
  accepted-order truth gap before attempting another reduce-only submit.
- Diagnose also lacked a dedicated nonblocking cost bucket, so resolved
  zero-fill/fallback traces could be read as generic close noise instead of
  actionable source-gate inefficiency.

### Fix

- `PassiveCloseReadiness` now marks the current maker leg blocked with
  `recent_maker_zero_fill` when the same passive-close phase has already seen a
  zero-fill cycle and the alternate leg is ready, close switches maker leg
  before submitting another passive maker order. If the alternate leg is not
  ready, the current ready leg remains eligible so V1 zero-fill retry/phase
  exhaustion can still progress to fallback.
- One-sided live flatten now resolves any owner/venue/leg
  `accepted_order_truth_gap` through the shared
  `resolve_accepted_order_truth(...)` helper before placing another reduce-only
  order. `filled` applies the missing close fill, `live_flat` clears the gap
  only after full live-flat/open-order proof, and `truth_gap` /
  `truth_unavailable` / `open_order_present` retain pending and block duplicate
  submit.
- Diagnose now reports `close_cost_inefficiency_summary` with maker no-fill,
  zero-fill cycle, and fallback-after-zero-fill counts. Terminal-resolved items
  remain historical/nonblocking; unresolved items still require exchange truth.
- No strategy thresholds, risk limits, spread filters, or exchange adapter
  order-submit semantics were changed.

### Verification

| Scope | Evidence | Result |
|---|---|---|
| local RED | recent maker zero-fill still allowed same-leg maker submit | failed before fix |
| local RED | existing accepted close truth gap did not resolve before one-sided reduce-only retry | failed before fix |
| local RED | live-flat accepted close truth gap did not clear pending through exchange truth | failed before fix |
| local RED | resolved zero-fill/fallback traces had no dedicated close-cost diagnostic bucket | failed before fix |
| local review RED | recent zero-fill gate could block the only ready maker leg and stop phase-exhaustion progress | failed before review fix |
| local GREEN | `tests/test_passive_close.py::test_passive_close_readiness_blocks_recent_zero_fill_leg_and_switches` and two one-sided accepted-gap tests | passed |
| local GREEN | `tests/test_diagnose_live.py::test_business_progression_quality_reports_close_cost_inefficiency_as_non_blocking` | passed |
| local focused | `tests/test_passive_close.py tests/test_diagnose_live.py` | 351 passed |
| local full | `.venv/bin/pytest -q` | 4488 passed, 9 skipped, 1 warning |

## Cluster CL-132 - Aster Structured Submit Reject Evidence To Admission Core

### Evidence

- Production deploy `70dccad` had repeated `ESPORTSUSDT` Aster submit rejects:
  `order.rejected` carried `exchange_code=-5018` and the exchange message
  "maximum notional value limit", but the same since-deploy window had no
  `runtime.entry_admission_blocked` and no Aster symbol cooldown for that real
  deterministic reject.
- The Aster endpoint-zero/headroom-zero advisory path was behaving correctly:
  it emitted `aster_headroom_advisory_zero` with nonblocking scope. The broken
  path was the later passive-maker submit reject, where structured
  `exchange_error` evidence stopped inside `EntrySyncExecutor`.
- Bug docs already required this semantic split: endpoint-zero remains advisory,
  real `-5018` is symbol-scoped admission evidence, and `aster:*` is reserved
  for account-level or venue-wide proof.

### Root Cause

`EntrySyncExecutor` converted maker submit rejection into an
`EntryExecutionResult(route=REJECTED)` but only preserved the generic
`reject_reason`. Runtime admission classification then saw mostly
`aster_v3 POST /fapi/v3/order rejected status=400` text and could miss the
structured `exchange_code=-5018` / exchange message / request context that the
adapter had already produced. That left deterministic exchange truth as a
terminal entry abort, not as a reusable admission block.

### Fix

- `EntryExecutionResult` now carries `reject_evidence`.
- `EntrySyncExecutor` copies structured `maker_result.exchange_error` into that
  result when passive maker submit is rejected.
- `EntryDispatchRuntime._record_entry_result_admission_blocks(...)` now merges
  generic reject text with structured evidence, verifies evidence venue scope,
  reuses the existing admission classifier, and records symbol-scoped cooldown
  through the same `_record_symbol_admission_block(...)` path.
- Aster real max-notional reject source is normalized to
  `exchange_5018_fallback`, matching the existing transport diagnostic and bug
  card checklist. Advisory-zero remains nonblocking; no strategy threshold,
  risk limit, spread filter, or order-truth semantic was changed.

### Verification

| Scope | Evidence | Result |
|---|---|---|
| local RED | passive maker reject result lacked `reject_evidence` | failed before fix |
| local RED | structured Aster `-5018` reject did not create symbol admission block; second dispatch still submitted | failed before fix |
| local GREEN | `tests/test_entry_sync.py::TestPassiveMakerLifecycle::test_passive_maker_reject_result_carries_structured_exchange_evidence` | passed |
| local GREEN | `tests/live_harness/test_exchange_admission_incidents.py::test_aster_submit_reject_evidence_creates_symbol_admission_block` | passed; cooldown key `aster:ESPORTSUSDT`, source `exchange_5018_fallback`, no `aster:*` |
| local focused | `tests/live_harness/test_exchange_admission_incidents.py`, `tests/test_entry_sync.py::TestPassiveMakerLifecycle`, Aster transport `-k "aster_5018 or advisory_zero or max_notional"`, `tests/test_diagnose_live.py` | 37 passed; 4 passed; 2 passed / 457 deselected; 191 passed |
| local full | `.venv/bin/pytest -q` | 4500 passed, 9 skipped, 1 warning |
| GitNexus | index fresh at `70dccad`; impact for `_record_entry_result_admission_blocks` LOW; `detect-changes` HIGH because entry dispatch flow changed | accepted with focused and full test coverage |
| cloud deploy | `/opt/lightfee-v2` fast-forwarded `70dccad..e1ea937`; `.deploy_version=e1ea937`; manifest check; singleton; services; since-deploy diagnose | PASS; `version_mismatch=false`, `health.ok=true`, `lifecycle=running`, `risk_mode=running`, `open_position_count=0`, `pending_entry_count=0`, `pending_close_count=0`, `exchange_truth_flat=true`, `exchange_truth_no_open_orders=true`, `gate_passed=true`, conclusion `healthy` / risk `low` |
