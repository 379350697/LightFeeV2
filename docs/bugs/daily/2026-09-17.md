# Bug Ledger Daily - 2026-09-17

## CL-153 自然验证完成 → closed

00:52:53（6h TTL 拦截到期的第一次尝试），部署中的修复在真实 wire 上通过：

- `order.entry_leverage_ready {"venue": "gate", "symbol": "LSK_USDT",
  "position_mode": "dual"}` —— 形状派生标签直接记录了 Gate 实际返回的是
  dual-mode 数组响应（此前审核保留意见"数组形状属推断"由此转为生产观察）。
- 同窗口内 `entry_leverage_unavailable` 零次（00:30-00:31 的拦截事件是
  昨天 18:52 旧代码失败武装的 6h TTL 缓存尾巴，到期后首次尝试即成功）。
- 随后完整的真实交易回合：00:53:40 `entry.opened` 22 张（gate 多/bybit
  空，正是被堵两天的 `lskusdt:gate->bybit` 对）→ 01:00 资金费捕获 →
  01:05 平仓 → 交易所全平、高置信度。
- 保留残留（不阻断本契约）：dual-side retry 路径的数组/非目标 side 反例
  仍未覆盖，作为 monitored 风险记于 v2 批次。

## CL-155 触发侧观察

00:53:51 新仓开立后仅 1 条 `risk.warning_triggered`
（`long_snapshot_unavailable`，min_health 94%），8 分钟持仓期无重发——
转换节流在生产生效。cleared 转换尚未观察到自然样本（仓位终态化时仍处
警告态，与 V1 行为一致）。一腿快照缺失根因（本次为 gate 腿）仍未修。

## CL-154 事故族实时复现 + 新发现：Gate 适配器能力缺口

01:05 平仓走了与 03:05 完全相同的路径（遏制 revert 生效中）：

1. gate maker 提交失败（`passive_close_maker_submit_error`）→
   dual-taker drive → 两腿真实成交：gate `103019842112259486` 卖 22、
   bybit `fda2d7a3-0b01` 买 22（又一次 110072 duplicate-OrderLinkedID，
   对账自愈）。
2. executor PendingClose（`exit.pending_close_registered`，含真实腿）在
   仓位终态化后被孤儿丢弃（`reconciliation.pending_close_orphaned`）——
   CL-154 revert 后的已知行为。
3. **新发现（v2c 范围更新）**：本笔的记账 owner 经
   `aggressive_close_execution` 注册，身份齐全（两腿 22/22、owned 22/22），
   但 33 秒内被判 `exit.billing_evidence_debt_irrecoverable` +
   `automatic_history_terminal_status: irrecoverable_audit_debt`。根因：
   **`GateAdapter` 没有实现 `fetch_order_fill_reconciliation` 与
   `discover_historical_close_fill_reconciliation`**（grep 零命中）——
   gate 腿永远无法通过精确证费 recheck，自动历史路径按
   `history_discovery_unsupported` 能力缺失直接终态化。
4. 影响：CL-153 修复使 gate→bybit 恢复交易后，每一笔未干净对账的
   gate 腿平仓都会产出不可恢复审计债（第 5 条 owner
   `entry-1789577616841-LSKUSDT` 已诞生，operator-import-only）。
   v2c 的 discovery/证费补齐必须包含 Gate 适配器方法
   （Gate v4 `GET /futures/usdt/orders/{id}` 与 `my_trades` 端点），
   存量结算范围随之扩为三笔：两笔 LSK 旧债 + 本笔新债。

## 生产状态（01:0x）

交易所七所全平/无挂单（本笔已平）；lifecycle/risk running；owner 5 条
（2 by-design + 2 旧 LSK + 1 新 gate 腿债）；无运行时错误。
