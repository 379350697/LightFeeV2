# Bug Ledger Daily - 2026-05-20

## GitNexus Index

| Cluster ID | Status | Severity | Fingerprint | Components | Symbols | Files | Commits | Outcome |
|---|---|---:|---|---|---|---|---|---|
| CL-001-hyperliquid-wallet-key-v1-parity-drift | fixed; deployed and cloud-verified | critical | `v2.hyperliquid.exchange-signing.api-secret-used-as-wallet-private-key` | `venue-hyperliquid`, `exchange-signing`, `live-order`, `v1-parity` | `VenueTransport.place_order`, `build_hyperliquid_exchange_payload`, `LiveCredential.wallet_private_key` | `lightfee/venues/transport.py`, `tests/test_venues_transport.py`, `tests/test_venues_contract.py` | `1029089` | Hyperliquid live exchange payload signs with `wallet_private_key`, matching V1 `resolve_wallet()` semantics. Cloud `/opt/lightfee-v2` on `1029089` shows order-path preflight ok for Hyperliquid and no current-run private-key signing errors. |
| CL-002-fail-closed-latch-v1-parity-drift | fixed; deployed and cloud-verified | critical | `v2.fail-closed-latch-clears-with-pending-work-or-sticks-in-clean-disabled-monitor` | `supervisor`, `risk-mode`, `recovery`, `pending-entry`, `v1-parity` | `Supervisor.update_global_risk_mode`, `Supervisor._fail_closed_can_clear`, `Supervisor.supervise`, `needs_reconciliation` | `lightfee/engine/supervisor.py`, `tests/test_supervisor_execution.py` | `1029089` | Fail-closed auto-resume follows V1: do not clear while operator fail-closed, recovery block, or lifecycle-blocking recovery work exists; do clear clean fail-closed state even when risk monitor is disabled. Cloud `/opt/lightfee-v2` on `1029089` shows `risk_mode=running` with zero open/pending work. |
| CL-003-local-l2-exchange-semantics-root-fix | fixed; deployed and cloud-probe verified | high | `v2.local-l2.exchange-semantics.rest-snapshot-as-delta + snapshot-buffered + bitget-checksum-as-sequence + unsupported-position-probe + unsupported-local-l2-symbol` | `local-l2-data-plane`, `local-l2-ws`, `venue-parsers`, `startup-recovery`, `bitget`, `bybit`, `binance`, `okx`, `aster`, `hyperliquid`, `exchange-semantics` | `LocalL2DataPlane.ingest_external_update`, `parse_binance_l2_update`, `parse_okx_l2_update`, `BitgetL2WsClient.parse_depth_message`, `parse_bitget_l2_update`, `parse_bybit_l2_update`, `LiveRuntime._filter_symbols_supported_by_venue`, `LiveRuntime._activate_local_l2_phase`, `LiveRuntime._ensure_l2_active_for_candidates`, `LiveRuntime._restore_local_l2_state`, `BitgetAdapter.supported_symbols`, `BinanceAdapter.ensure_supported_symbols_loaded`, `AsterAdapter.ensure_supported_symbols_loaded`, `HyperliquidAdapter.ensure_supported_symbols_loaded` | `lightfee/marketdata/local_l2_data_plane.py`, `lightfee/marketdata/local_l2_ws.py`, `lightfee/marketdata/local_l2_venues.py`, `lightfee/engine/runtime.py`, `lightfee/venues/bitget.py`, `lightfee/venues/binance.py`, `lightfee/venues/aster.py`, `lightfee/venues/hyperliquid.py`, `scripts/probe_exchange_l2_regressions.py` | `bda5505`, `ba067ff`, `05424e0`, `fd4ee51`, `6b33192` | Exchange-documented REST/WS snapshots now reset/establish local books instead of being parsed as deltas or buffered during bootstrap/rebuild; Binance REST `/fapi/v1/depth`, OKX REST `/api/v5/market/books`, and Bybit REST `/v5/market/orderbook` parse as snapshots; Bitget `seq/pseq` are used as sequence continuity fields instead of checksum; startup fallback position probes plus local-L2 startup, candidate, retained, and persisted snapshot restore paths skip unsupported/non-trading/delisted symbols after loading venue catalogs. |

## Cluster CL-001-hyperliquid-wallet-key-v1-parity-drift

### GitNexus Keys

- Fingerprint: `v2.hyperliquid.exchange-signing.api-secret-used-as-wallet-private-key`
- Components: `venue-hyperliquid`, `exchange-signing`, `live-order`, `v1-parity`
- Symbols: `VenueTransport.place_order`, `build_hyperliquid_exchange_payload`, `LiveCredential.wallet_private_key`
- Files: `lightfee/venues/transport.py`, `tests/test_venues_transport.py`, `tests/test_venues_contract.py`
- Commits: `1029089`
- Watch: Hyperliquid signing errors containing `private key must be exactly 32 bytes`, `order.submit_failed`, `runtime.entry_aborted`

### Summary

Production hedge submit failed because the Hyperliquid signing path passed `api_secret` into the exchange payload signer. Live config validation already required `wallet_private_key`, and V1 resolves/signs Hyperliquid actions through the wallet key path (`LightFee/src/live/hyperliquid.rs::resolve_wallet`).

### Root Cause

V2 had a credential semantic drift: validation used `wallet_private_key`, but `VenueTransport.place_order()` signed Hyperliquid `/exchange` actions with `LiveCredential.api_secret`. When `api_secret` was empty, signing failed before the hedge order could be submitted.

### Fix Status

Fixed, committed, and deployed in `1029089`.

- `VenueTransport.place_order()` now passes `LiveCredential.wallet_private_key` to `build_hyperliquid_exchange_payload()`.
- Added a regression test proving live Hyperliquid order signing uses `wallet_private_key` even when `api_secret` is empty.
- Updated existing Hyperliquid live-order tests to put the valid signing key in `wallet_private_key`, preserving V1 credential semantics instead of teaching tests the old drift.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-20 | local | Red test before fix: `tests/test_venues_transport.py::TestHyperliquidLiveOrderNowSupported::test_live_place_order_signs_with_wallet_private_key_not_api_secret` | Failed with captured private key equal to empty `api_secret`. |
| 2026-05-20 | local | `pytest tests/test_venues_transport.py -k "HyperliquidLiveOrderNowSupported or HyperliquidSigningDependencyPreflight or hyperliquid" -q` | `11 passed, 270 deselected` |
| 2026-05-20 | local | `pytest tests/test_venues_contract.py -k "hyperliquid or place_order" -q` | `42 passed, 91 deselected` |
| 2026-05-20 | cloud `/opt/lightfee-v2` | Commit `1029089`, `lightfee-live.service` active, current-run scan of `/opt/lightfee-v2/runtime/live-events.jsonl` | No current-run `private key must be exactly 32 bytes` / `api_secret` / Hyperliquid order-submit signing failures; `startup.order_path_preflight` for Hyperliquid `/exchange` reported `status=ok`. |

### Regression Watch

- Hyperliquid live orders must never use `api_secret` as an L1 signing key.
- `wallet_private_key` may be configured with or without `0x`; signing must receive that wallet key value.
- A recurrence of `private key must be exactly 32 bytes, instead of 0 bytes` means credential resolution or signer input regressed.

## Cluster CL-002-fail-closed-latch-v1-parity-drift

### GitNexus Keys

- Fingerprint: `v2.fail-closed-latch-clears-with-pending-work-or-sticks-in-clean-disabled-monitor`
- Components: `supervisor`, `risk-mode`, `recovery`, `pending-entry`, `v1-parity`
- Symbols: `Supervisor.update_global_risk_mode`, `Supervisor._fail_closed_can_clear`, `Supervisor.supervise`, `needs_reconciliation`
- Files: `lightfee/engine/supervisor.py`, `tests/test_supervisor_execution.py`
- Commits: `1029089`
- Watch: `risk.fail_closed_auto_resumed`, `risk_mode=fail_closed`, `pending_entry_count`, `open_position_count`, `pending_close_count`, `recovery_blocked_reason`

### Summary

V2 fail-closed recovery was not equivalent to V1. One path could clear the fail-closed risk mode while pending recovery work still existed; another path could leave a clean fail-closed latch sticky when `risk_monitor_enabled=false`, because `supervise()` returned before latch recovery.

### Root Cause

V1 `fail_closed_latch_can_clear()` is based on `state_is_safe_to_resume(recovery_work_snapshot())` plus no operator fail-closed override. V2 approximated this with a narrow check on `recovery_blocked_reason` and selected open-position flags, which missed pending entries/pending closes/reconcile work. V2 also skipped the V1 recompute/auto-resume path when the risk monitor was disabled.

### Fix Status

Fixed, committed, and deployed in `1029089`.

- `_fail_closed_can_clear()` now blocks on operator fail-closed, `recovery_blocked_reason`, and `needs_reconciliation(self.state)`.
- `update_global_risk_mode()` keeps `FAIL_CLOSED` latched when health recovers but recovery work is still present.
- `supervise()` now performs the clean fail-closed auto-resume path before returning when the risk monitor is disabled.

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-20 | local | Red tests before fix: pending entry work and disabled-monitor clean latch cases | First cleared incorrectly; second stayed sticky incorrectly. |
| 2026-05-20 | local | `pytest tests/test_supervisor_execution.py tests/test_recovery_reconciliation.py -q` | `58 passed` |
| 2026-05-20 | local | `pytest tests/test_live_entry_hedge_root_fix.py tests/test_runtime_entry_flow.py tests/test_entry_sync.py tests/test_entry_state_machine.py tests/test_entry_local_l2.py -q` | `251 passed` |
| 2026-05-20 | cloud `/opt/lightfee-v2` | Commit `1029089`, `lightfee-live.service` active, current `/opt/lightfee-v2/runtime/live-state.json` | `lifecycle=running`, `risk_mode=running`, `open_position_count=0`, `pending_entry_count=0`, `pending_close_count=0`, `recovery_blocked_reason=null`. |

### Regression Watch

- `risk.fail_closed_auto_resumed` must not appear while `needs_reconciliation(state)` is true.
- A clean fail-closed state with no pending/open/reconcile work should resume to running on the next supervisor tick, including when `risk_monitor_enabled=false`.
- Operator-requested fail-closed must remain latched until operator control is removed.

## Cluster CL-003-local-l2-exchange-semantics-root-fix

### GitNexus Keys

- Fingerprint: `v2.local-l2.exchange-semantics.rest-snapshot-as-delta + snapshot-buffered + bitget-checksum-as-sequence + unsupported-position-probe + unsupported-local-l2-symbol`
- Components: `local-l2-data-plane`, `local-l2-ws`, `venue-parsers`, `startup-recovery`, `bitget`, `bybit`, `binance`, `okx`, `aster`, `hyperliquid`, `exchange-semantics`
- Symbols: `LocalL2DataPlane.ingest_external_update`, `parse_binance_l2_update`, `parse_okx_l2_update`, `BitgetL2WsClient.parse_depth_message`, `parse_bitget_l2_update`, `parse_bybit_l2_update`, `LiveRuntime._filter_symbols_supported_by_venue`, `LiveRuntime._activate_local_l2_phase`, `LiveRuntime._ensure_l2_active_for_candidates`, `LiveRuntime._restore_local_l2_state`, `BitgetAdapter.supported_symbols`, `BinanceAdapter.ensure_supported_symbols_loaded`, `AsterAdapter.ensure_supported_symbols_loaded`, `HyperliquidAdapter.ensure_supported_symbols_loaded`
- Files: `lightfee/marketdata/local_l2_data_plane.py`, `lightfee/marketdata/local_l2_ws.py`, `lightfee/marketdata/local_l2_venues.py`, `lightfee/engine/runtime.py`, `lightfee/venues/bitget.py`, `lightfee/venues/binance.py`, `lightfee/venues/aster.py`, `lightfee/venues/hyperliquid.py`, `scripts/probe_exchange_l2_regressions.py`
- Commits: `bda5505`, `ba067ff`, `05424e0`, `fd4ee51`, `6b33192`
- Watch: `runtime.local_l2_snapshot_error`, `runtime.local_l2_buffer_overflow_rebuild`, `runtime.entry_local_l2_readiness_diagnostics`, `recovery.live_position_probe_error`, `recovery.live_position_probe_symbol_skipped`, `runtime.local_l2_symbol_skipped`

### Summary

Post-`1029089` cloud evidence showed Hyperliquid signing and clean fail-closed were fixed, but local-L2 and Bitget startup probe noise remained. These were not V1 fail-closed/signing parity issues; they were exchange-semantics defects in the V2 local-L2 ingestion/parsing/probing layer.

### Root Cause

Five independent root causes were confirmed:

- Binance USD-M REST `/fapi/v1/depth` returns a full orderbook snapshot with `lastUpdateId`, `E`, and `T`; V2 treated the presence of `E` as a delta marker and merged the REST snapshot into the existing book.
- OKX REST `/api/v5/market/books` returns a snapshot in `data` without an `action` field; V2 defaulted missing `action` to `update`, again merging a REST snapshot as a delta.
- Authoritative non-Binance snapshots were buffered while books were `BOOTSTRAPPING`/`REBUILDING`, so a valid exchange snapshot could fail to establish a HOT local book.
- Bitget WS parsing treated `checksum` as the book sequence, even though current Bitget UTA depth docs use `seq` and `pseq` for continuity and the 2026-05-12 Bitget changelog removes `checksum` from depth pushes.
- Bybit REST `/v5/market/orderbook` returns orderbook fields under `result` and represents a current snapshot; the parser only handled WS-style `data`, causing valid REST snapshots to look like empty-side books.
- Startup fallback position probes asked Bitget about symbols not present in its contract catalog, producing avoidable unsupported/delisted-symbol and rate-limit errors.
- Local-L2 activation, retained-book recovery, and full persisted book snapshot restore used raw candidate/retained/restored symbols without consulting venue trading catalogs. Cloud evidence after `ba067ff` and the first symbol-catalog deploy showed Binance `SYSUSDT` depth returning empty sides while `/fapi/v1/exchangeInfo` reported the contract as `SETTLING`, so the correct root fix is to keep non-`TRADING`/non-`PERPETUAL` contracts out of every active L2 book creation/restore path.
- The next live run proved the same catalog rule had to cover Aster and Hyperliquid: Aster `RLSUSDT` was absent from `/fapi/v1/exchangeInfo` and its depth endpoint returned exchange 400 for delivering/settling/closed/pre-trading; Hyperliquid `MAV` was present in `meta.universe` with `isDelisted=true` and returned an empty `l2Book` bid side.

### Fix Status

Fixed, deployed, and cloud-probe verified.

- `LocalL2DataPlane.ingest_external_update()` applies authoritative `SNAPSHOT` updates immediately during bootstrap/rebuild, clears the pre-snapshot buffer, and transitions a successfully applied book to HOT.
- `parse_binance_l2_update()` treats REST `/fapi/v1/depth` payloads with `lastUpdateId` as snapshots even when Binance includes `E`/`T`, and uses `T`/`E` as the exchange event time.
- `parse_okx_l2_update()` treats REST `/api/v5/market/books` payloads without `action` as snapshots.
- `BitgetL2WsClient.parse_depth_message()` and `parse_bitget_l2_update()` parse `seq`, `pseq`, `b/a`, and `checksum` according to current Bitget payloads.
- `parse_bybit_l2_update()` accepts REST `result` wrappers as snapshots and uses Bybit `u` as the update sequence.
- `LiveRuntime._fetch_startup_live_position_snapshots()` now loads a venue catalog when available and filters fallback per-symbol probes to symbols supported by that venue.
- `BitgetAdapter` exposes `supported_symbols()` and lazy `ensure_supported_symbols_loaded()` backed by the contract metadata loader.
- `BinanceAdapter` exposes `supported_symbols()` and lazy `ensure_supported_symbols_loaded()` backed by `/fapi/v1/exchangeInfo`, filtered to `status=TRADING` and `contractType=PERPETUAL`.
- `AsterAdapter` exposes the same catalog contract, backed by `/fapi/v1/exchangeInfo`, filtering out non-`TRADING` and non-perpetual contracts.
- `HyperliquidAdapter` exposes the same catalog contract, backed by `/info {"type":"meta"}`, filtering out `isDelisted=true` perp assets while preserving Hyperliquid's native asset names for `_venue_symbol()` matching.
- `LiveRuntime._filter_symbols_supported_by_venue()` is shared by startup live-position probes, local-L2 startup activation, candidate-triggered local-L2 activation, retained-book recovery, and full persisted local-L2 snapshot restore; unsupported venue symbols are skipped before any book/bootstrap/restore path is created.
- `scripts/probe_exchange_l2_regressions.py` fetches real public exchange payloads for the previously failing cases and asserts the current parser/catalog behavior against those payloads.

### Official Exchange Evidence

| Venue | Evidence | Code Consequence |
|---|---|---|
| Binance REST | USD-M REST `/fapi/v1/depth` documents `lastUpdateId`, `E`, `T`, `bids`, and `asks` in the response example, and the local orderbook guide uses REST snapshots to establish the local book before replaying depth updates. | `lastUpdateId` REST payloads must be snapshots even when `E`/`T` are present. |
| Binance catalog | USD-M `/fapi/v1/exchangeInfo` exposes per-symbol `status` and `contractType`; live evidence for `SYSUSDT` showed `status=SETTLING` and empty depth. | Active local-L2 symbols must be filtered to trading perpetual contracts before book creation/bootstrap. |
| Aster catalog | Aster `/fapi/v1/exchangeInfo` is documented as current exchange trading rules and symbol information, and Aster contract statuses include pre-trading/settling/closed states. Live evidence showed `RLSUSDT` absent from exchangeInfo and depth returning exchange 400. | Aster active local-L2 symbols must be present in exchangeInfo and trading perpetual before book creation/bootstrap. |
| Hyperliquid catalog | Hyperliquid `/info {"type":"meta"}` returns the perp universe used for perp coin names, while `/info {"type":"l2Book"}` expects `coin` from that universe. Live evidence showed `MAV isDelisted=true`. | Hyperliquid active local-L2 symbols must be filtered through non-delisted meta universe names before l2Book bootstrap. |
| OKX | REST `/api/v5/market/books` returns `data[0].asks`, `data[0].bids`, `ts`, and `seqId` as an orderbook response; WS docs use `action` for snapshot/update messages. | REST payloads without `action` must be snapshots, not deltas. |
| Bybit | REST `/v5/market/orderbook` says the response is a current-time slice and returns `result.s`, `result.b`, `result.a`, `result.ts`, `result.u`, and `result.seq`; WS orderbook docs say a new `snapshot` resets the local orderbook. | REST `result` must parse as a snapshot, and snapshots must be allowed to establish/reset local state. |
| Bitget | UTA depth docs define `seq` as the serial number and `pseq` as previous-push serial; the 2026-05-12 changelog removes `checksum` from depth channels and says to use `seq/pseq` for consistency verification. | Parser continuity must use `seq/pseq`, not `checksum`. |
| Snapshot bootstrap model | Binance local orderbook docs require buffering deltas, fetching a REST snapshot, setting the local book to that snapshot, then replaying applicable deltas. | Snapshot-first bootstrap remains the model; authoritative snapshots must not be trapped in the delta buffer. |

### Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-20 | local | Red test before fix: `TestDataPlaneBootstrap::test_external_snapshot_during_bootstrap_applies_and_completes_hot` | Failed because the snapshot was buffered and returned no apply events. |
| 2026-05-20 | cloud `/opt/lightfee-v2` after `bda5505` | Current run `lightfee-1779290962611-1222219` showed `runtime.local_l2_snapshot_error` with Binance/OKX crossed books; direct transport probes showed Binance and OKX REST depth payloads were parsed as `kind=delta`. | Confirmed remaining REST snapshot-as-delta root cause. |
| 2026-05-20 | local | Red tests before fix: Binance USD-M REST depth with `lastUpdateId`/`E`/`T`, OKX REST books without `action` | Both failed with `update_kind=delta`. |
| 2026-05-20 | local | Red tests before fix: Bitget WS/parser snapshot and delta sequence assertions | Failed because `sequence` came from `checksum`, not `seq`. |
| 2026-05-20 | local | Red test before fix: Bybit REST `result` wrapper parser | Failed because REST orderbook was treated as a delta over empty payload fields. |
| 2026-05-20 | local | Red tests before fix: startup fallback position probe filters and lazy Bitget catalog load | Failed because unsupported symbols were still probed and the catalog was not loaded. |
| 2026-05-20 | local | `pytest tests/test_local_l2_runtime.py tests/test_local_l2_ws.py tests/test_local_l2_venue_rules.py tests/test_live_startup_preflight.py tests/test_venues_transport.py::TestBitgetAdapterL2MetadataGuard tests/test_venues_transport.py::TestBitgetL2Guard -q` | `208 passed` |
| 2026-05-20 | local | `pytest tests/test_entry_local_l2.py tests/test_runtime_maker_event_local_l2.py tests/test_marketdata_l2.py tests/marketdata/test_local_l2_runtime_targets.py tests/test_supervisor_execution.py tests/test_engine_recovery.py tests/test_recovery_reconciliation.py tests/recovery/test_restart_recovery_semantics.py tests/ops/test_production_health.py tests/ops/test_current_state_and_metrics_export.py -q` | `404 passed` |
| 2026-05-20 | local | `pytest tests/test_venues_transport.py tests/test_venues_contract.py tests/venues/test_v1_capability_matrix.py tests/venues/test_market_data_client.py tests/venues/test_order_sizing_and_headroom.py -q` | `509 passed` |
| 2026-05-20 | local | `pytest -q` | `2809 passed` |
| 2026-05-20 | local | `pytest -q` after Binance/OKX REST snapshot classification fix | `2811 passed` |
| 2026-05-20 | cloud `/opt/lightfee-v2` after `ba067ff` | Current state-run logs plus direct public probes | No Binance/OKX REST snapshot-as-delta errors; remaining `runtime.local_l2_snapshot_error` samples were Binance `SYSUSDT` `book_empty_side_bid`, and direct `/fapi/v1/exchangeInfo` showed `SYSUSDT status=SETTLING`. |
| 2026-05-20 | local | Red test before fix: local-L2 candidate activation with Binance catalog supporting only `BTCUSDT` | Failed because `SYSUSDT` still created a local-L2 book. |
| 2026-05-20 | local | Red test before fix: local-L2 startup bootstrap with WS disabled and retained `SYSUSDT` | Failed because retained-book recovery recreated `SYSUSDT` after filtering. |
| 2026-05-20 | local | Red test before fix: Binance adapter catalog with `TRADING`/`SETTLING`/quarterly contracts | Failed before the adapter had a filtered supported-symbol catalog. |
| 2026-05-20 | local | `pytest tests/test_live_startup_preflight.py tests/test_venues_transport.py::TestBinanceAdapterSymbolCatalog tests/test_venues_transport.py::TestBitgetAdapterL2MetadataGuard tests/test_local_l2_venue_rules.py -q` | `102 passed` |
| 2026-05-20 | local | `pytest tests/test_entry_local_l2.py tests/test_runtime_maker_event_local_l2.py tests/test_marketdata_l2.py tests/marketdata/test_local_l2_runtime_targets.py tests/test_supervisor_execution.py tests/test_engine_recovery.py tests/test_recovery_reconciliation.py tests/recovery/test_restart_recovery_semantics.py tests/ops/test_production_health.py tests/ops/test_current_state_and_metrics_export.py -q` | `404 passed` |
| 2026-05-20 | local | `pytest tests/test_venues_transport.py tests/test_venues_contract.py tests/venues/test_v1_capability_matrix.py tests/venues/test_market_data_client.py tests/venues/test_order_sizing_and_headroom.py -q` after Binance catalog follow-up | `510 passed` |
| 2026-05-20 | local | `pytest -q` after Binance catalog follow-up | `2814 passed` |
| 2026-05-20 | local sandbox | `PYTHONPATH=. python3 scripts/probe_exchange_l2_regressions.py --timeout-s 5 --json` | Expected DNS-blocked sandbox failure for all public endpoints; cloud run remains the authoritative live probe. |
| 2026-05-20 | cloud `/opt/lightfee-v2` after first symbol-catalog deploy | Current run `lightfee-1779294845205-1226349` | Public probe passed, but current-run logs still showed Binance `SYSUSDT` `book_empty_side_bid`; evidence proved persisted `local_l2_books_snapshot` restore could resurrect unsupported books after candidate filtering. |
| 2026-05-20 | local | Red test before restore-path fix: persisted full-book snapshot containing `BTCUSDT` and `SYSUSDT` with catalog supporting only `BTCUSDT` | Failed because `_restore_local_l2_state()` recreated `SYSUSDT`. |
| 2026-05-20 | local | `pytest tests/test_live_startup_preflight.py tests/test_venues_transport.py::TestBinanceAdapterSymbolCatalog tests/test_venues_transport.py::TestBitgetAdapterL2MetadataGuard tests/test_local_l2_venue_rules.py -q` after restore-path fix | `103 passed` |
| 2026-05-20 | local | `pytest -q` after restore-path fix | `2815 passed` |
| 2026-05-20 | cloud `/opt/lightfee-v2` after `fd4ee51` | Current run `lightfee-1779295438642-1227267` | Binance `SYSUSDT` no longer emitted snapshot errors and appeared as `runtime.local_l2_symbol_skipped`; remaining rootable errors were Aster `RLSUSDT` exchange 400 and Hyperliquid `MAVUSDT` empty side. |
| 2026-05-20 | cloud public probe | Aster `/fapi/v1/exchangeInfo`, Hyperliquid `/info {"type":"meta"}` | `RLSUSDT` absent from Aster exchangeInfo; Hyperliquid `MAV` present with `isDelisted=true`. |
| 2026-05-20 | local | Red tests before Aster/Hyperliquid catalog fix: Aster catalog excludes settling/non-perp contracts; Hyperliquid catalog excludes `isDelisted=true` assets | Failed before adapters exposed filtered supported-symbol catalogs. |
| 2026-05-20 | local | `pytest tests/test_live_startup_preflight.py tests/test_venues_transport.py::TestAsterAdapterSymbolCatalog tests/test_venues_transport.py::TestHyperliquidAdapterSymbolCatalog tests/test_venues_transport.py::TestBinanceAdapterSymbolCatalog tests/test_venues_transport.py::TestBitgetAdapterL2MetadataGuard tests/test_local_l2_venue_rules.py -q` | `105 passed` |
| 2026-05-20 | local | `pytest tests/test_venues_transport.py tests/test_venues_contract.py tests/venues/test_v1_capability_matrix.py tests/venues/test_market_data_client.py tests/venues/test_order_sizing_and_headroom.py -q` after Aster/Hyperliquid catalog fix | `512 passed` |
| 2026-05-20 | local | `pytest -q` after Aster/Hyperliquid catalog fix | `2817 passed` |
| 2026-05-20 | cloud `/opt/lightfee-v2` after `6b33192` | `scripts/probe_exchange_l2_regressions.py --timeout-s 12 --json` | `ok=true`; Binance, Aster, OKX, Bybit, and Bitget real REST books parsed as `snapshot`; Binance `SYSUSDT status=SETTLING` not supported; Aster `RLSUSDT` not supported; Hyperliquid `MAV isDelisted=true` not supported. |
| 2026-05-20 | cloud `/opt/lightfee-v2` after `6b33192` | Current event run `lightfee-1779296116362-1228299` from `/opt/lightfee-v2/runtime/live-events.jsonl` and `/opt/lightfee-v2/runtime/live-state.json` | `snapshot_errors=[]`, `probe_error_count=0`, no Hyperliquid private-key/api-secret signing terms, `runtime.local_l2_symbol_skipped` includes `hyperliquid/MAVUSDT` and `aster/RLSUSDT`; state is `lifecycle=running`, `risk_mode=running`, `degraded_venues=[]`. |

### Regression Watch

- `book_bootstrapping`/`book_rebuilding` after this fix should be tied to real missing snapshots, exchange restarts, disconnects, sequence gaps, or unsupported symbols, not to parser/schema drift.
- Binance/OKX REST depth probes must report `update_kind=snapshot`; seeing `delta` for `/fapi/v1/depth` or `/api/v5/market/books` means the parser regressed.
- `book_empty_side_bid` on Bybit REST snapshots should not recur for payloads with `result.b`/`result.a`.
- Bitget orderbook consistency checks should use `seq/pseq`; a missing `checksum` must not break parsing.
- Bitget startup recovery should show skipped unsupported symbols instead of repeated per-symbol probe errors for removed contracts.
- Binance non-`TRADING` or non-`PERPETUAL` symbols must appear as `runtime.local_l2_symbol_skipped` or be absent from current-run L2 books, not as recurring empty-side snapshot errors.
- Cloud deployment acceptance must include `scripts/probe_exchange_l2_regressions.py` against real public endpoints plus state-run log checks scoped to `/opt/lightfee-v2/runtime/live-state.json`'s current `run_id`.

## Full Verification

| Date | Environment | Command / Evidence | Result |
|---|---|---|---|
| 2026-05-20 | local | `python3 -m compileall -q lightfee` | passed |
| 2026-05-20 | local | `git diff --check` | clean |
| 2026-05-20 | local | `pytest -q` | `2803 passed, 2 skipped, 1 warning` |
| 2026-05-20 | local | `pytest -q` after CL-003 root fixes | `2809 passed` |
| 2026-05-20 | local | `pytest -q` after CL-003 Binance/OKX REST snapshot follow-up | `2811 passed` |
| 2026-05-20 | local | `pytest -q` after CL-003 Binance catalog/local-L2 target filtering follow-up | `2814 passed` |
| 2026-05-20 | local | `pytest -q` after CL-003 persisted local-L2 snapshot restore filtering follow-up | `2815 passed` |
| 2026-05-20 | local | `pytest -q` after CL-003 Aster/Hyperliquid catalog filtering follow-up | `2817 passed` |

## Residual Classification

- `book_bootstrapping`, `book_rebuilding`, `book_timestamp_missing`, and `book_empty_side` are partially reduced by CL-003 where the root cause was parser/schema drift or snapshot buffering. Remaining live occurrences require post-deploy verification and, where V1/V2 already match, venue-specific evidence from exchange semantics.
- `entry_waiting_for_finalization_window_too_early` remains a strategy timing gate, not a strategy-unrelated defect.
- Pending/reconcile exchange uncertainty and below-min-notional residual handling are not solved by blindly copying V1 alone; V1 supplies the state-machine/idempotency contract, while venue order/fill/position evidence still determines the safe terminal action.
